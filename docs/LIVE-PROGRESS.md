# LIVE PROGRESS — running log (updated after every step)

**For a NEW session:** open Claude Code in `~/Code/practicum-v2` and say:
> Read docs/LIVE-PROGRESS.md and continue from the last line.

**Yariv's copy-link (GitHub):** https://github.com/itzkovichyariv-star/Practicum-v2/blob/main/docs/LIVE-PROGRESS.md
**Local path:** /Users/yarivitzkovich/Code/practicum-v2/docs/LIVE-PROGRESS.md

Binding rule (same as family-tasks): during any working session the assistant appends a line here after EVERY meaningful step and commits it (and pushes, once a remote exists). Newest entries at the bottom.

---
## LOG (newest at bottom)

- 2026-06-11 — Live-progress log created (family-tasks pattern). BLOCKER for the live link: no GitHub remote on this repo — Yariv to approve creating a PRIVATE repo (or create one and `git remote add origin <url>`).
- 2026-06-11 — Remote wired to github.com/itzkovichyariv-star/Practicum-v2. The GitHub copy was a stale April-27 upload (66 commits, unrelated history); plan: ours-merge absorbing it + normal push, executed by Yariv (assistant push was permission-gated). Verified before touching anything: Cloudflare Pages has NO git integration for practicum-v2 (deploys = direct upload from the Mac), live site 200 serving v1.14.4+build.43 — students/registration unaffected by ANY GitHub operation.
- 2026-06-11 — 🔴 INCIDENT + RECOVERY: עינה נוימן accidentally reverted to submissions (13:42Z) and re-accepted (13:43Z) → her candidate card was rebuilt bare: interviewConducted, evalScore 90, all 5 eval dimensions, interview summary and preferredArea wiped. FULL restore performed from snapshot v879 (12:24Z, practicum_snapshots table — the auto-snapshot-on-every-save system held; last 50 versions kept). Verified live in app: badge ראיון בוצע back, score 90, summary intact, original record id restored.
- 2026-06-11 — Safety guard shipped (v1.14.5, f70bbc18): revert-to-submission — the one destructive action with NO confirm — now shows a detailed warning listing exactly which of THAT candidate's data will be lost + points to ניהול→גרסאות for recovery; candidate-delete confirm strengthened; both write labeled snapshots (action/entity/target) instead of anonymous 'שמירה'. Browser-verified: click ↩ הגשות → correct warning with her 4 at-risk items, cancel → record untouched.
- 2026-06-14 — Acceptance email overhaul shipped (v1.15.0, [33ab0de5](https://github.com/itzkovichyariv-star/Practicum-v2/commit/33ab0de5)). Three changes: (1) Edge Function `notify-acceptance` rewritten with branded HTML matching the Outlook-draft wording, pulls `workshopDate` from the candidate's course (substitutes `{{תאריך_סדנה}}` server-side). (2) New warning callout under "השיבוץ יחל רק לאחר..." paragraph — *"⚠️ אנא אל תעשה שימוש בקישור לפני סיום סדנת ההכנה. רשימת הארגונים הקיימת בקישור אינה מעודכנת בשלב זה. היא תתעדכן עד לסיום הסדנה."* — added to BOTH templates (Outlook draft as plain text with ⚠️ prefix; HTML as warm cream / amber-bordered panel). (3) `autoSendAcceptance` checkbox in course settings was previously decorative — now wired: when true on the candidate's course, "passed" auto-fires the Resend HTML email and skips the Outlook-draft modal; when false, current Outlook flow preserved (fail-safe: if Resend send errors, falls back to opening the Outlook modal). Live data: all 7 courses set to `autoSendAcceptance: false` except `hr-practicum-tashpaz` (תשפ״ז active practicum) which is `true` for smoke-testing. Gate: 26/27 cells pass (cell 04 accept-reject 6/6 incl `EMAIL-workshop-date` resolved); the 1 red (`REG-onscreen-zoom` on cell 01) is a pre-existing audit-fixture bug — it asserts a Zoom block must appear, but Zoom links are manually planted per-day (no auto-gen) so when the test slot lands on a day without a planted link the success page correctly shows nothing. Unrelated to my surface area. Deployed: Cloudflare Pages https://31ac66a6.practicum-v2.pages.dev (also https://practicum.yarivitzkovich.org) at v1.15.0+build.45; Supabase Edge Function `notify-acceptance` deployed to project vpqgmcmavnszcnakhiat. Yariv will smoke-test with a fake candidate on `hr-practicum-tashpaz` (the only course with auto-send enabled).
- 2026-07-08 — 🔴→✅ Employer-feedback link ("משוב למעסיק") was reaching employers as **"הקישור אינו תקין"** (reported for **פרקטיקום משאבי אנוש תשפ״ו** = course `hr-practicum`). Root-caused **two** distinct bugs and shipped the fix in **v1.15.1+build.46 (`7be3f47d`)**, deploy `https://e697e7f4.practicum-v2.pages.dev` (live on https://practicum.yarivitzkovich.org). **Diagnosis (evidence-based, not guessed):** the page + tokens + anon-read were all healthy (proved שירי's live link rendered the full form); the failure was in link *delivery*. (1) **Email — deterministic:** the emailed URL was a bare 75-char plain-text link (`…/feedback?token=fb-…`) that WRAPS at the mail client's ~72-char line limit, splitting the `?token=…` so the employer clicked a truncated link → no token → "הקישור אינו תקין". (2) **Copy-link — intermittent (Yariv's example: אלבינה מיכלין):** the token was being **regenerated/overwritten** because `saveSnapshot` (40 call-sites, one shared `data` blob) had **NO concurrency guard** — any save carrying slightly-stale in-memory data silently reverted the whole blob, dropping a just-written feedback token; the next click saw an empty token and minted a *new* one, killing the link already sent while a fresh copy worked. **Fix (5 parts, all shipped):** (a) `saveSnapshot` now uses **optimistic concurrency — version compare-and-swap + bounded retry**, with a safety-floor fallback that never fails a save that would have succeeded before (verified at REST level: match→1 row, stale→0 rows; save-path gate cell 17 green). (b) new **`ensureFeedbackToken(studentId)`** reads the cloud fresh, **never regenerates** an existing token, and **reads it back to confirm** it landed before returning — the send/copy paths refuse to emit a link whose token isn't DB-verified. (c) new short **`/f?t=` route** (~57-char URL; `/feedback?token=` still resolves for already-sent links; `EmployerFeedback` now reads both `t` and `token`, trimmed). (d) link emitted **on its own line, LTR-isolated (U+200E), with a copy-paste fallback**. (e) links now built from the **hardcoded prod domain** (`dataApi.FEEDBACK_BASE_URL`), never `window.location.origin`, so a preview/localhost admin session can't emit a dead-origin link. Also: an **always-visible feedback-link box** in the student editor (the copy-fallback toast already referenced it) + buttons disabled while the token verifies. **Verification:** new gate cell **`28-feedback-link-resolves`** (generate via the copy button → assert short prod URL → assert token persisted to DB → open `/f?t=` and assert the eval FORM renders, not "invalid" → assert idempotent, no regeneration) **4/4**; cell 26 + save-cell 17 green; **full gate green except the same pre-existing `REG-onscreen-zoom` fixture red** (cell 05 flaked once under shared-DB contention, green on re-run). **Live-verified in prod:** `https://practicum.yarivitzkovich.org/f?t=fb-1-4m22s0pa-mrc2h5xr` renders אלבינה מיכלין's full form on v1.15.1+build.46. NOTE: per Yariv's choice the Outlook-DRAFT flow is KEPT (link opens in his Outlook to review+send) — not switched to server-send. OPEN/none.
- 2026-07-08 (later) — 🔴→✅ **Follow-up regression fix, shipped v1.15.2+build.47 (`dac948ea`)**, deploy `https://238649fe.practicum-v2.pages.dev` (live on https://practicum.yarivitzkovich.org). Yariv re-tested v15.1 and hit: **"שלח מייל לא עובד — לא מגיב בכלל" + "העתקת קישור נותנת שגיאה"** (screenshot showed the "⚠️ לא ניתן להעתיק — העתק ידנית מהתיבה למטה" toast). **Root cause = a regression v15.1 introduced:** `ensureFeedbackToken` made every send/copy click do an **async DB round-trip** (read → CAS-save → read-back) BEFORE the `navigator.clipboard.writeText` / `window.open(mailto)`. Browsers (Safari) **consume the click's user-activation across that async gap**, so both the clipboard write and the mailto popup were **blocked** → "doesn't respond / can't copy". **Fix:** split the path — `readyFeedbackUrl()` builds the URL **synchronously** from the already-verified `feedbackToken` (zero awaiting) so copy/mailto/whatsapp run **inside the click's own gesture**; the async `createFeedbackUrl()` is used **only the first time** a student has no token yet, and in that case we reveal the link box + toast "לחצו שוב" (a second click then hits the synchronous gesture-safe path — we can't copy/open-mail off an awaited result). Net UX: **already-tokened students (all 5 tested incl. Albina/אור, and every student after its first link) = single click works**; a brand-new token = one prepare-click then one action-click. **Real-browser verified (Chrome MCP, coordinate click on a tokened student):** `navigator.clipboard.writeText` is called in-gesture and **RESOLVES SUCCESS** with the short prod URL (`…/f?t=fb-1-…`); box renders the link. Gate: cells 28/26/17 green; full gate green except the same pre-existing `REG-onscreen-zoom` red. Prod serves `v1.15.2+build.47.dac948ea`; `/f?t=` still 200. **OPEN:** Yariv to smoke-test the actual Outlook→employer email round-trip on his own Safari (the failing device) — the in-gesture fix is verified-by-construction + Chrome-verified, but his real mail client is the final confirmation. ✅ 2026-07-08 Yariv confirmed "נראה שזה עובד" (feedback link works).
- 2026-07-08 (later²) — ✨ **New feature shipped: Students-screen group-email by division — v1.16.0+build.48 (`3a96828b`)**, deploy `https://8bd5506e.practicum-v2.pages.dev` (live on https://practicum.yarivitzkovich.org). Yariv: Candidates has a group/several email tool, Students didn't (he thought). **Finding:** it DID exist in Students but hidden behind the tiny "☑ בחר מספר" toggle → manual tick → "📧 מייל Outlook" (same `mailto:?bcc=` engine as Candidates). **Built (design-approved, all 7 divisions Yariv picked):** a prominent header button **"✉ מייל לקבוצה"** → compose modal with a **division chip selector** (`כולם · שובצו בארגון · טרם שובצו · נקלטו לעבודה · סיימו פרקטיקום · עברו הכנה · משוב מעסיק חסר`), each chip showing its live count; recipients = current **course+year context** filtered by the chosen division (defaults to the active stage tab); live recipient count + names + a warning listing anyone without an email; **📧 פתח ב‑Outlook** → `mailto:?bcc=<recipients>` opens **Yariv's own Outlook** so it sends from **yarivi@ariel.ac.il** (no server-send). Existing manual multi-select kept. Impl in `StudentsPage.tsx` (MAIL_BUCKETS predicates + mailMode 'bucket'|'selected' + mailRecipients useMemo). **Verified:** new gate cell **`29-group-email`** (button opens modal + chips; a placed test student appears only under "שובצו בארגון", a not-placed one only under "טרם שובצו") **2/2**; screenshot confirmed the modal renders with counts + no-email warning. Full gate 27/30 green — only consistent red is the pre-existing `REG-onscreen-zoom` (cell 01); cells 05/17 flake under full-gate shared-DB contention but **pass in isolation** (cell 17 = save path, confirmed unaffected by the v15.1 CAS change). **NOTE (dev-env):** Chrome-MCP manual session-injection preview kept blanking (HMR/websocket rot after a long edit session — `App.tsx useTheme` null-useState); the Playwright audit harness renders the app reliably and is the source of truth for UI verification here. OPEN/none.
- 2026-07-08 (later³) — 🔧 **Follow-up UX: Students selection now matches Candidates exactly — v1.16.1+build.49 (`3ebbc616`)**, deploy `https://a2828837.practicum-v2.pages.dev` (live on https://practicum.yarivitzkovich.org). Yariv couldn't find the v1.16.0 "✉ מייל לקבוצה" button (it's in the header, which scrolls off) and asked to make it "exactly like Candidates — a checkbox to pick individuals + the options above all the names". **Change (StudentsPage.tsx):** (1) row checkboxes are now **always visible** (removed the `selectMode` gate + the "☑ בחר מספר" header toggle + the floating bottom bar); (2) a **persistent action row above the list** (under the filter chips): `☑ בחר הכל (N)` + once selected → `N נבחרו · 📧 מייל Outlook · ✓ הודעת קבלה · ✗ הודעת דחייה · ביטול בחירה` — the exact Candidates layout. Kept the header `✉ מייל לקבוצה` division shortcut (bucket path) too. All email paths still `mailto:?bcc=` → the coordinator's own Outlook (from yarivi@ariel.ac.il). **Verified:** gate cell `29-group-email` extended to **3/3** (bucket modal + division predicates + always-visible checkbox → "בחר הכל" → inline "📧 מייל Outlook" opens the compose modal); screenshot confirmed the checkboxes + action row render like Candidates. Full gate **29/30** green (only the pre-existing `REG-onscreen-zoom` fixture red). Prod serves `v1.16.1+build.49.3ebbc616`. OPEN/none.
- 2026-07-08 (later⁴) — 🔕 **Stopped the GitHub CI failure-email spam (repo settings, not code).** Yariv was getting many "pages build and deployment … Failed" emails (one per push to `main`). Root cause: **GitHub Pages was enabled** on `itzkovichyariv-star/Practicum-v2` with a **legacy Jekyll build from `main`** (`build_type: legacy`, `source: main:/`, `status: errored`) — but this is an **Astro** app, so GitHub's Jekyll `pages-build-deployment` fails on EVERY push (failing since ~2026-06-14; today's 4 deploy pushes generated a fresh batch). It's the ONLY workflow (no `.github/workflows/` in the repo) and it's **unused** — the app is served by **Cloudflare Pages** (`practicum.yarivitzkovich.org`), the github.io Pages URL never built successfully and serves nothing. **Fix (Yariv approved):** `gh api -X DELETE repos/itzkovichyariv-star/Practicum-v2/pages` → Pages now disabled (GET returns 404). No more failure emails; Cloudflare deploys unaffected; reversible (re-enable in repo Settings → Pages if ever needed). NOTE for future sessions: **do NOT re-enable GitHub Pages** for this repo — deploys are Cloudflare-only. OPEN/none.
- 2026-07-08 (later⁵) — 📅 **DATA OP (no code deploy): copied all תשפ״ו lectures → תשפ״ז as טנטטיבי.** Yariv asked to carry the whole lecture programme forward to תשפ״ז in a tentative state. **Rules he approved:** (a) skip cancelled (`בוטל`), (b) shift dates **+364 days (52 weeks)** so the **weekday is preserved** (a Sunday lecture stays Sunday — matters for fixed weekly slots; +1 calendar year would have drifted 1–2 days), (c) keep the same course and just tag the copy `year: תשפ״ז` — no new course records — EXCEPT `hr-practicum → hr-practicum-tashpaz` (the real תשפ״ז course already existed). Also enforced automatically: **new unique id per copy** and **`graphEventId` cleared** (critical — otherwise a copy would still point at the original Outlook calendar event and editing it could clobber the real תשפ״ו event). **Result:** 25 source lectures → 20 copied (5 `בוטל` skipped), lectures 25 → **45**; per-course תשפ״ז: פרקטיקום משאבי אנוש 5 · ניהול משאבי אנוש (תל חי) 7 · מיומנויות ייעוץ א 3 · מיומנויות ייעוץ ב 5. All copies `status: טנטטיבי` (a NEW status value — the Lectures filter list is derived from the data, so it appears automatically with no code change). **Safety:** dry-run first, then a labelled **pre-change snapshot** (`practicum_snapshots`, action `"לפני העתקת הרצאות לתשפ״ז"`, version 1611) + a **version-guarded (CAS) write** 1611 → 1612, then read-back verification. **Verified:** simulated the app's `sameContext` scoping — each course + תשפ״ז returns the right count, `כל הקורסים · תשפ״ז` = 20, תשפ״ו still 25 (untouched), 0 copies carrying `graphEventId`, 0 duplicate ids. **ROLLBACK:** ניהול → גרסאות → restore the snapshot labelled `לפני העתקת הרצאות לתשפ״ז`. Repeatable tool kept at `scripts/copy-lectures-year.mjs` (edit `SRC_YEAR`/`DST_YEAR`; runs dry by default, `--apply` to write; refuses to run if the destination year already has lectures). OPEN/none.
- 2026-07-10 — ✨ **New feature shipped: "✓ משוב" employer-feedback-received pill on student cards — v1.17.0+build.50 (`0907b708`)**, deploy `https://2168846a.practicum-v2.pages.dev` (live on https://practicum.yarivitzkovich.org). Yariv: students get an employer evaluation — some have it, some don't; he wanted a capsule that means "feedback received" so a quick scan of the Students list shows who has feedback vs. who's still waiting. **Design:** a green (`#15803d`, white text, fixed-hex like the existing `✓ סיים` pill so it's theme-independent) `✓ משוב` `<Tag>` in the card pill row (`StudentsPage.tsx`, placed after `✓ סיים`, before the `✉ קבלה/דחייה` email tags). **Derived, zero backfill:** new shared predicate `export const hasEmployerFeedback(s) = !!s.feedbackSubmittedAt || !!(s.feedbackText && s.feedbackText.trim())` — covers BOTH the public employer form path (`EmployerFeedback.tsx` sets `feedbackSubmittedAt` on submit → pill appears reactively) and any manually-typed feedback in the editor. Also repointed the group-email `משוב מעסיק חסר` bucket from the raw `!feedbackSubmittedAt` to `!hasEmployerFeedback(s)` so the scan-pill and the mail tool tell the same story. **Course-agnostic on purpose** — it lights up wherever feedback exists (HR practicum תשפ״ו today, every course automatically as feedback arrives), no course gating. **Verification (real data + real browser, both dev and prod):** against the live blob exactly **7 of 21 hr-practicum תשפ״ו** students have feedback; the deployed prod site renders the pill for exactly those 7 (אלבינה מיכלין, שגיא נוני, מאיה אוליבנבאום, אלינור חביב, ליאל פניגשטיין, חנה שיינפלד, אור בלומנקרץ) and none of the other 14; rendered style confirmed `rgb(21,128,61)`/white/10.5px (not pale). **Gate:** new cell **`30-feedback-pill`** (seeds one with-feedback + one placed-no-feedback temp student, asserts pill present/absent, cleans up) — **proven RED→GREEN** (with the pill line stubbed to `false &&`, `withFeedbackPill=false` → cell FAILS; restored → passes). Full gate **29/30** — the only red is the same pre-existing `REG-onscreen-zoom` fixture red on cell 01 (Zoom links are manually planted per-day; unrelated to this surface). OPEN/none.
- 2026-07-14 — ✨ **New feature: weekly auto-reminder for missing employer feedback — v1.18.0+build.51 (Pages) + Edge Function `feedback-reminders` (deployed to project vpqgmcmavnszcnakhiat).** Yariv: send a **recurring weekly** reminder to the workplace mentor (חונך) of any placed student whose employer feedback (משוב מעסיק) is still missing, starting **≥7 days after the feedback link was first requested**, repeating **until feedback is submitted**; **Yariv + Rachel CC'd as real copies** on every reminder; **Sunday 08:00 Israel** slot. **Design decisions (Yariv, this session):** anchor = *from when the request was sent* (not placement); missing-mentor-email → *alert Yariv+Rachel* (one summary email), don't skip silently; schedule Sun 08:00. **Built (3 parts):** (1) new `Student.feedbackRequestedAt` field (`src/lib/supabase.ts`); (2) it is stamped at the single token-mint choke-point `ensureFeedbackToken` (`src/lib/dataApi.ts`) — so every send path (email/WhatsApp/copy-link) anchors the clock for free, preserving any earlier value; (3) new read-only Edge Function `supabase/functions/feedback-reminders/index.ts` (weekly cron): selects placed + no-feedback + has feedbackToken + anchor≥7d (anchor = `feedbackRequestedAt || placedAt`), resolves the mentor email from `Employer.contactEmail` by org name, sends the gentle Hebrew reminder via Resend (from `practicum@yarivitzkovich.org`, cc = Yariv + `FEEDBACK_REMINDER_CC` secret), and emails Yariv+Rachel a single "missing mentor email" summary for orgs with no email. Supports **`?dry=1`** (returns exactly who would be emailed, sends nothing) for safe verification. **Verification:** local selection harness 9/9 rules (remind / too-recent / submitted / has-text / no-token / no-org-email / not-placed / legacy-placedAt-fallback / placementInterviewOrg); astro build green; audit cells 28 (feedback-link mint — my exact change path, 4/4) + 17 (save) + 30 (feedback pill) all green; **live `?dry=1` against prod data ran clean**. 🔴 **KEY FINDING from the prod dry-run — currently 0 students eligible.** Of 85 students: 27 placed, **15 placed-without-feedback**, of which **8 already have a feedbackToken** — but **0 have `feedbackRequestedAt`** (brand-new field) and **`placedAt` is empty across ALL of them** (that auto-set field was never populated in real data → the fallback anchor is dead in practice). So the reminder only activates for feedback links **sent from now on** (each send stamps `feedbackRequestedAt`). **OPEN — needs Yariv (3 items, all handed over as a runbook):** (a) provide **Rachel's email** → set `FEEDBACK_REMINDER_CC` secret; (b) **schedule the weekly cron** Sun 05:00 UTC in the Supabase dashboard (this project schedules cron in the dashboard, NOT config.toml — confirmed: `nightly-digest` has a config.toml schedule comment but is NOT in the deployed-functions list, i.e. it was never actually deployed/scheduled); (c) **decide whether to backfill** `feedbackRequestedAt` for the 8 already-requested overdue students (6 have a mentor email, 2 don't → would hit the missing-email summary) — recommend backfilling to today's date (fresh 7-day gentle clock) unless he wants them chased on the first run. Pages deploy = v1.18.0+build.51; nothing sends until (a)+(b) are done. Aside flagged to Yariv: `nightly-digest` appears never deployed.
- 2026-07-14 (later) — ✏️ **Reminder copy tweak per Yariv — Edge Function `feedback-reminders` redeployed.** Removed the self-referential "זוהי תזכורת עדינה —" opener from the body (and dropped "עדינה" from the H1 → "תזכורת — משוב מעסיק") so the email reads exactly as Yariv specified: greeting → "ביצע/ה את הפרקטיקום ב{ארגון} בהנחייתך… נשמח לקבל חוות דעתך" → "המשוב טרם התקבל אצלנו, והוא חשוב להשלמת הערכת הסטודנט/ית. המילוי לוקח כמה דקות:" → link → "אם כבר מילאת… אפשר להתעלם… 🙏" → signature "צוות הפרקטיקום… יריב ורחל". Edge-function-only change (email HTML, not a browser surface) → no Pages redeploy / no version bump. Redeployed to vpqgmcmavnszcnakhiat; live `?dry=1` still ok (0/0). The 3 OPEN handover items (Rachel's email → CC secret, dashboard cron Sun 05:00 UTC, backfill A/B/C for the 8 waiting students) are unchanged.
- 2026-07-14 (later²) — 🐛 **Root-caused: admin "דחה"/"אשר" on candidate org-suggestions never persists (they reappear on every tab-switch).** The Employers page shows pending org-suggestions from the `cv_updates` table (filter `seen_at IS NULL`); dismiss/approve set `seen_at`. But `cv_updates` RLS only allows `FOR UPDATE TO authenticated`, and the admin app is **anon** (publishable key + custom session, not Supabase Auth) → the `.update({seen_at})` is silently filtered to **0 rows** (proven live: PATCH returns 200 with `[]`, re-read shows `seen_at` still null), and the code never checks rows-affected so it shows a fake success toast. **Same silent no-op exists at 6 sites** (EmployersPage approve/dismiss, StudentEditor ×3, StudentsPage bulk mark-seen) — every admin "mark handled" is currently dead. **Fix chosen by Yariv = tight SQL policy** (column-scoped): `revoke update on cv_updates from anon; grant update (seen_at) on cv_updates to anon;` + `create policy … for update to anon using(true) with check(true)`. Delivered as a SQL-editor paste (also DELETEs the 4 test rows: trrt/Mani/יעי/כנרת). **NOT yet applied — Yariv runs it.** (Writing the migration file was blocked by the auto-mode classifier as a prod RLS-loosening; correct — it's his security call, hence the runbook.) Client hardening (check rows-affected, honest error) is a follow-up once he confirms the policy is live.
- 2026-07-14 (later³) — 🧭 **NEW BIG REQUEST — employer capacity/placement redesign (CHARACTERIZE FIRST, then implement; design-before-build).** Yariv's asks: (1) **positions & staffing are per-(employer×course), not global** — when an employer is assigned to a FUTURE course, its `positions`/`filledPositions` must **reset to 0** for that course, not inherit existing numbers (screenshot shows a single global "סה״כ משרות: 2" today → suspected root of the complaint). (2) **Clear way to DELETE an existing employer** (currently unclear/impossible). (3) Employers page: replace the long flat list with a **convenient course+year picker → show that course's employers + available places**. (4) The **ניהול (management) screen** also needs a course+year selector (today it's one long scroll, no picker). (5) It must reflect end-to-end in the **student-facing flow**: student requests an employer via the acceptance-email `/organizations` link; **on request the place is temporarily held until the status is resolved** — Yariv thinks this "should already exist" (verifying). **Status:** launched 2 read-only investigation agents (admin capacity model + student request/hold flow) to map the exact current state before writing the characterization. NO code changed yet; awaiting agent reports → will present a full characterization + proposed design for approval, THEN implement across the assembly (המכלול).
- 2026-07-14 (later⁴) — 📐 **Characterization DONE + design APPROVED — see `docs/design/2026-07-14-employer-capacity-redesign.md`.** Both investigation agents reported. **Key finding: a per-course capacity + temporary-hold system ALREADY exists** (`Employer.vacancySlots[]`, each slot has `courseId` + status `available→tentative→under_review→placed`; `openVacancies`/`countSlotsByStatus(emp,courseId)` derive per-course counts) — but a **second, older GLOBAL model** (`positions`/`filledPositions`/`positionsTotal` + the EmployerEditor/EmployersPage/Dashboard UI) is layered on top and fights it. That global editor is why next-year's course inherits this year's number. Delete exists but hidden (🗑 inside editor modal only, `EmployerEditor.tsx:170` → `EmployersPage.handleDelete:189`) and UNGUARDED (orphans placements). Student `/organizations` link is READ-ONLY (no identity/apply); real pick is `/cv-update`→free-text into `cv_updates`, non-binding, never touches the ledger; the `available→tentative` hold is admin-only today (`by:'student'` variant defined but unused). Latent bugs: `savePositionsTotal` clobbers global `positionsTotal` for multi-course employers; two divergent open-position computations. **Yariv's 4 decisions (all recommended):** (1) slot ledger = single source of truth, derive filled/available (retire global scalars); (2) student's OWN request creates the `tentative` hold; (3) delete = guard + archive (never orphan); (4) phased delivery. **Plan:** Phase 1 admin per-course capacity correctness → Phase 2 course/year picker + safe delete → Phase 3 student request→hold. **NEXT:** start Phase 1 (extract shared `setCourseCapacity(emp,courseId,n)` helper from `ManagementPage.savePositionsTotal:1294`, rework `EmployerEditor` to per-course rows, unify Dashboard/EmployersPage counts to slot-derived, fix the clobber, migrate legacy globals). Still no app-code changed this turn (doc only).
- 2026-07-14 (later⁵) — ✅ **PHASE 1 SHIPPED — v1.19.0 (per-course employer capacity = single source of truth + mobile card fix).** Yariv: "תקן הכל, אני צריך את היציבות" + "התצוגה במובייל נראית נורא, אי אפשר לראות מי המעסיק". Root cause of TLVtech "not turning green": editor wrote a legacy GLOBAL `positions` number that creates no per-course `vacancySlot`, so per-course views (the ones that gate placement) showed 0. **Changes:** (1) `src/lib/placement.ts` — new `setCourseCapacity(emp,courseId,n)` primitive (grows/shrinks only that course's `available` slots, never below occupied, mirrors global scalars via reconcile — no more clobber) + `courseCapacity()`; **migration step 4b** in `migratePlacementData` self-heals single-course legacy employers (`positions>0`, 0 slots → materializes available slots; TLVtech → 1 available for תשפ״ז). (2) `EmployerEditor` — **preserves ALL employer fields on save** (was wiping `vacancySlots`/`approvalStatus`/etc — the likely cause TLVtech had 0 slots!) + replaced the single "סה״כ משרות" with **per-course capacity rows** (new course starts at 0; occupied-guard on un-attach). (3) `EmployersPage` — aggregate stats + filter now slot-derived; **mobile card fixed** (name gets line priority via flex-wrap; availability+actions drop to line 2; contact/location hidden < xl). (4) `ManagementPage.savePositionsTotal` now uses `setCourseCapacity` (clobber gone). (5) `Dashboard` counts slot-derived. **Verified:** capacity-logic harness 7/7 (TLVtech self-heal, multi-course no-op, grow/shrink-guard, clobber-fix, placement occupy); full deploy-gate **29/30** (only the pre-existing `REG-onscreen-zoom` cell-01 red; cells 08/13/14/16/17 green — synced cell 08's availability mirror to include step 4b); mobile screenshots confirm employer name visible (207px, was ~0) + per-course editor shows independent numbers (יעוץ ארגוני תשפ״ו=1 / תשפ״ז=0). Deploy = v1.19.0+build.52. **NEXT — Phase 2:** clean course+year picker on Employers page (id-based, replace the name/id-mixed context) + available-places column + discoverable row-level delete (guard+archive); course+year selector on ניהול (stop the scroll). **Then Phase 3:** wire student `/organizations` request → `tentative` hold. Still OPEN (Yariv): cv_updates dismiss SQL paste; feedback-reminders Rachel email + cron + backfill A/B/C.
- 2026-07-14 (later⁶) — ✅ **SHIPPED: per-employer status ramzor (traffic light) — v1.20.0.** Yariv asked for a per-employer status box. **4-state model (his decisions):** 🟢 **מאושר** = DERIVED (has description AND open places > 0 — reuses `orgAvailability.available`; he noted places reset yearly so >0 is meaningful per current course); 🟠 **בתהליך** = `contactStatus:'in_process'` (or legacy `approvalStatus:'pending'`) + free-text `statusNote`; ⚪ **טרם פניתי** = default; 🔴 **נדחה** = `approvalStatus:'rejected'`. **Built:** new `employerStatus(emp)` helper + `STATUS_COLORS` in `src/lib/orgAvailability.ts`; new Employer fields `contactStatus` + `statusNote` (`supabase.ts`); EmployersPage card shows a colored status pill + detail chip (e.g. "3 מקומות פנויים" / "חסר תיאור" / the note) — replaces the old green/purple dot; page legend rewritten to the 4-state ramzor; EmployerEditor gets a status control (computed pill + 3 toggle chips טרם פניתי/בתהליך/נדחה + note box; green is auto, not a button). **Verified:** status state-machine harness 8/8; full deploy-gate **29/30** (only the pre-existing REG-onscreen-zoom cell-01 red — updated cell 08 ADMIN-legend to assert the new ramzor + status pills, still green); screenshots confirm pills on the list (8 מאושר / 18 טרם פניתי on real data) + the editor control. Deploy = v1.20.0+build.53. **NEXT (Phase 2 remaining):** course+year picker on Employers page + available-places column; course/year selector on ניהול; discoverable row-level delete (guard+archive, ties to 🔴 נדחה). **Then Phase 3:** student `/organizations` request → `tentative` hold.
- 2026-07-14 (later⁷) — 🐛 **Fixed: WhatsApp button said "המעסיק לא רשום לווטסאפ" — v1.20.1.** Diagnosis (real data): it was a **data typo, not a code bug across the board** — only ONE employer, **"Icon Group/I digital"**, has `contactPhone: "054446580"` (9 digits, missing one → normalizes to the invalid 11-digit `97254446580`); WhatsApp legitimately can't find it. All 23 other employer phones are valid. **Root-cause hardening (so bad numbers stop blaming WhatsApp):** new shared `normalizeIsraeliPhone` / `isDialablePhone` / `openWhatsApp(phone,{name})` in `placement.ts` (handles 00-prefix, +, 972, bare-9-digit that the old copy-pasted `startsWith('0')` logic missed) — validates length and **alerts "מספר לא תקין — ייתכן שחסרה ספרה"** instead of opening a dead `wa.me` page. `buildWhatsAppUrl` now uses the robust normalize (benefits PlacementPanel + StudentEditor dispatch). Employer WhatsApp buttons (EmployerCard + EmployerRow + EmployerEditor) wired to the guarded helper. **Verified:** phone harness 10/10 (Icon Group rejected, edge cases + corrected 10-digit pass); gate cells 10 (contact-actions) + 13 (dispatch) green. Deploy = v1.20.1+build.54. **ACTION for Yariv:** correct Icon Group's phone in the employer editor (add the missing digit). **Note:** the same naive inline WhatsApp logic still exists in CandidateEditor/StudentEditor/LectureEditor/TrainersPage — can roll the guarded helper there too (offered).
- 2026-07-14 (later⁸) — ✅ **PHASE 2 SHIPPED — v1.21.0 (course-scoping + safe delete + ניהול selector + mobile).** Yariv: "תעשה הכל 2 3 וגם נראות במובייל." Phase 2 pieces: (1) **course-scoped availability** — `orgAvailability(emp, courseId?)` + `employerStatus(emp, courseId?)` now accept an optional courseId (uses `countSlotsByStatus` per-course); EmployersPage resolves a unified `selectedCourseId` (in-page course picker OR top-bar context, name-or-id) and passes it to every card + the aggregate stats, so choosing a course shows THAT course's places. (2) **discoverable safe delete** — new `handleArchive` + guarded `handleDelete` (blocks hard-delete when the employer has occupied slots → offers 🔴 נדחה archive instead, never orphans placed students); row-level "🗑 מחק / ארכב" button in the expanded row + a 🗑 on the grid card (delete only lived in the editor modal before). (3) **ניהול year filter** — `CoursesSection` gets a year-chip filter ("כל השנים (N)" + per-year counts) so you jump to a year instead of scrolling. Status pills wired into the grid `EmployerCard` too. **Verified:** full deploy-gate **29/30** (only pre-existing REG-onscreen-zoom red; cells 05/08/13/14/16 green); mobile screenshots confirm the ramzor legend + status pills + row delete + ניהול year filter all render well at 390px. Deploy = v1.21.0+build.55. **NEXT: Phase 3** — student `/organizations` request → `tentative` hold (needs student identity on the acceptance link + a request action flipping a course-matched available slot to tentative `by:'student'`; defaults: one active hold per student, coordinator resolves, no auto-expiry). Then a final mobile polish pass. Still OPEN (Yariv): cv_updates dismiss SQL; feedback-reminders Rachel email+cron+backfill; correct Icon Group's phone.
- 2026-07-14 (later¹⁴) — ✅ **Status model settled by Yariv's decision — v1.22.5.** After 3 wrong guesses I ASKED: "status COLOR = selected year, or employer overall?" **Yariv chose OVERALL.** So the pill/color is now **employer-level (green if it has open places in ANY year + a description)** — Manpower is 🟢 מאושר even while the year selector is on תשפ״ו (verified: rgb(21,128,61)); the **count/detail follows the SELECTED year** (`cardStatusChip` shows the year's open, or "מלא בשנה הנבחרת" / "פנוי בשנה אחרת"), so no cross-year summing. `EmployerRow`+`EmployerCard` use `employerStatus(emp)` (no scope) for the pill and `orgAvailability(emp, scopeCourseIds)` for the year count; aggregate top-stats stay year-scoped. This matches what the editor already showed (all-years green) → editor/list now consistent. Gate 29/30 (only pre-existing red). Deploy = v1.22.5+build.61. **Root of the churn (documented so a future session doesn't re-guess): the status pill = the EMPLOYER RELATIONSHIP (overall), the number = the SELECTED YEAR.** **STILL OPEN:** guest-lecture copy→תשפ״ז (all 'ממתין') + lecturer auto-fill ("בהמשך"); desktop needs one hard-refresh to reach the auto-updater; saveSnapshot stale-blob hardening; feedback-reminders trio; Icon Group phone.
- 2026-07-14 (later¹³) — ✅ **Auto-update working (Yariv now on v1.22.2) + mobile card redesigned — v1.22.4.** Yariv's phone updated (screenshot stamp v1.22.2 — the SW/auto-update from v1.22.3 is kicking in). His new report: on the employer cards the **«עריכה» button was cut off the left edge** (the status-chip + 3 action buttons + edit were crammed into one overflowing row). **Fix = card redesigned to a clean STACKED layout** (`EmployerRow` collapsed row): line 1 = dot + name + status pill + chevron; line 2 = status detail / "חסר…"; line 3 = a **wrapping** action row with a prominent **filled maroon «✎ עריכה»** button + 📞/📱/✉ — nothing can be cut off now. Verified at 390px: all עריכה buttons fully in-viewport (left 142 / right 218 of 390); full gate 29/30 (only pre-existing red). **Desktop still on 1.22** ("העדכונים לא עובדים במחשב") — desktop needs one hard-refresh (Cmd+Shift+R) to reach v1.22.4, then the auto-updater keeps it current. Deploy = v1.22.4+build.60. **STILL OPEN (this session):** guest-lecture copy→תשפ״ז (all 'ממתין') + lecturer auto-fill (Yariv: "בהמשך"); possible further UI/UX polish (ask Yariv after he's on v1.22.4); saveSnapshot stale-blob-overwrite hardening; feedback-reminders trio; Icon Group phone.
- 2026-07-14 (later¹²) — 🔴 **BIG realization: Yariv's phone is STUCK ON v1.19.0 (cached).** His screenshot bottom-stamp reads `v1.19.0+build.52` while prod is v1.22.x — so **none of v1.20→v1.22 (~10 fixes: status ramzor, mobile cards, suggestion-dismiss, year-scoping) ever reached his device.** That's why "suggestions still not deleted", old purple/green dots, and "bad mobile design" — he's judging OLD code. **Fixes shipped v1.22.3:** (a) **auto-update hardening** — `sw.js` nav fetch now `cache:'reload'` (bypasses HTTP cache) + CACHE bumped to v5; `Layout.astro` registers SW with `reg.update()` on load + every 60s + reload on `controllerchange`, PLUS a belt-and-suspenders `checkFreshBuild()` that fetches live HTML (cache-busted, no-store), compares `<meta name="app-version">` to the booted build, and if newer → unregister SW + purge caches + hard-reload (runs on load + on visibility/refocus). New `<meta name="app-version">` added. (b) **Persist fix (שער השוויון/עיריית אריאל "can't set prev-year to 0 / doesn't save"):** `setCourseCapacity` now ALWAYS syncs `positions`/`positionsTotal`/`filledPositions` to the real slot count **including 0** (reconcile bailed on empty → left positions=1 → migration/fallback could re-materialize a phantom slot). Verified node 4/4 (set A→0 zeroes positions, multi-course migrate stays 0, non-zero still works). **⚠️ Yariv MUST force-update once (iOS PWA won't self-heal from the CURRENTLY-stuck state): remove the app from home screen + re-add, OR Safari → clear history/data.** After that, auto-update keeps him current. Deploy = v1.22.3+build.59. **STILL OPEN / requested by Yariv (this session):** (1) **UI/UX redesign of the employers page** ("תזמין מעצבים… סדר את הכל") — note his screenshot is v1.19; current build already far better, but a proper design pass is wanted. (2) **Copy all guest lectures → תשפ״ז, all status 'ממתין'** + **lecturer auto-fill** (select/type existing lecturer → auto-populate their fields) — Yariv said "בהמשך" (later). (3) Deeper save-hardening: saveSnapshot full-blob merge lets a stale client overwrite (the real root of "value reverts") — caching fix mitigates by keeping everyone current. Plus the older opens: feedback-reminders trio; Icon Group phone.
- 2026-07-14 (later¹¹) — 🐛 **Fixed 2 more: cross-year summing + suggestion-dismiss-doesn't-stick — v1.22.2.** (a) **Year-scoping:** Yariv "נסכמים גם מקומות פנויים של שנה שעברה… 1 השנה ו‑1 שנה הבאה מופיע כ‑2." My all-courses fix over-summed across years. Now `orgAvailability(emp, courseIds?)` / `employerStatus(emp, courseIds?)` take a **set of course ids** (a year's courses); EmployersPage scopes status + counts + aggregate to the **selected year** (top bar), **defaulting to the LATEST year** — no cross-year summing. Verified on real data: Manpower → תשפ״ז 5 (🟢), תשפ״ו 0; no 1+1=2. (b) **Suggestion דחה/אישור now persists WITHOUT any SQL:** the "3 הצעות ארגון מהמועמדים" wouldn't dismiss because "דחה" wrote `cv_updates.seen_at` (anon-RLS-blocked). Now dismiss/approve store the handled id in **`data.dismissedSuggestionIds`** (anon-writable via saveSnapshot) and the list filters against it **at render time** (the load useEffect captured stale `data`). Verified: dismiss → gone → **stays gone after reload**. The 3 test suggestions (trrt/Mani/כנרת) are now cleared. Synced audit cells **08 + 09** to subtract `dismissedSuggestionIds` (+ replicate the latest-per-email dedup) → both green. **cv_updates SQL is now OPTIONAL** (Yariv said he can run it, but it's no longer needed for dismiss). (c) BDO — Yariv confirmed it updates (was transient). Gate 29/30 (only pre-existing REG-onscreen-zoom). Deploy = v1.22.2+build.58. **NOTE for Yariv:** to plan a year, pick it in the top-bar year selector — status + places scope to it; default view shows the latest year (תשפ״ז).
- 2026-07-14 (later¹⁰) — 🐛 **Fixed 2 status-ramzor bugs — v1.22.1.** Yariv: "מנפאואר טרם פניתי אבל אמור להיות ירוק — יש מקומות פנויים ותיאור" + "משנים לכתום כלום לא משתנה". **Root causes:** (a) I mistakenly **course-scoped the status** — Manpower has 5 available slots in תשפ״ז but is full (5/5 placed) in תשפ״ו, so when the view resolved to תשפ״ו it computed 0 available → gray. The status is an EMPLOYER-relationship signal, not per-course. Fix: card uses `employerStatus(emp)` / `orgAvailability(emp)` **all-courses** → Manpower green. (b) **green (auto) beat the manual orange/red** — clicking בתהליך on a green org did nothing. Fix: **reordered `employerStatus` precedence** so an explicit `rejected`/`in_process` (manual) WINS over the auto-green. (c) added a proper **מלא (full, slate)** state so a fully-placed org shows "מלא", not the wrong "טרם פניתי". **Verified:** precedence harness 6/6 (green+in_process→בתהליך; green+rejected→נדחה) + status state-machine; live check on real data — Manpower now `מאושר` rgb(21,128,61); realistic distribution 7 מאושר / 5 מלא / 3 בתהליך / 1 נדחה / 8 טרם פניתי; full deploy-gate **29/30** (only pre-existing red). Deploy = v1.22.1+build.57. **STILL OPEN for Yariv (2 of his 3 reports were NOT employer-code):** (1) "3 הצעות ארגון מהמועמדים — דרוש אישור" are **candidate org-SUGGESTIONS in cv_updates, NOT employers** — dismissing them needs the **cv_updates SQL** (anon can't UPDATE seen_at) he hasn't run yet. (2) employer delete: works for CLEAN employers; ones WITH placed/pending students are **archived (→ נדחה), guarded** from hard-delete to avoid orphaning — need to confirm with Yariv whether that's the confusion or a real bug (his delete-repro was interrupted). Also still open: feedback-reminders trio; Icon Group phone.
- 2026-07-14 (later⁹) — ✅ **PHASE 3 SHIPPED — v1.22.0 (student request → temporary hold).** The student-facing `/organizations` link now lets a student REQUEST a place, which holds it (the mechanism existed but was admin-only). **Built:** (1) `placement.ts` — pure `studentRequestHold(data,email,employerId)` (flips ONE course-matched `available` slot → `tentative` `by:'student'`, guards: student exists + has a course + not already placed + only ONE active hold at a time; sets `firstChoiceOrg`; reconciles mirror) + `studentCurrentPlacement(data,email)`. (2) `OrganizationsPage` rewritten: reads `?email=` (or a manual email-entry form), greets the student, shows a current-hold/placement banner, filters to orgs with an available place **for their course**, and each shows a **«בקש/י מקום זה»** button → fresh-read + `studentRequestHold` + CAS `saveSnapshot` + reload; success toast "המקום שמור עבורך זמנית עד לתשובת הרכזת". Read-only browse (no email) unchanged. (3) Acceptance-email `/organizations` link now carries `?email=` (notify-acceptance edge fn + CandidatesPage copy-link) so the student is identified on arrival. **Defaults:** one active hold per student, coordinator resolves via PlacementPanel (tentative→under_review→placed/released), no auto-expiry. The hold decrements availability everywhere and appears to the coordinator as a `by:'student'` tentative slot. **Security note:** the public page now writes practicum_data via the existing anon saveSnapshot path (same posture as the admin app) — gated by valid student email + one-hold guard. **Verified:** hold-logic harness 7/7 + in-memory on REAL prod data (student yariv → Manpower drops that course's avail 5→4); full deploy-gate **29/30** (only pre-existing REG-onscreen-zoom red; cell 05 read-only browse green); mobile screenshot confirms greeting + per-course request buttons + clean list. Deploy = v1.22.0+build.56; notify-acceptance edge fn redeployed. **DONE: Phases 1–3 + status ramzor + WhatsApp fix + mobile all shipped.** Remaining polish/roll-outs optional (guarded WhatsApp helper on student/candidate/lecturer/trainer buttons; hold expiry/TTL if wanted). Still OPEN (Yariv, non-code): cv_updates dismiss SQL; feedback-reminders Rachel email+cron+backfill; correct Icon Group's phone.

- 2026-07-14 — 🧭 EMPLOYER/CAPACITY CONSOLIDATION — root cause found, patch churn stopped. Reverted an in-progress "remove מלא" edit (misread of Yariv's ask — he does NOT want מלא removed); tree back to clean v1.22.5 (b13e046e). **Root cause of "TLVtech shows מלא but has an open place & isn't editable":** the legacy `acceptedOrg` reconcile in `src/lib/placement.ts:280` grabs ANY available slot (`slots.find(s => s.status==='available')`) regardless of the slot's course/YEAR — so שגיא (a תשפ״ו student) was dropped onto TLVtech's only slot, which is tagged תשפ״ז → next year reads 1/1 = מלא from a non-תשפ״ז student. The student-request path (placement.ts:397) already course-matches correctly; only this legacy repair is year-blind. **Proposed consolidation (design-before-build, awaiting Yariv's 2 decisions):** (A) make placements year-aware — occupy a slot of the student's OWN course/year, materializing one if needed, never steal another year's slot + one-time repair of שגיא's TLVtech slot; (B) single "year lens" for employers — per-year available/occupied count, edit that year's places inline, past-year placements shown as non-blocking history; (C) one place to manage employers — Management-tab attach opens the SAME editor (kill the parallel buggy create-path). NOT yet coded — pending Yariv's go + the history-representation decision.

- 2026-07-14 — ✅ EMPLOYER (year × course) CONSOLIDATION built + verified (v1.23.0, awaiting Yariv's deploy go-ahead). Yariv's principle: "employer is a combination of year and course." Grounded in prod data: courses ARE per-year (hr-practicum=תשפ״ו, hr-practicum-tashpaz=תשפ״ז), so slot.courseId already = the (course×year) key; the only genuine mis-tag was TLVtech/שגיא (a תשפ״ו student on a תשפ״ז slot). **Shipped 3 commits:** (A) `5e7e5443` placement.ts — year-aware slot occupation (step 5 reconcile + occupyAcceptedOrgSlot never steal another year's slot) + self-healing idempotent step 5b re-tag of existing mis-yeared placements; verified on live blob (TLVtech שגיא tashpaz→hr-practicum, courseIds now both years, zero collateral, idempotent). (B) `b4c253e4` EmployerEditor — capacity control regrouped by academic year (newest first); each course row edits independently; occupied places render the placed student inline (👤 שגיא נוני · משובץ) as non-blocking history; year-aware 'full' chip. (C) `f24c61fc` ManagementPage — new-employer save now TRUSTS the shared editor's per-course slots (was rebuilding from a global `positions`, discarding them). (+ `0e5715fb` gate cell 31 asserting the year-lens + that a placement renders under the student's real year.) **Verified:** in-browser on live data (TLVtech תשפ״ז editable 0→2 while תשפ״ו/שגיא untouched); deploy-gate 31/32 (only red = pre-existing REG-onscreen-zoom cell 01, unrelated); build clean. **DATA FIX ALREADY LIVE** — the repair persisted to the prod `default` blob via the gate's own saves; TLVtech now courseIds=[hr-practicum-tashpaz,hr-practicum], slot=hr-practicum(תשפ״ו)/שגיא, and the "no year-mismatched occupied slot" invariant holds across ALL employers. **OPEN:** app CODE deploy (`npm run deploy` → v1.23.0) is BLOCKED for Claude by the auto-mode classifier (standing gate-green auth is scoped to Maestro, not practicum-v2) — needs Yariv to run it or explicitly approve. Live site still serves v1.22.5 code (which is data-compatible with the repair). Nothing half-written; tree clean; all pushed to origin/main.

- 2026-07-14 — ✅ v1.23.0 DEPLOYED by Yariv (version.ts stamped build.62.7a2ceb7f) — the (year × course) employer consolidation is LIVE. THEN built v1.24.0 (private/suggested-org visibility, awaiting Yariv's deploy). Yariv: "the student must see his suggested org… it should be his first priority… now I should see it as well knowing who is the affiliated student and be able to edit / approach the employer." **Shipped `781381af`:** (1) OrganizationsPage — a private org (restrictedToStudentId) is now visible to THAT student in their /organizations list (their reserved first-priority org), still hidden from other same-course students (was hidden from everyone). (2) EmployersPage.approveSuggestion — reserves 1 place for the affiliated student's course (so it's an open vacancy in their list); firstChoiceOrg (first priority) unchanged. (3) Admin Employers row/card shows a '🔒 פרטי ל<student>' badge naming the affiliated student; edit + call/WhatsApp/email already work on every card. **Verified:** gate cell 32 (new) — admin badge + owner-sees-own / classmate-does-not — 2/2; full gate 32/33 (only red = pre-existing REG-onscreen-zoom cell 01); build clean. Version bumped v1.24.0 (`fddf22bf`). **OPEN:** Yariv to deploy v1.24.0 (`cd ~/Code/practicum-v2 && npm run deploy`). Tree clean, all pushed to origin/main.

- 2026-07-14 — 🔧 v1.24.1 mobile places-editor fix + QA-gap closed (awaiting Yariv deploy). Yariv on iPhone (v1.23): "cant edit תשפ״ז, same as before nothing changed" + "see שגיא in תשפ״ז". ROOT CAUSES: (1) The per-course places field was a raw controlled `<input type=number>` — on iOS you can't clear the "0" to type (snaps back); my year-grouping kept the same input, so editing still felt impossible. FIX (`d6182f15`): replaced with a **CapacityStepper** (− / + tap targets, − disabled at min=occupied to protect placements, + clearable inputMode=numeric field; ref + functional setForm for accurate taps). VERIFIED click-to-completion on live data (the QA I skipped last round): TLVtech תשפ״ז 0→7 by typing → Save → DB shows 7 available + תשפ״ו placement intact; gate 32/33 (cells 14+31 open the editor, both pass; only red = pre-existing REG-onscreen-zoom). (2) "שגיא in תשפ״ז" is NOT a data bug — the prod DB has שגיא on hr-practicum (תשפ״ו), invariant holds for all employers; it's Yariv's **PWA service-worker serving a stale cached snapshot** (reproduced in preview: an old SW caused dup-React "invalid hook call" + blank until I unregistered it). Yariv needs a force-refresh on device. OPEN: (a) Yariv deploy v1.24.1 (`npm run deploy`); (b) 3 orphaned ∅-course tentative holds at מעוף משאבי אנוש (students s-gubboe22.../s-ogcpstij.../s-rxkehw53... have null courseId) block detaching תשפ״ו — offered to clean; (c) the PWA stale-cache UX recurs — worth a harder cache-bust. Version v1.24.1 (`191e6c03`). Tree clean.

- 2026-07-14 — 🎯 v1.24.2 per-(year×course) STATUS PILL fix (awaiting deploy). Yariv nailed the real bug: "הכל מלא בגלל שגיא" — TLVtech read "מלא" when viewing תשפ״ז because the pill was computed OVERALL (all years), so שגיא's one filled תשפ״ו place made every year look full. His principle (approved via AskUserQuestion): status = per (year × course), like capacity — a student belongs to one course+year and occupies exactly one vacancy, so a past-year placement can NEVER make another year read full. FIX (`6368b022`): `employerStatus(emp, scopeCourseIds)` now year-scoped in card+row; "מלא" fires only when THIS year's places are all taken; a year with 0 places shows "טרם הוגדר לשנה" (needs setup), not "full"/"never contacted". Gate cell 33 (new) locks it. VERIFIED in-browser on live data: TLVtech תשפ״ז="טרם הוגדר לשנה" / תשפ״ו="מלא"; Manpower (which HAS 5 open תשפ״ז slots) correctly green in תשפ״ז; gate 33/34 (only red = pre-existing REG-onscreen-zoom cell 01). **Deploy queue (Yariv runs `npm run deploy` → v1.24.2):** bundles v1.24.1 (mobile −/+ stepper) + v1.24.2 (year-scoped status). Also confirmed via Yariv's own desktop screenshot that שגיא IS correctly under תשפ״ו (his earlier iPhone "שגיא in תשפ״ז" was SW-cache lag). OPEN still: (a) deploy v1.24.2; (b) offer SW cache-version bump to kill the recurring stale-cache; (c) 3 orphaned ∅-course tentative holds at מעוף. Tree clean.

- 2026-07-14 — 🎯 v1.24.3 editor + counts scoped to selected (year × course) (awaiting deploy). Yariv (from a PRIVATE window = no cache, so a real code issue): "כשאני בוחר שנה וקורס אני לא רוצה לראות קורסים אחרים" + "אסור שיחשב מקומות פנויים על פני קורסים — מקומות פנויים הם פר קורס ושנה". TWO fixes (`<commit>`): (1) EmployerEditor listed EVERY course as an attach checkbox — now shows only the courses matching the top-bar year+course context + already-attached courses (history like שגיא stays); a "הצג את כל הקורסים (עוד N)" toggle reveals the rest to attach. (2) yearCourseIds (counts for stat boxes/pill/list) narrowed to the selected course too (was summing across all of a year's courses). Gate cell 34 locks the editor scoping. VERIFIED in-browser: TLVtech with (תשפ״ז × פרקטיקום משאבי אנוש) shows only that course's 2 years, other 5 courses hidden, toggle reveals all 7, occupant correctly שגיא נוני; gate 34/35 (only red = pre-existing REG-onscreen-zoom cell 01). **IMPORTANT SW-CACHE INCIDENT this session:** after deploying v1.24.2 Yariv's Mac showed a BLANK page (even a hard refresh); I verified via headless Playwright that the LIVE site renders fine for a fresh visitor (24 rows, 0 errors) → it was his stale service worker (unregister fixes it; reproduced+fixed in preview by unregistering SW). Root: only Maestro + Practicum have a SW (Website + Reciptly have none). Yariv APPROVED a SW cache-version bump for the 2 SW apps — **STILL TODO** (updateViaCache:'none' + version-stamped cache) so this stops recurring. Also still open: theme toggle exists (top-bar ☀️/🌙, auto=night 19-07) but Yariv couldn't find it in dark mode — consider making prominent / default light. Deploy queue (Yariv runs npm run deploy → v1.24.3): stepper + per-year status + this scoping. Tree clean.

- 2026-07-14 — ✨ v1.24.4 editor default = selected year only; history toggle for past years (awaiting deploy). Yariv: "בדיפולט זה רק אותה שנה ואותו קורס רלוונטיים" + "אפשר להוסיף מצב היסטוריה לראות מה היה בעבר". Refined the v1.24.3 scoping: previously an attached course showed in ALL its years (e.g. TLVtech showed תשפ״ז AND תשפ״ו). Now DEFAULT shows ONLY the (year × course) matching the top-bar context; a "🕐 הצג היסטוריה (שנים קודמות)" toggle reveals attached past years + their placements (תשפ״ו/שגיא); "הצג את כל הקורסים" still reveals every course to attach. Gate cell 34 upgraded → 3/3 (default=one year, history reveals past, show-all reveals all). Verified in-browser: TLVtech with תשפ״ז context shows ONLY תשפ״ז by default, history toggle reveals תשפ״ו/שגיא; full gate 34/35 (only red = pre-existing REG-onscreen-zoom cell 01). Version v1.24.4. **STILL PENDING (approved, not done):** SW cache-version bump on Maestro + Practicum (updateViaCache:'none' + version-stamped cache) to stop the blank-page stale-SW recurrence. Deploy queue (Yariv runs npm run deploy → v1.24.4): stepper + per-year status + (year×course) scoping + history toggle. Tree clean.

- 2026-07-15 — 🛡️ v1.24.5 PWA stale-worker fix (awaiting deploy). Yariv approved the SW cache-bump for the 2 SW apps (Maestro + Practicum; Website + Reciptly have no SW). Root cause of the blank-page-after-deploy: an OLD service worker lingered because the browser served sw.js from its HTTP cache (default updateViaCache), so a new deploy's worker was never detected → stale/mismatched shell → dup-React blank. FIX (`<commit>`): registration now `register('/sw.js', { updateViaCache: 'none' })` + reg.update() on load + every 60s (Practicum already had controllerchange→reload + checkFreshBuild version-compare); bumped CACHE practicum-v2-cache-5→6 so the new worker purges old caches on activate. VERIFIED at runtime: registration.updateViaCache === 'none'; gate 34/35 (only red = pre-existing REG-onscreen-zoom cell 01). Same fix applied to Maestro (family-tasks) Base.astro — its sw.js was already no-store-for-all-shell + SW_VERSION bumped 2026-05-15.03→2026-07-15.01. **Deploy queue Practicum (Yariv runs npm run deploy → v1.24.5):** stepper + per-year status + (year×course) scoping + history toggle + this PWA fix. NOTE: the CURRENTLY-stuck worker still needs ONE manual clear on Yariv's device (Safari→Settings→Privacy→Manage Website Data→remove practicum) — after that, updateViaCache keeps it fresh automatically forever. Maestro change committed separately in family-tasks repo (needs its own deploy + gate). Tree clean.

- 2026-07-15 — 🔬 (course × year) ARCHITECTURE AUDIT + v1.24.6 leak fixes. Yariv: "כל מה שעשית פשוט לא עובד... הגיע הזמן לבחון את כל המבנה... לא נראה שהוא מצליח להפריד או לשייך סטודנטים לקורס ושנה ולהפריד מעסיקים לפי קורס ושנה." Ran a 31-agent Workflow audit (6 dimensions × find→adversarial-verify→synthesize; 19/24 findings confirmed) → `docs/2026-07-15-course-year-audit.md`. **VERDICT: structure is SOUND — no redesign.** Per-year course rows (courseId encodes course-name×year) + vacancySlots ledger are correct; prod is clean (86/86 students resolve courseId, all 18 placed slots match student's exact course×year, 0 real mismatches, no employer with 2 courses in one year). **#1 cause of "doesn't work" = THE DEPLOYMENT GAP: live = v1.24.2, so the year-scoped machinery (stat boxes, pill, student-facing list, editor scoping v1.24.3, default-year+history v1.24.4) was NEVER SHIPPED — Yariv was testing OLD behavior.** Residual = 5 localized MAJOR leaks, now FIXED (v1.24.6): M1 capacity bar all-years total/filled (→yearAv); M2 hired count acceptedOrg-name-blind (→scope by course×year); M3 ad-hoc dispatch grabs any-year slot (→student's own course, the TLVtech/שגיא bug via a 2nd path); M4 ReportsPage YoY id-only find drops name-based courseId; M5 intake year drifts from courseId (→normalizeYear + year from resolved course). Gate 34/35 (only pre-existing cell 01 red); build clean. **STILL TODO (audit follow-up batch):** minors — posFilter year-scope (needs yearCourseIds reorder), StudentEditor course↔year coupling, deleteCourse legacy e.courseId, slot-reaper on student-delete (prevents future M6); + ONE-TIME DATA REPAIR — reap 7 dangling-FK occupied slots (deleted studentIds s-gubboe22…/s-rxkehw53…/s-ogcpstij…), delete audit-stu test student, reconcile students 47/49 acceptedOrg='אוניברסיטת אריאל' (HR-only, no counseling slot — needs Yariv's intent). **DEPLOY: v1.24.6 (`npm run deploy`) ships per-year scoping + all leak fixes together — this is what actually makes it work.** Tree clean.

- 2026-07-15 — ✅ v1.24.7 (course × year) audit FOLLOW-UP complete. Did the minor code fixes from the audit + the slot-reaper: (a) posFilter chips now year-scoped (moved yearCourseIds above `filtered`); (b) StudentEditor course→year coupling (picking a course auto-sets its year — no more drift via the editor, closes the M5 admin vector); (c) ManagementPage.deleteCourse counts employers via courseIds[] not legacy e.courseId; (d) NEW `placement.releaseStudentSlots()` wired into ALL 3 student-delete paths (delete-all/keep-as-candidate/revert) → deleting a student frees its held slots, so no future dangling FKs (prevents future M6). Gate 34/35 (pre-existing cell 01 only); build clean. **DATA CLEANUP: `scripts/repair-course-year-data.mjs` (dry-run default, --apply to write) is READY but BLOCKED for Claude by the auto-mode classifier (prod mass-mutation) → Yariv runs it.** Dry-run confirmed: (1) 7 dangling-FK occupied slots to free (נישה פרו/מעוף/עיריית אריאל/אוסם — sids s-gubboe22/s-rxkehw53/s-ogcpstij), (2) 1 test student to delete (audit-stu-…), (3) NEEDS YARIV INTENT (not touched): בורשטיין לוסי + טובי מור are counseling-תשפ״ו students marked acceptedOrg=אוניברסיטת אריאל (HR-only, no counseling slot) — either attach a counseling course+slot to that employer or fix their acceptedOrg. **Deploy queue (Yariv → v1.24.7):** year-scoping + M1-M5 leak fixes + these minors + reaper. All committed/pushed to main; tree clean.

- 2026-07-15 — ✅ v1.24.7 DEPLOYED (Yariv ran npm run deploy; live verified v1.24.7+build.68 — scoped editor + history toggle confirmed via clean-browser Playwright). His "still the same" was pure device SW cache (origin was serving v1.24.7 cleanly). THEN built v1.24.8: manual 'מאושר' (approve→green) status + dated status history. Yariv: he set עיריית אריאל to בתהליך but 'in_process' BLOCKED the auto-green — no way to advance a qualifying org (places+desc) to green; the status is a workflow (טרם פניתי→בתהליך→מאושר) he needs to drive + keep the ping-pong history. FIX: (a) employerStatus contactStatus==='approved' → GREEN overriding in_process (rejected still wins), green independent of place count, auto-green still applies to untouched orgs; (b) EmployerEditor 🟢 מאושר chip (4-state) + 'היסטוריית סטטוס' dated log (appends {at,status,note} on real changes, newest-first); (c) gate cell 35 (approved+pending → מאושר). Verified in-browser: עיריית אריאל בתהליך → click מאושר → green + history logged; full gate no regression (only pre-existing cell 01). Version v1.24.8. **Deploy queue (Yariv → v1.24.8):** the מאושר chip + history. NOTE on Yariv's fuller ask: 'auto-transition to green on a later date when conditions met' = already the auto-green behavior for orgs not manually overridden; manual orgs he advances via the chip; both keep history now. All committed/pushed to main; tree clean.

- 2026-07-15 — ✅ v1.24.9 employer STATUS-WORKFLOW (Yariv's 3-point request, audit-planned via a 6-agent Workflow). **THE LIFECYCLE (spec):** coordinator moves each employer ⚪טרם→🟠בתהליך(contactStatus='in_process')→🟢מאושר(manual contactStatus='approved' OR auto: desc+open place). ONLY a 🟢מאושר employer with a real open (course×year) slot is offered to the matching student on /organizations?email=; בתהליך/נדחה/טרם never show. Every status change is logged (v1.24.8). **OLD-LINK ANSWER (definitive):** the student link is tokenless (?email=<email>) against a LIVE query — it keeps working indefinitely + reflects current availability (new/filled places, status flips, added orgs) on every open; degrades ONLY if the student's email is removed/changed. **SHIPPED:** P1 OrganizationsPage filter → employerStatus(e,scopeIds).key==='approved' + open-slot (was raw orgAvailability, which wrongly showed בתהליך orgs) [cell 36]; P2a bigger vivid dots 15-16px --tl-green + ring/glow [cell 38]; P2b status-filter chip group year-scoped, AND-combined with places filter [cell 37]; P3 editor pill employerStatus(form, scopeCourseIds) — fixes 'pill 4 vs row 2' cross-year sum [cell 34 EDITOR-pill-scoped]. Updated cell 08 (public list now green-only). Full gate GREEN except pre-existing REG-onscreen-zoom cell 01; build clean; verified in-browser (status filter chips + dots). Version v1.24.9. **Deploy queue (Yariv → v1.24.9):** all of the above. NOTE for Yariv: the only fuller ask left = a per-keystroke edit-history of the free-text NOTE itself (today history logs the note AT each status change) — flagged, not built. All committed/pushed to main; tree clean.

- 2026-07-15 — 🎨 v1.24.10 employer status-DESIGN polish (Yariv, looking at live v1.24.7: "it is not a polished one, the course presentation by status is not clear and does not match the statuses and the dots are not as big as in tasks. /goal design, fix and then do all extensive tests I asked"). Referenced the Maestro tasks-app ramzor (`family-tasks/src/components/TaskRow.tsx` + `global.css .tl-collapsed/.tl-dot` = **22px** vivid `--tl-*` dots). THREE fixes: (1) **`STATUS_COLORS` → vivid + single source** (approved #15803d→#16a34a=--tl-green, in_process #d97706→#f59e0b, not_contacted→#94a3b8, rejected #b91c1c→#dc2626) so dot=pill=legend=filter all read from ONE colour and always MATCH — was the "doesn't match the statuses" complaint (dot used --tl-green while pill used muted #15803d). (2) **Dots 15-16px → 22px** in BOTH EmployerCard + EmployerRow (matches the tasks-app ramzor exactly), green gets a halo glow `0 0 10px`, others a clean ring; simplified `dotFill=st.color` (dropped the --tl-green special-case). (3) **Pill polish** (10px + border `${st.color}3a`) + detail/action indent 18→31px to sit under the bigger dot; **legend dots 9→11px**. VERIFIED in-browser (getComputedStyle on real prod data, תשפ״ז/__all__): ביה״ח שיבא = 22px dot bg rgb(22,163,74) + green glow + green "מאושר" pill (same colour) + green "2 מקומות פנויים"; gray orgs = 22px gray dot + gray pill (dot===pill for every row). Gate cell 38 (dot ≥15 + green + glow) still green. Version v1.24.10. **NEXT: run the extensive E2E (student-link end-to-end) Yariv asked for, then deploy-gate, then hand deploy runbook.** Tree: version.ts + orgAvailability.ts + EmployersPage.tsx + this log modified (uncommitted — commit after E2E+gate).

- 2026-07-15 — ✅ v1.24.10 gate + E2E GREEN, then Yariv EXPANDED the design scope. Deploy-gate 38/39 green (only red = pre-existing REG-onscreen-zoom cell 01); NEW **cell 39 status-lifecycle-E2E 4/4** (בתהליך hidden → approve → shown → selectable; other-year student does NOT see it = cross-(course×year) isolation proven end-to-end in a real headless browser). Cells 08/34/36/37/38 all stayed green (no regression from the vivid-colour/22px-dot change). Committed as a clean checkpoint. **THEN Yariv: "pay attention that it is not only dots size — it is the COMPLETE design of the way the view is CHOSEN and PRESENTED with its internal logic that SEPARATES a course-per-year from other courses."** → the real ask is the Employers view's INFORMATION ARCHITECTURE: the TopBar has TWO independent dropdowns (קורס default __all__/"כל הקורסים" + שנה), and EmployersPage renders a FLAT list with NO course grouping — so when course=__all__, `yearCourseIds` = ALL courses of the year and capacity/status are SUMMED across courses (HR+counseling), the exact opposite of "(course×year) is one unit". Launched a design Workflow (understand→3 designs→judge panel→synthesize) `wf_e9bd7f6d-7b2` to design the fuller presentation (chooser + per-course grouping + per-(course×year) summary + kill the cross-course sum). NEXT: review synthesis → mockup → confirm direction with Yariv → implement as v1.25.0 → gate + E2E. v1.24.10 dot/colour polish stands as the interim; the redesign builds on it.

- 2026-07-15 — 🧭 DESIGN DECIDED (Yariv, after mockup + design Workflow `wf_e9bd7f6d-7b2`): rebuild the Employers view around per-(course×year) UNITS. **Yariv's refinement (KEY):** "as the first step is to pick a year and a course, you should only see the org within this year and course unless you add a second view of year+course — that way it is clean." So: (a) pick a specific (course × year) → CLEAN single-unit list, each org once, scoped to that unit; (b) widen (all courses / all years) OR explicitly ADD a compare unit → sectioned multi-view (health board + one section per (course×year), an org fanned out to one row per unit it serves = the isolation proof); (c) TOP summary = a per-(course×year) HEALTH BOARD (Yariv chose option 1), replacing the 4 summed stat-boxes that blended courses. Architecture find: the card/row ALREADY scope by `scopeCourseIds` correctly — the cross-course SUM is purely page-level (`yearCourseIds` = every course of the year when course=__all__, EmployersPage.tsx:188-197) + the summed StatBoxes (222-224). FIX = drive the page by resolved units, pass each row a SINGLE unit courseId, delete yearCourseIds + selectedYear=years[0] + summed boxes + the redundant in-page course <select>; reuse the app's own groupByYearCourse (pageShared.ts:54) + GroupHeader (StudentsPage.tsx:851). Implementing now as v1.25.0 → gate + NEW cell 40 + E2E. v1.24.10 (colour/22px dot) already committed+pushed `97a45f80` as the interim.

- 2026-07-15 — ✅ v1.25.0 EMPLOYERS (course × year) VIEW REBUILD — built, browser-verified, gate 39/40. Implemented the decided design in `src/components/EmployersPage.tsx` (single file, as planned): the page is now organized by (course × year) UNITS. **Behaviour:** (a) pick a specific course + year → ONE clean section (that unit's rows, each org once, hero shows that unit's own פתוחות/מאוישות — no board); (b) widen to all-courses/all-years OR pin a unit via the new "➕ הוסף קורס/שנה להשוואה" control → multi-view = a per-(course×year) HEALTH BOARD (one card each, click-scrolls) + one section per unit (serif course name + year chip + "× הסר" + rollup strip). **Mechanism:** employers are FANNED OUT — one entry per (employer × unit it serves) → an org in HR+Counseling shows as two rows under two sections. **Deleted the cross-course sum at the root:** yearCourseIds (all-courses-of-year), selectedYear=years[0] fallback, the 4 summed StatBoxes (→ distinct-employer count + per-unit rollups), the redundant in-page course <select> (→ compare control). Each row/card gets a SINGLE-id `scopeCourseIds={[unit]}` so dot=pill=capacity all describe one unit (card/row internals already scoped correctly — the bug was purely page-level). **Verified in real browser (:4325, real prod data):** single-unit (משאבי אנוש × תשפ״ז) = 1 section/no board; all-courses × תשפ״ז = 2 sections + 2 health cards; compare-pin (יעוץ ארגוני) → 2 sections + board; 0 console errors. **NEW gate cell 40** (SECTIONS-per-unit / ISOLATION-two-dots / NO-blended-count / GRID-also-groups): a seeded org FULL in course A + OPEN in course B same year renders a slate 'מלא' dot under A + a green 'מאושר·1' dot under B — **4/4 GREEN on new code, proven 0/4 RED on old flat-list code via git stash** (tests desired behaviour, not the impl). Full gate **39/40** (only red = pre-existing REG-onscreen-zoom cell 01); cell 39 E2E student-link still 4/4; 08/31/33/34/36/37/38 all green (no regression). Version v1.25.0. **DEPLOY: Yariv runs `npm run deploy` (gate-green; the deploy cmd is classifier-blocked for Claude) → then smoke-test on his iPhone/PWA per [[skill_deploy_validation_outcome_and_device]].** All committed/pushed to main; tree clean.

- 2026-07-15 — 🚀 v1.25.0 DEPLOYED TO PRODUCTION. Yariv reported the live site still showed v1.24.7 "even in private mode" (correct — nothing since v1.24.7 had ever been deployed; private mode = no SW, so it was the true deployed build). Ran `npm run deploy` (gate 39/40 green = standing authorization): astro build clean (6 pages, 2.4s) → `wrangler pages deploy` → **✨ Deployment complete, deploy id `55fe8d9e`** (https://55fe8d9e.practicum-v2.pages.dev). **Production domain https://practicum.yarivitzkovich.org now serves `v1.25.0+build.71.f55de428`** (verified: page <title> shows the version in a fresh browser context). This ONE deploy shipped everything queued since v1.24.7: v1.24.8 manual-approve, v1.24.9 status workflow (green-only student link + filter), v1.24.10 vivid/22px dots, v1.25.0 (course×year) section rebuild, AND the v1.24.5 PWA updateViaCache:'none' fix. NOTE: the updateViaCache fix only prevents FUTURE stale-SW; the currently-stuck old worker on Yariv's normal (non-private) browser may still serve v1.24.7 until ONE manual clear (Safari → clear practicum website data) or a couple reloads — private mode shows v1.25.0 immediately. Build counter 70→71 stamped into version.ts by the predeploy hook (committed next). **STILL YARIV'S (unavoidable):** on-device iPhone/PWA smoke-test of the new (course×year) sections + student link.

- 2026-07-15 — 🟢 v1.26.0 STATUS UX + AUTO-GREEN (Yariv, on live v1.25.0: "פרקטיקום משאבי אנוש בתשפ״ז לא נותן לערוך את בתהליך"). Diagnosed by reproducing his exact flow in the sandbox browser: the edit DID work (open→🟢 מאושר→שמור→green+history), so the traps were (1) the status change only saved on the far-down שמור button, and (2) a manual בתהליך blocked auto-green even with places+desc. Asked Yariv (AskUserQuestion) → he chose ALL THREE fixes. Implemented: **(1) auto-save on chip click** — the editor status chips persist immediately (no separate שמור), sheet stays open (`onQuickSave` → `handleQuickSave`); **(2) one-tap row toggle** — the status pill on each list row/card opens an inline 4-chip picker that writes immediately (`onSetStatus` → `handleSetStatus`), no editor; **(3) auto-green when ready** — reordered `employerStatus` so `av.available` (desc + open place for THIS course×year) beats `contactStatus:'in_process'` → a ready בתהליך org auto-advances to 🟢 מאושר; only 🔴 נדחה blocks it. New shared `applyEmployerStatus(emp, key)` used by BOTH editor + row so they write identically. **Semantic consequence (Yariv chose it):** a ready in_process org is now GREEN → SHOWN on the student link (was hidden). **Gate cells updated for the new precedence:** 08 (auto-green beats in_process in the replication), 36 (split into NOT-ready-בתהליך HIDDEN + READY-בתהליך SHOWN), 37 (in_process seed made not-ready so the filter still catches it), 39 (hidden state = not-ready no-desc). **NEW cell 41** (AUTOGREEN-ready green / AUTOGREEN-not orange / ROW-toggle-saves persists rejected+red) — 3/3, browser-verified all 3 fixes end-to-end (chip auto-save persists w/o שמור, row toggle persists w/o editor, auto-green renders). Version v1.26.0. Running full gate; deploy after green.

- 2026-07-15 (later) — v1.26.0 gate GREEN (only pre-existing cell 01 red); cells 33+35 were a span→button selector break from the one-tap pill (fixed to query `button, span` + strip the ▾), NOT a behavior regression. Deploying.

- 2026-07-15 (later²) — 🔄 v1.26.1 SW AUTO-UPDATE HARDENING. Yariv again saw a stale version (1.25.0 while prod was 1.26.0) — root cause = a service worker installed BEFORE the updateViaCache/network-first fixes cannot detect the new worker on its own (needs one manual clear). Confirmed the self-heal machinery is otherwise sound (Layout.astro: network-first SW, updateViaCache:none, controllerchange→reload, checkFreshBuild version-compare via <meta app-version>). THE GAP: sw.js was byte-identical across deploys (static `const CACHE = practicum-v2-cache-6`), so the "new worker → reload" trigger never fired — relied solely on checkFreshBuild. FIX: `scripts/bump-version.mjs` now stamps the SW cache name with the exact version on every bump (`practicum-v2-cache-<version>`), so each deploy is a genuinely new worker → skipWaiting+clients.claim+controllerchange→auto-reload (2nd independent trigger). Verified live: prod serves v1.26.1+build.73.338ac83a and /sw.js CACHE = practicum-v2-cache-v1.26.1+build.73.338ac83a. Deploy 2bfe8b58. Yariv still needs ONE manual clear for his currently-stuck pre-fix worker; after that, deploys auto-refresh.

- 2026-07-15 (later³) — 🅧 v1.26.2 CLOSE-UX (Yariv, mobile: "הטופס לא נסגר צריך x סגירה קטן בכל כרטיס ובכל מסך... וששמירה תסגור את הכרטיס"). The one-tap status picker (v1.26.0) had no dismiss. Added: (1) a ✕ button + tap-outside-to-close + close-on-pick to StatusChips (row + card); (2) a "✕ סגור" button on the expanded employer row (previously only re-tap-header collapsed); (3) editor now SAVES-AND-CLOSES — handleQuickSave (status chip auto-save) closes the sheet, matching the existing שמור-closes + top סגור ✕. Status stays auto-saved ("נשמר מיד ✓"). Browser-verified: ✕ closes, outside-tap closes, expanded-card ✕ collapses; status cells 33/35/37/41 still green; full gate green except pre-existing cell 01. v1.26.2. NOTE: applied to the Employers page only (where Yariv hit it) — offered to sweep the same pattern to students/candidates/lecturer editors if wanted.

- 2026-07-15 (later⁴) — 🔁 feedback-reminders CC made DATA-DRIVEN (CLI secret path abandoned). Yariv could not set the FEEDBACK_REMINDER_CC secret: the CLI `secrets` subcommand fails "Invalid access token format" in v2.102.0 even with a valid token (functions commands work fine — proven: functions list/deploy succeed, secrets list/set fail, same token/shell). Root-caused as a `secrets`-subcommand bug, NOT a token issue. FIX: added a `feedbackReminderCc` field (array or comma-string) to practicum_data read by the function CC builder (alongside supervisorEmail + the legacy secret) → deployed feedback-reminders v5 (from Claude shell — classifier allowed functions deploy this time). CC now settable via the anon-key PATCH (no CLI). Set feedbackReminderCc=["yariv@ariel.ac.il"] → dry-run CC now ["itzkovichyariv@gmail.com","yariv@ariel.ac.il"]. REMAINING: (a) add Rachel (need her email → 1 data write, no deploy); (b) confirm the Sunday cron in the dashboard (only dashboard-login step); (c) backfill feedbackRequestedAt on the 8 open students at activation (Yariv: "start from the day the reminder was sent"). NOTE: supervisorEmail left untouched (used by notify-acceptance/rejection/submission BCCs + SettingsPage).

- 2026-07-15 (later⁵) — ✅ feedback-reminders ARMED for Sun 2026-07-19. CC finalized via data (feedbackReminderCc=["yariv@ariel.ac.il","rachelshal@ariel.ac.il"]) → dry-run cc=[gmail, yariv@ariel, rachelshal@ariel]. Yariv chose "this Sunday" activation → backfilled feedbackRequestedAt=2026-07-07 (8 days back) on the 8 placed-without-feedback students that have a token → **dry-run now remind=8, missing=0**: דניאל שטרקס/Aqurate, עמית שלם/Codeoasis, אלה לוי/עיריית אריאל, שירי הולנדר/Icon Group·I digital, שיראל קורן+נעמה ביטרמן/Manpower, עדי בוקי/אוניברסיטת אריאל, שיראל אבוקסיס/קופ״ח מאוחדת — all with valid mentor emails (post the 2-student fix). **ONLY REMAINING = Yariv schedules the weekly cron in the Supabase dashboard (Integrations→Cron, `0 5 * * 0`, Edge Function feedback-reminders, POST) — the one dashboard-login step; his browser is now logged in from the CLI login. No cron = nothing fires.** Offered a SQL/pg_cron fallback if the dashboard UI is unavailable. Nothing emailed yet (dry-run sends nothing; cron is the trigger).

- 2026-07-15 (later⁶) — 🎉 feedback-reminders FULLY LIVE (cron set via SQL, no dashboard). Yariv said "do the sql" → linked the project (`supabase link`, skipped DB pw) and used `supabase db query --linked` (Management API, runs as postgres) since the CLI `secrets` subcommand was broken but `functions`/`db` work. pg_cron + pg_net already enabled. Created cron **job id 3 `feedback-reminders-weekly` schedule `0 5 * * 0` active=true** → net.http_post to the feedback-reminders function (apikey+Authorization anon headers, function is --no-verify-jwt). **VERIFIED END-TO-END:** fired the identical call with ?dry=1 via pg_net → net._http_response id=22 status_code=200 body={ok, dryRun, remind:8, missing:0, cc:[gmail,yariv@ariel,rachelshal@ariel], wouldSend:[8]}. So cron→pg_net→function→correct 8 recipients all proven; Sunday run is identical minus ?dry=1 (=real send). **DONE: function v5 + data-driven CC + 8 anchored (feedbackRequestedAt=2026-07-07) + weekly cron active + verified.** First real reminders Sun 2026-07-19 ~05:00 UTC/08:00 IL to the 8 mentors, CC Yariv+Rachel, weekly thereafter to only those still missing. Nothing emailed yet (all dry). Repo tree clean (supabase link artifacts gitignored).

- 2026-07-15 (later⁷) — ✅ feedback-reminders VERIFIED WITH A REAL SEND (Yariv confirmed receipt). Controlled test: parked the 7 real students (anchor→now), seeded a temp student with mentor=delivered@resend.dev, CC temporarily = only Yarivi@ariel.ac.il (Rachel excluded so she is not test-spammed) → dry-run remind=1/to=delivered@resend.dev/cc=[Yarivi]; then REAL send (no ?dry) → sent:true, Resend id bd6d61b9-bf6c-48f5-a1e0-f25a028bc646, 0 errors. **Yariv confirmed the CC email arrived in his Ariel mailbox** = end-to-end proof (function sends for real + CC delivers + wording renders). Restored exactly: 7 students re-armed (anchor 2026-07-07), CC back to [Yarivi@ariel.ac.il, rachelshal@ariel.ac.il], temp org+student deleted, dry-run confirmed remind=7/no test recipient. NOTE: שירי הולנדר now has feedback (submitted 2026-07-15 14:05Z) so she correctly dropped 8→7. Also this session: fixed Yariv git email→Yarivi@ariel.ac.il, set supervisorEmail=Yarivi@ariel.ac.il (drops gmail from the reminder CC AND routes notify-acceptance/rejection/submission BCCs to Ariel — flagged to Yariv). SYSTEM FULLY DONE — first real reminders Sun 2026-07-19 ~08:00 IL to 7 mentors, CC Yariv+Rachel, weekly self-pruning thereafter.

- 2026-07-15 (later⁸) — ✏️ v1.26.3 rename employer "הערות" field → "תיאור ההתמחות בארגון" (Yariv: the field is the student-facing internship description, not "notes"). EmployerEditor: label הערות→תיאור ההתמחות בארגון + placeholder ("מה הסטודנט/ית תעשה...") + hint ("מוצג לסטודנט/ית בעמוד בחירת הארגונים · שדה זה + מקום פנוי = מאושר/ירוק"), rows 2→3. Clarified the two comment fields: notes (הערות→now תיאור ההתמחות) = student-facing description + drives hasDesc/green; statusNote (הערת סטטוס) = admin-only card note. Browser-verified new label/placeholder/hint render, old label gone; gate green except pre-existing cell 01. Yariv will rewrite the actual פסגות description text himself (data). Version v1.26.3.

- 2026-07-15 (later⁹) — 🛟 DATA RECOVERY + durability fix. Yariv: "המעסיקה מילאה משוב על שירי הולנדר אבל אין לו חיווי, שחזר אותו + כל מי שמילא ואין לו חיווי." Root cause = **my own raw REST PATCHes (whole-blob replacement, no CAS merge) during the CC/reminder work clobbered concurrent app writes.** Traced via practicum_snapshots audit log: שירי (id 12) feedback submitted by the employer 2026-07-15 14:05:24 (712 chars, feedbackText JSON + feedbackSubmittedAt + hired) → wiped to "" by 15:05:53. **Scan of all 14 students who EVER had feedback (feedbackText OR feedbackSubmittedAt) vs now: ONLY שירי was lost** (13 intact). RECOVERED via atomic SQL (supabase db query --linked, jsonb_set, order-preserving, 85→85 students) — restored feedbackText/feedbackSubmittedAt/hired from the 14:05 snapshot. Verified: fbLen 712, hasEmployerFeedback=true (✓ משוב shows), excluded from reminder. ALSO found the 7 reminder anchors (feedbackRequestedAt) had been wiped the same way (remind dropped 7→0). **Durable fix (function v6): the reminder now hinges on the STABLE feedbackToken, not the fragile anchor** — a placed+token+no-feedback student with NO anchor is treated as OVERDUE (legacy link sent long ago) and reminded; students WITH an anchor still get the 7-day grace (new links get feedbackRequestedAt from dataApi:191). Dry-run back to remind=7, missing=0, CC [Yarivi@ariel, rachelshal], all "legacy/overdue", שירי excluded. **LESSON: for practicum_data, use atomic SQL jsonb merges (db query --linked UPDATE), NOT whole-blob REST PATCHes — the latter lose-update concurrent saves.** snapshots = the recovery lifeline.

- 2026-07-16 — 🛡️ REMINDER DATA-LOSS SAFEGUARDS (Yariv's "both": a pre-send check + an in-function guard). Follow-up to the 07-15 שירי wipe. **(1) `scripts/preflight-reminders.mjs`** — REST-only (anon key, no CLI/auth), run before any Sunday: checks ① core collections present, ② NO feedback lost vs the last 25 practicum_snapshots (feedback is monotonic — any id that HAD feedback and now doesn't = flagged), ③ the reminder dry-run recipients + every target has a real mentor email + the CC, ④ per-(course×year) student-link availability. Exit 0=safe, 1=stop. **(2) In-function guard (feedback-reminders v7, deployed)** — before sending, compares current feedback count + student roster to the last 25 snapshots; feedback DROP (or roster collapse >10%) ⇒ ABORT the send and email Yariv+Rachel a Hebrew alert (`integrityAlertHtml`) instead of mailing employers on corrupted data. `?dry=1` REPORTS the abort (`{aborted,detail,curFbCount,maxFb,alerted:false}`) without alerting. CC now built BEFORE the guard so the alert can reach it. **VERIFIED end-to-end on live prod:** (A) good data → dry not-aborted, remind=7, CC [Yarivi@ariel, rachelshal]; preflight exit 0 all-green (85 stu / 14 fb intact / 0 lost / 7 valid targets). (B) simulated loss → inserted a synthetic 100-feedback snapshot via `db query --linked` → dry `aborted:true "feedback count dropped — now 14, recent max 100"`, `alerted:false` (dry=report only) → **deleted the synthetic snapshot** → dry back to normal (aborted:false, remind=7). preflight's section-3 reordered to read `aborted` before `counts` (else it threw when the guard aborts). No app deploy (edge-function + script only; version.ts untouched). Committing to main; note: family-tasks has unrelated in-progress calendar files left dirty (not mine). **First real reminders still Sun 2026-07-19 — now guarded: if anything wiped feedback before then, employers get NOTHING and Yariv+Rachel get an alert instead.**
- 2026-07-19 — 🔒 **PRIVACY FIX shipped: public /organizations was leaking other programmes' orgs — v1.26.4+build.76 (`987752c9`)**, deploy `https://5b9316c2.practicum-v2.pages.dev` (live). Yariv met the **פרקטיקום משאבי אנוש תשפ״ז** students; the link they opened listed **15 organizations, 5 of which belong to other programmes** (אוסם נסטלה, מעוף משאבי אנוש, עמותת צ'ימס, עמותת שער השוויון, LTS Device technologies). Requirement: a student sees ONLY their own course's **approved (green)** orgs — 10. **Root cause:** `OrganizationsPage` computed `scope = studentCourse || courseFilter` and then guarded EVERY filter with `if (scope && …)`. With no `?email=` and no `?course=` (an unrecognised email, or a link carrying neither) `scope` was empty, so the course filter, the course×year green check AND the availability check were **all skipped** → it listed every globally-approved org across all courses and years. The page **failed OPEN**: a missing URL parameter widened access. **Fix (Yariv chose "show nothing until identified"):** (a) **fail closed** — no resolved course ⇒ list nothing + show the identify prompt; this also neutralises every unscoped link already in students' inboxes (un-recallable), and entering an email self-heals to the right list; (b) an org with **no course assignment** no longer slips through a scoped filter (the `ids.length &&` bypass — latent today since all 24 employers are assigned, but a live trap); (c) acceptance-email **drafts** now embed `/organizations?email=<recipient>` (was a bare unscoped link — the Resend/notify-acceptance path already did this); (d) the **טפסים Stage-2 share link** carries `?course=` for the top-bar course, so there's still a correctly-scoped browse link to show students in a meeting. NOTE: `QuickLinksCard` in FormsPage also builds a bare `/organizations` link but is **dead code (never rendered)** — left alone, flagged here. **Verification:** new cell **`42-public-orgs-scope`** 3/3 — unscoped ⇒ 0 orgs + prompt; `?course=` ⇒ only orgs the DATA assigns to that course (derived, not hardcoded); `?email=` ⇒ identical set to `?course=`. **Proven RED→GREEN by git-restoring the old fail-open logic: `ORGS-fail-closed` reported `orgsListed=15` — the exact number the students saw.** Cells **05** and **08** were asserting the OLD fail-open behaviour (browsed unscoped, expected a populated list) — updated to exercise the real course-scoped student view (05: 10 cards/badge/expand/search against a real course; 08: `0 < visible ≤ orgs-assigned-to-course < admin-total`). Full gate green except the pre-existing `REG-onscreen-zoom` fixture red. **Prod-verified:** `/organizations` → 0 orgs + prompt; `?email=ariella7b@gmail.com` → "שלום אריאלה סינגר" + exactly the 10 correct orgs, 0 leaked extras. OPEN/none.
- 2026-07-19 — 🧭 **Student stage-2 flow AUDIT + fixes 1&2 shipped (v1.26.5+build.77 `ba5734c6`, live).** Yariv asked: will students retype their email every time? what happens if they change preferences? is it all coupled to the free-places count? Ran a 4-angle code audit with adversarial verification (8 agents) + my own live browser tests. **ANSWERS.** (1) *Retype every time* — **YES, confirmed live**: the email lived only in React state, nothing in localStorage/sessionStorage/URL; a reload of a bare link returned 0 orgs + the email box. Only the acceptance-email link (`?email=`) survived. (2) *Preferences* — `/cv-update` writes a **NEW `cv_updates` row per submission** (plain `.insert()`, never an update), so re-submitting appends history and only the NEWEST row feeds placement (`cvHistory[0]`, `.limit(40)`). **Preferences and slot-holds are two disconnected stores** — changing a preference does NOT release a held place, and a student can hold org A while their stated 1st choice is org B. (3) *Slot coupling* — the count IS derived live from the `vacancySlots` ledger and "בקש/י מקום" really does flip a slot to `tentative` for everyone, BUT: the public page **fetched once on mount** (it sits outside App.tsx's `practicum-live` realtime channel) so other students saw stale counts; the places badge for an unidentified visitor used **unscoped** `orgAvailability(emp).open` (counts other courses' slots); two students can **double-book the last place** (the `saveSnapshot` CAS guards only the window INSIDE saveSnapshot, not the caller's read→write window — and there is an intentional unguarded last-writer-wins fallback after 5 failed CAS attempts, dataApi.ts:92-105); a held place has **no TTL** — an abandoned hold suppresses the count forever. Also found: suggested-org is implemented TWICE with different fidelity (approving from the student card does **not** persist `firstChoiceOrg` — StudentsPage saves employers only; path B never writes `dismissedSuggestionIds`, so the same suggestion stays live on the Employers banner → duplicate-employer route; no name-matching against existing employers; the student is never notified their suggestion was approved). **SHIPPED NOW (fixes 1+2):** verified identity remembered on-device (only when the email resolves to a real student — never a typo) + explicit **"זה לא אני"** escape for shared phones; page refetches on focus/visibilitychange + 45s interval; places badge always `countSlotsByStatus(scope)`. Cell `42-public-orgs-scope` extended to **5/5** (adds `ORGS-badge-scoped` + `ORGS-remember-email`). Gate green except the pre-existing `REG-onscreen-zoom`. **Prod-verified:** type email once → reload bare link → still "שלום אריאלה סינגר" + 10 orgs. **STILL OPEN (Yariv approved both, shipping separately):** ③ last-place double-booking race (needs a two-browser repro to prove the fix), ④ connect preferences ↔ held places (needs a product decision from Yariv on what changing a preference SHOULD do to an existing hold). Also unresolved/pre-existing: anon UPDATE on `cv_updates` is RLS-filtered to 0 rows so "mark seen" silently no-ops at 6 sites (policy never applied).
- 2026-07-19 — 🏁 **FIX ③ shipped: two students could BOTH be told they got the last place — v1.26.6+build.78, deploy `https://1f5304ac.practicum-v2.pages.dev` (live).** Yariv's rule: *"אף מקום לא משתחרר אלא אם הסטודנט ויתר עליו"* — this violated it silently. **Mechanism:** `requestOrg` read → computed the hold → handed a **pre-computed blob** to `saveSnapshot`. The CAS only guards the window INSIDE `saveSnapshot`; the caller's own read→compute→write window was unguarded. Two simultaneous clicks on one place both "succeeded": the loser lost the CAS, retried by replaying a blob computed BEFORE the race, and replaced the `employers` array wholesale — **erasing the winner's reservation**. Both students saw "המקום שמור עבורך"; only one actually held it; the other would have shown up at an org believing they had a spot. **Fix:** `saveSnapshot` now accepts a **`SaveMutator`** re-run against freshly-read cloud state on EVERY CAS attempt, so a retry recomputes from the winner's data and `studentRequestHold` declines cleanly ("אין כרגע מקום פנוי") instead of overwriting. The unguarded last-writer-wins **safety-floor is disabled for mutators** (for a reservation, failing loudly is required; writing blindly IS the lost update) — plain-blob callers keep the floor so nothing else regresses. **PROVEN RED→GREEN with a real two-browser concurrent race** (`43-hold-race`): buggy 3 runs → `A=success B=success` holder=A/A/B ❌; fixed 3 runs → one success + one clean refusal, and the holder is always the one told success ✅. **A sequential "stale page" test does NOT catch this** (the old code re-read at click time and refused) — my first attempt at this cell passed on the buggy code and was rewritten; the cell header records this so it isn't "simplified" back into a false pass. Gate green except the pre-existing `REG-onscreen-zoom`. **STILL OPEN:** ④ preferences ↔ holds — Yariv wants each of 3 preferences to hold a place, but **capacity blocks it**: פרקטיקום מש״א תשפ״ז has **11 students × 3 = 33 demanded vs 21 places** (⛔ short by 12), so the first ~7 students would consume everything. Decision needed from Yariv (raise capacity, or only the 1st preference holds, or preferences stay advisory + warn). He also chose: **surface stale holds, never auto-release**.
- 2026-07-19 — ⏸ **Fix ④ (preferences ↔ places) DESIGNED + PROVEN but NOT SHIPPED — parked on branch `wip/prefs-dont-reserve` (pushed).** `main` remains clean at **v1.26.6+build.78**. Yariv's model (his words): *"מאחר ואלו העדפות — מה שקובע תפיסה של מקום צריך להיות שליחת קורות חיים… ליד כל העדפה צריכה להיות תיבת סימון לשליחת קורות החיים. אם אין מקום התיבה לא [תפעל]. התיבה בכל מקרה לוקחת מקורות החיים המעודכנים"* + *"אף מקום לא משתחרר אלא אם הסטודנט ויתר עליו"*. **Why the change was needed:** `buildPlacementPreferences` flipped a slot to `tentative` for EVERY preference, so 3 prefs × N students consumed 3N places — פרקטיקום מש״א תשפ״ז demanded **11×3=33 against 21 real places**, i.e. a course could read "full" before a single CV went out. **Done on the branch:** preferences reserve nothing (slotId:null, status 'tentative' = "ממתין לשליחה"); a preference for a FULL org is now allowed (intent only); `addPlacementPreference` given the same rule; PlacementPanel's send now ACQUIRES an available slot at send time and is blocked unless there is BOTH an updated CV and a free place (reason surfaced); the coordinator's capacity chip made course-scoped (it was counting other courses'/years' places); `StudentPreference.slotId` → `string | null`; stale "משריין מקום" labels corrected. **Proven RED→GREEN** by new cell `44-prefs-dont-reserve`, which runs the REAL library through esbuild: old code turned 6 free places into 3 and refused a full org; new code 6→6 and accepts it. Cell 13 was **re-pinned to the new rule** (slotId===null, place stays `available`) rather than weakened — its build half passes end-to-end. **WHY NOT SHIPPED (3 open items):** (1) the AD-HOC "add an employer outside the list" path stopped creating its preference (cell 13 `adhoc prefOk=false`) — undiagnosed, in StudentEditor's handler; (2) the per-preference **checkbox** Yariv asked for is not built — a single checkbox cannot choose WhatsApp-vs-email, so the existing send buttons were kept and merely gated; needs his decision; (3) cell `14-vacancy-counter` likely still asserts the old reserve-on-build rule. **LESSONS THIS SESSION:** (a) a 10-min Bash timeout SIGTERMs a dev server started in the same command — the resulting 18 gate reds were ALL "app unreachable", not code; start the dev server detached and run the gate with `run_in_background`. (b) My race cell's cleanup lost its CAS during the concurrent gate and left a test employer + 2 students in PROD (since removed) — audit cleanups need CAS retry. (c) Never conclude "I broke cell X" from ONE run — cell 31 failed once then passed 3/3; several gate cells are genuinely flaky under contention (05, 17, 28, 31). (d) I ran `npm run build` while the dev server was up, violating skill_gate_no_concurrent_build.
- 2026-07-19 — 📋 **SPEC captured for finishing fix ④ (Yariv, verbatim intent). Nothing built yet — `main` still v1.26.6, work parked on `wip/prefs-dont-reserve`.**
  **(A) Student-suggested org → special resolution path.** *"בכל מקרה אם סטודנט העלה ארגון זו הופכת להיות העדפה ראשונה שלו ואז אם הוא כבר במגעים עם הארגון אין צורך לשלוח קורות חיים… המערכת שואלת אם לשלוח קורות חיים או שהסטודנט כבר במגעים מתקדמים עם הארגון וצריך רק אישור של יריב. אישור של יריב משמעו השמה."* → For an org the STUDENT proposed (which must become **preference #1**), the system must OFFER TWO PATHS: (1) **שלח קו״ח** — the normal flow (acquires a place, → `under_review`, employer reviews); or (2) **כבר במגעים מתקדמים — נדרש רק אישורך** — NO CV is sent, and Yariv's approval means **השמה: the slot goes straight to `placed`**, skipping the employer-review loop. ⚠ DESIGN NOTE: path (2) commits a student with one click and consumes the place as `placed` — must be behind a confirm dialog, and it interacts with the "no place is released unless the student gave it up" rule.
  ⚠ **PREREQUISITE BUG (found in the 4-agent audit, still open):** "the suggestion becomes preference #1" is only reliably true when the suggestion is approved from the **EmployersPage** banner (`EmployersPage.tsx:189` writes `firstChoiceOrg` AND persists students in the same saveSnapshot). Approving from the **student card** (`StudentEditor` path B) sets `firstChoiceOrg` via `setForm` only and `StudentsPage.tsx:817-818` saves `{ ...data, employers }` — **students are never written**, so the first-choice silently does not persist. Also path B never writes `data.dismissedSuggestionIds`, so the same suggestion stays live on the Employers banner → duplicate-employer route. Fix this BEFORE building (A), or (A) will be built on a flow that half-works.
  **(B) Student should not have to type an email at all.** *"לא ברור לי למה הסטודנט צריך להקיש את המייל שלו. הלינק כבר נשלח למייל שלו זה זיהוי מספק. אם אפשר להימנע מזה אני מאשר."* → STATUS: already true for every PERSONAL link (`?email=` — acceptance email via notify-acceptance, the per-recipient Outlook draft fixed today, and the מועמדים per-student link). Typing only appears on the GENERIC טפסים "שלב 2 · רשימת הארגונים" link, which carries `?course=` but deliberately no identity because it is shared; and since v1.26.5 even that is once per device (remembered, with a "זה לא אני" escape). REMAINING OPTIONAL WORK: carry identity over from `/cv-update?email=…` (write the same `practicum_student_email` key on load/submit) so a student who did stage 2 is recognised on `/organizations` without typing. Recommend telling Yariv to share the PERSONAL link rather than the generic one.
- 2026-07-19 — 🔧 **FIX shipped: student-card approval of a suggested org now persists first choice — v1.26.7+build.79, deploy `https://72d6e8ab.practicum-v2.pages.dev` (live). Prereq for the parked fix ④.** Yariv "כן תקן". Bug (found in the 4-agent audit): approving a student-suggested org from the STUDENT CARD ran `onApproveSuggestion` which saved ONLY employers — `firstChoiceOrg` (set in the editor's local form) was dropped unless the coordinator separately hit Save, and `dismissedSuggestionIds` was never written. Second bug it exposed: `PendingSuggestionsBanner` (StudentsPage) ignored `dismissedSuggestionIds` and filtered only on `cv_updates.seen_at` — which anon CANNOT set under RLS — so approved/dismissed suggestions reappeared in the banner forever. **Fix** (mirrors the working EmployersPage path): card-approve now persists in ONE save — private employer (with `setCourseCapacity(courseId,1)`) + student.`firstChoiceOrg` + suggestion added to `dismissedSuggestionIds`; the `onApproveSuggestion` signature now carries `{studentId, firstChoiceOrgName, courseId, suggestionId}`; and `PendingSuggestionsBanner` now honours `dismissedSuggestionIds` (the only anon-writable "handled" marker). **Proven RED→GREEN** with new cell `45-suggestion-firstchoice` (seed student + `cv_updates` suggestion → open card → approve → assert DB): old handler → `firstChoice=""` + `dismissed=false`; fixed → firstChoiceOrg set + private employer + dismissed. Gate green except the pre-existing `REG-onscreen-zoom`. **DATA NOTE:** anon can INSERT `cv_updates` but NOT update/delete it (RLS) — my test runs left 8 audit suggestion rows (`sug-*@audit.local` / `audit-cvtest-*`) that I added to `dismissedSuggestionIds` (v1791), so they're HIDDEN post-deploy but still physically in `cv_updates`; a service-role SQL `DELETE FROM cv_updates WHERE email LIKE '%@audit.local'` would remove them for good (optional). Cell 45's cleanup hardened with a CAS-retry loop so it won't recur. **NEXT (unchanged):** finish parked `wip/prefs-dont-reserve` (ad-hoc pref regression + the per-preference checkbox decision) then build Yariv's two-path suggested-org resolution (שלח קו״ח / כבר במגעים → אישור=השמה) — spec already in this log.
- 2026-07-20 — ✅ **Fix ④ COMPLETE on `wip/prefs-dont-reserve` (rebased onto v1.26.7 main) — pending final gate → ship.** Yariv's CV-sending model built end to end. (1) **Preferences reserve nothing** — `buildPlacementPreferences` + `addPlacementPreference` create prefs with `slotId:null` / status `tentative` ("ממתין לשליחה"); a preference for a full org is allowed (intent only). Dissolves the 33-vs-21 capacity problem. `StudentPreference.slotId` → `string|null`. (2) **Per-preference "שלח קו״ח · תופס מקום" CHECKBOX** (Yariv's requested affordance, his channel decision = "checkbox → ask channel"): ticking opens a WhatsApp/מייל picker; choosing a channel calls the existing `handleDispatch`, which now ACQUIRES an available course-slot at that moment and flips it to `under_review`. Disabled (greyed + reason) unless there's an updated CV AND a free place. (3) **Sent = checked box** ("✓ קו״ח נשלח · ממתין לתשובת המעסיק"); unticking → withdraw-confirm → releases the place (a place is only ever freed by an explicit coordinator action). Capacity chip in PlacementPanel made course-scoped. **The "ad-hoc regression" that parked this was a FALSE ALARM** — cell 13 passes 3/3 (one flaky run during a polluted gate). **Cells:** `44-prefs-dont-reserve` (RED→GREEN proven: old build turned 6 free→3 + refused full org; new 6→6 + accepts), `14-vacancy-counter` REWRITTEN to the new model (build keeps both places open; tick send-CV + pick channel → 1 taken + under_review — passes), `13-placement-bridge` re-pinned (slotId===null, place stays available), `16-final-placement` updated to send via the checkbox (was clicking the bare WhatsApp button; passes). Only cells 14+16 reference dispatch, both updated. **NEXT after ship:** the two-path suggested-org flow (שלח קו״ח / כבר במגעים מתקדמים → אישור=השמה) — spec in this log; its prerequisite (card-approve persists first choice) already shipped in v1.26.7.
- 2026-07-20 — 🚢 **Fix ④ SHIPPED as v1.27.0+build.80 (`03b2fab6`/`d203f1b4`, live) — the CV-sending model above is in production.** (Log had recorded it as "pending ship"; confirming it went live.) Preferences reserve nothing; the per-preference "שלח קו״ח · תופס מקום" checkbox + WhatsApp/מייל picker takes a place only on send; sent = checked box; untick → withdraw-confirm → releases. Gate green except the pre-existing `REG-onscreen-zoom`.
- 2026-07-20 — ✅ **TWO-PATH suggested-org flow SHIPPED — v1.28.0+build.81.69ad0fef, deploy `https://6abf9203.practicum-v2.pages.dev` (live).** This closes spec item **(A)** (2026-07-19 SPEC in this log) — Yariv: *"אם הוא כבר במגעים עם הארגון אין צורך לשלוח קורות חיים… אישור של יריב משמעו השמה."* **What shipped:** for an org the STUDENT proposed (a private employer `restrictedToStudentId===student.id`, which is already forced to preference #1 by the v1.26.7 fix), the placement panel now shows a SECOND path beside the "שלח קו״ח" checkbox — a green **"✓ כבר במגעים — אשר שיבוץ"** button (`data-place-direct`), rendered ONLY for a suggested org in the tentative state. Clicking it opens a confirm dialog **"✅ שיבוץ ישיר (השמה)"** ("{student} כבר במגעים מתקדמים עם {org}. לאשר שיבוץ ישיר — ללא שליחת קו״ח? פעולה זו מסמנת השמה ותופסת מקום.") → **"אשר שיבוץ ישיר →"**. **Behaviour:** `handlePlaceDirect` acquires a free course-slot at the org — or **MINTS a `placed` slot** when the private org carries none (the suggestion IS the place, so path 2 always succeeds for the proposing student, even with 0 provisioned vacancies) — marks it `placed` held by the student, sets the preference to `placed` holding that slot, and converges the student to `submissionStatus='placed'` + `acceptedOrg` + `placedAt`. **No CV is sent**; the employer-review loop (`under_review`) is skipped. Same final state the send→נקלט path reaches, in one step. Uses the established **plain-blob coordinator-write** model (matches `handleResult`/`handleRelease`) — the SaveMutator hardening from fix ③ is for the high-concurrency PUBLIC student-hold path, not single-admin coordinator writes. **Proven RED→GREEN** with new cell **`46-suggested-place-direct`**: seeds a student whose pref #1 is a restricted org with **zero** slots + no CV → git-stash the PlacementPanel change → `btnSeen=false` → RED (0/1); restore → full convergence (btnSeen/approved/acceptedOrg/statusPlaced/prefPlaced/slotPlaced all true) → GREEN. **Gate flakiness caught + fixed honestly:** cell 46 passed standalone but FALSE-RED'd once when it ran last in the full gate (`approved=true` but `acceptedOrg=false`) — `onDataChange`→`saveSnapshot({...data,...patch})` is a plain-blob CAS-with-backoff; under peak gate load (right after the 86s cell 42) the CAS retried past the 4.8s DB poll, i.e. a slow-but-successful write, not a lost write. Hardened the cell to gate the DB read on the UI's **"✅ שובץ"** completion signal + a longer settle (24×500ms) — re-ran the FULL gate: cell 46 GREEN in-context (8.3s), no regressions. Visual-verified in a real browser (button green #15803d alongside the send-CV checkbox, RTL clean; confirm dialog renders as designed). **Gate: 45/46 green, only the pre-existing `REG-onscreen-zoom` red** (REG-validation + REG-happy-path pass). **NEXT:** the integrated end-to-end test of ALL the session's features together (orgs scope + remember-email + live counts + suggestion→first-choice → send-CV checkbox → placement → two-path direct placement) — Yariv: "continue and at the end test all these features together."
- 2026-07-20 — ✅ **"Test all these features together" DONE — new integrated E2E cell `47-integrated-e2e` (`2396e8ee`, pushed).** Runs the session's placement features CHAINED in ONE unbroken coordinator session and asserts the HANDOFF between them that no single cell covered: (1) a student SUGGESTED an org → coordinator approves it from the student card "…כבחירה ראשונה" → firstChoiceOrg + a private employer restricted to the student + suggestion dismissed (v1.26.7 feature); (2) coordinator builds preferences ("אשר העדפות והכן לשליחה") → `buildPlacementPreferences` turns the approved first choice into **preference #1 in the tentative state holding no place** (v1.27.0 CV-sending model); (3) because the student is already in advanced contact, coordinator takes **PATH 2** — the "✓ כבר במגעים — אשר שיבוץ" button → confirm → **direct placement (השמה), no CV** (v1.28.0). Asserts the student ends PLACED at the suggested org with one occupancy. **Proven RED→GREEN** by temporarily restoring the pre-feature `PlacementPanel` (`git show d203f1b4:…`): the chain still reaches first-choice + a built tentative pref, but `twoPathSeen=false` → the student is never placed (RED, `accepted=false/status=false/slot=false`); with the feature, full convergence (GREEN, all true). This is the specific integration the isolated cells miss — cell 45 stops at first-choice, cell 46 seeds the pref directly; only 47 proves *approve → build → the button appears → placement* works as one flow. Read-backs gated on the UI "✅ שובץ" signal; CAS-retry cleanup. **FINAL GATE (3rd full run this session): 46/48 green — the only reds are (a) the pre-existing `01 REG-onscreen-zoom` (REG-validation + REG-happy-path both pass) and (b) `39-status-lifecycle-e2e`, a CONFIRMED FLAKE — it passed in the two earlier full-gate runs today and passes 4/4 standalone; it false-reds under peak contention because its approve→realtime→public-visibility read-back has a fixed timeout too short under load (identical root cause to cell 46's earlier false-red).** My session change is test-only from that point and runs AFTER cell 39, so it cannot regress it. **NET: v1.28.0 is live and all placement features — org scope, remember-email, live/scoped counts, atomic hold, suggestion→first-choice, send-CV checkbox, two-path direct placement — are individually gated AND proven to chain end to end.** Known-flaky gate cells now include **39** alongside 05/17/28/31; cells 39 (and 42's 86s runtime) would benefit from the same UI-signal-gated read-back hardening 46/47 use — a test-only follow-up, not a code bug (chip spawned).
- 2026-07-20 — 🩹 **CORRECTION (Yariv): "אין מצב כזה אין קורות חיים — טופס שליחת בקשות מחייב העלאת קורות חיים." My cells 46/47 seeded an IMPOSSIBLE state (a student with no CV); fixed to seed reality. No app-code bug — the invariant is genuinely enforced.** I had conflated "path 2 does not SEND the CV" with "the student has no CV," and wrote the cells (and the entry above) around a CV-less student. **Audit of every student-creating path confirms Yariv:** `/register` hard-blocks submit without a CV (`RegistrationForm.tsx:132` `if (!cv) setErr('יש להעלות קורות חיים')`, plus the upload-failure guard at `:157`) → sets `cvUrl`; the stage-2 request form ("טופס שליחת בקשות") hard-blocks too (`CvUpdateForm.tsx:75` `if (!file) setErr('יש לצרף קובץ קורות חיים…')`, with `cv_updates.cv_file_path NOT NULL` as a server backstop) → and that `cv_file_path` is promoted to `student.cvUpdatedUrl` coordinator-side, automatically on Students-page mount (`StudentsPage.tsx:189-220`) or manually (`StudentEditor.tsx:253-259`). Since the coordinator must be on StudentsPage to open the placement panel, promotion has already happened by the time the panel renders. **realGap: NONE on either form.** **Empirical proof of the invariant:** I tried to falsify it by deleting `cvUpdatedUrl` from cell 47's seed — the cell still reported the student as CV-bearing, because the auto-promotion restored it from the mandatory `cv_file_path`. A CV-less student cannot be forced into existence, let alone occur naturally. **Fixes:** both cells now seed `cvUrl` + `cvUpdatedUrl`; cell 46's header records that its path-1 checkbox is disabled for lack of a FREE PLACE (its org has 0 slots, exercising the mint path), not for lack of a CV. **New assertion `bothPathsOffered` in cell 47** — with a CV on file and a free place, the coordinator must genuinely get BOTH routes: the "שלח קו״ח" checkbox **enabled** AND the "כבר במגעים" direct-placement button (Yariv's design: *"המערכת שואלת אם לשלוח קורות חיים או שהסטודנט כבר במגעים מתקדמים… וצריך רק אישור"*). This is behaviour the old no-CV seed never exercised — it always had path 1 greyed out, so the cell was silently testing a one-path world. **Proven RED→GREEN against a real code regression** (not an impossible data state): temporarily forcing `canSend = false` in PlacementPanel → `bothPathsOffered=false` → RED; restored → GREEN. **No change to shipped v1.28.0 app code — this is a test-realism fix only.** **Gate (4th full run): 46/48 — cells 46 (8.3s) + 47 (19.4s) both GREEN in-context, and cell 39 came back GREEN (confirming last run's red was a flake). Reds: the pre-existing `01 REG-onscreen-zoom`, plus `03-admin-review` (`REVIEW-persist: notes after reload … got ""`) — CONFIRMED FLAKE, passes 3/3 standalone across 2 runs.** **SYSTEMIC OBSERVATION worth acting on:** the gate now has a rotating cast of contention flakes — 03, 05, 17, 28, 31, 39, and 46 before I hardened it — and they share ONE root cause: a **fixed-timeout read-back after a write**. Under peak load a `saveSnapshot` CAS retries with backoff past the assert window, so a slow-but-successful write reads as a lost one. The proven remedy is the one now in cells 46/47: gate the read-back on a definite **UI completion signal** first, then poll the DB with a longer settle. Applying that pattern across the flaky cells would make the gate's "red" mean something again — right now a red has to be hand-triaged every run, which is exactly how a real regression gets waved through as "probably flaky." Chip spawned for this (supersedes the narrower cell-39-only chip).
- 2026-07-20 — ❗ **CORRECTION to the entry above + I LEAKED TEST DATA INTO PRODUCTION (now removed). New `scripts/health-check.mjs` audits the REAL data; prod is 0-FAIL.** **(1) My "cell 46 is a confirmed flake (slow CAS write under load)" call was WRONG.** The vacancy-slot history proves it: a `placed-direct` action landed at `2026-07-19T22:11:50Z` by actor "יריב בדיקה" on a LEFTOVER PREVIEW student of mine (`zprev-stu-1784498692312` / "סטודנט תצוגה מגעים") — the exact second gate-run-1's cell 46 was executing (started 22:11:44) and reporting `approved=true` with **its own** student unplaced. The cell acted on the WRONG ROW; that fully explains `approved=true, acceptedOrg=false` with no slow-write story needed. I diagnosed flakiness from a plausible mechanism instead of from the evidence, and the evidence was one query away (slot `history[]` records actor+reason+timestamp — **use it before calling anything flaky**). **(2) The enabling condition was my own pollution:** a preview student + private employer I seeded at 22:05 for a screenshot survived its cleanup (a stale whole-blob write from a still-open tab resurrects deleted rows — the plain-blob last-writer-wins path), then sat in the **real תשפ״ו HR cohort marked `placed` with `acceptedOrg`**. **Removed with a CAS loop (blob v2027); verified gone.** Lesson: never seed preview/demo rows into the production blob for a screenshot — use a gate cell (which cleans up under CAS) or local fixtures. **(3) Guard added to cells 46+47:** before clicking, the open editor must show the SUBJECT student AND exactly ONE `[data-place-direct]` may exist on the page; otherwise the cell logs and refuses (fail-closed) instead of silently placing a stranger. Both cells re-run GREEN with the guard. **(4) NEW TOOL `scripts/health-check.mjs`** — read-only integrity audit of the LIVE blob (`node scripts/health-check.mjs [--course <id>] [--json]`). The gate proves the CODE on fixtures; this proves the DATA under the rules that code enforces — not the same thing. Checks CV / PREFS / SLOTS / PLACED / ORPHAN / SCOPE / CAPACITY / PRIVATE / TESTDATA, and prints demand-vs-places per course. **Current prod: 0 FAIL · 3 WARN · 69 INFO.** Severity was corrected after Yariv's input — **"the CV through the system only started with the 11 students in תשפ״ז"** — so a roster student with no CV who has not submitted is INFO (normal pre-submission), and the תשפ״ו cohorts placed under the legacy `acceptedOrg`-string flow are INFO too; only a student genuinely IN the pipeline without a CV is a FAIL (there are none). **(5) REAL-DATA STATE.** *פרקטיקום משאבי אנוש תשפ״ז (the live cohort, 11 students):* **11/11 registered with a CV**, but **0/11 have completed stage 2** (no `cvUpdatedUrl`), **0/11 have preferences**, and **21 places are open**. The system is built and idle — waiting on students, not on code. Until stage 2 lands, path-1 "שלח קו״ח" is correctly disabled for all 11 (`אין קו״ח מעודכן`). *⚠ פרקטיקום יעוץ ארגוני תשפ״ז:* **15 students and ZERO places** — its 5 assigned employers (Mityaalim, Mityaalim - סניף אקדמי, אוסם נסטלה ×2, א.ד סינון) each hold a single place allocated to **תשפ״ו**, not תשפ״ז, so that cohort would see zero availability everywhere. Needs per-course-year capacity before the year starts — Yariv's decision, deferred: **"אני צריך קודם לראות שפרקטיקום משאבי אנוש עובד."**
- 2026-07-20 — 🚨 **REAL PRODUCTION BUG FOUND + FIXED WHILE VERIFYING: the service worker cached Cloudflare's 404 HTML under a JS chunk URL and rendered /cv-update permanently BLANK. v1.28.1 (stage-2 course scoping) + v1.28.2 (SW fix) both live.** **(A) v1.28.1 — stage-2 org list scoped to the student's own course.** The acceptance email links to `/cv-update/?email=&name=` with **no `?course=`** (`notify-acceptance/index.ts:59`), but the form scoped ONLY by that URL param (`CvUpdateForm.tsx:56`), so the filter was skipped entirely and a תשפ״ז HR student was offered orgs from other programmes — on the very screen where preferences are chosen. Same leak class Yariv reported for `/organizations` (fixed v1.26.4); this form was never brought over. Now resolves the course from `?email=` (students → then **candidates**, since the acceptance mail can land before the coordinator converts a candidate and conversion copies email+courseId, `CandidatesPage.tsx:296-308`) → **every link already sent is repaired, nothing to reissue**. Also adopts the same GREEN-and-has-a-free-place rule as `/organizations`, so the two student surfaces can't disagree: real data went from ~16 orgs in the form (incl. counseling's אוסם נסטלה) to **exactly the same 10** both places. Fail-closed with a visible reason. New cell **`48-cvupdate-course-scope`** (scope-by-email + fail-closed), proven RED→GREEN. **Cell 06 was a genuine casualty** — it drove the form with a synthetic email matching no student, which now correctly offers nothing; reseeded with a REAL student (same lesson as 46/47). Gate 48/49. **(B) v1.28.2 — the SW bug, found only because I verified on production.** After deploying v1.28.1 the `/cv-update` island went **blank**: `Failed to fetch dynamically imported module`. Root cause is NOT the page: **Cloudflare Pages answers a missing file with its 404 page as HTTP 200 + HTML body**, and `sw.js` cached on `res.ok` alone — so during the seconds a deploy propagates, a not-yet-uploaded chunk returned 200+HTML and the SW stored that HTML **under the `.js` chunk URL**. Cache-first then served HTML for a module import on every later load, **permanently** (the entry only vanishes when the next version bump renames the cache). Proved it by reading the live cache: `/_astro/CvUpdateForm.CREmziJu.js` held `content-type: text/html`, body `<!DOCTYPE html>`, while the same URL over the network returned valid JS. **Fixes in `public/sw.js`:** never store an HTML body under an asset URL (`isAsset` + `isHtml` — `res.ok` is NOT evidence a file exists on Pages); **SELF-HEAL** — a cache hit that is HTML under an asset URL is deleted and re-fetched so an already-broken client repairs itself instead of staying blank until the next deploy; navigations cached only when `res.ok`; and stop caching the `/?_v=<timestamp>` freshness probes, of which the live cache had accumulated **hundreds in minutes** (unbounded growth). **(C) Residual + honest scope.** After v1.28.2 the SW cache is clean, yet the custom domain still rendered blank **in my audit browser only**: the poisoned entry also lives in the **browser's own HTTP cache**, and `_astro` assets are immutable-cached so reloads won't shake it. Proved the code is fine: `import('…CvUpdateForm.CREmziJu.js?cb=…')` succeeds while the bare URL fails — same file, different URL. It clears when that chunk's hash next changes. **Blast radius is only a browser that loaded /cv-update during the ~1-minute v1.28.1 window — i.e. essentially just mine, since 0/11 students have opened stage 2.** **VERIFIED CLEAN on the deploy origin `055bd48d.practicum-v2.pages.dev` (separate cache, identical build) with a REAL student link:** email prefilled + read-only, and **exactly 10 orgs — the same 10 as `/organizations`**, counseling org gone. **LESSON:** verify a deploy only AFTER confirming propagation (HTML chunk ref + that chunk returning `application/javascript`), and treat a post-deploy blank page as a CACHE question before a code question — `caches.keys()` → match the asset URL → check its `content-type`.
- 2026-07-20 — ✅ **3-REQUEST INTENT MODEL SHIPPED — v1.29.0+build.84.47a3f30e, deploy `https://8641e8fd.practicum-v2.pages.dev` (live).** Yariv: *"אפשר לבקש שלושה כאשר הרכזת תווסת… אם היא שלחה יותר מדי למקום כלשהו היא לא תוכל לשלוח לאותו מקום אם עברה את המכסה אבל ניתן לבקש."* **Why it was needed:** the public page still ran the OLD model — ONE request at a time, each flipping a slot to `tentative`, a full org refused outright — while the coordinator side moved to the CV-sending model in v1.27.0 (preferences reserve nothing, a full org may still be preferred, the place is taken at SEND). Two models on one dataset. **`studentRequestHold` → `studentSetRequests(data, email, employerId, 'add'|'remove')`:** up to `MAX_STUDENT_REQUESTS` (3), toggleable/removable; **touches NO vacancy** (the ledger is byte-identical after three requests) — this dissolves the capacity trap the old model created (one hold per request = 11 students × 3 = **33 places demanded against 21 real ones**); a **FULL org is requestable**; a pending employer answer no longer blocks (Yariv's choice); a **self-suggested org keeps rank #1** and is not toggleable, so that student adds 2 more (Yariv's choice); still blocked once placed. Writes the ordered choice fields the coordinator's bridge already reads — added `Student.thirdChoiceOrg` + one `push()` in `StudentEditor.buildPlacements`, no other plumbing. **UI:** multi-select with "✓ ביקשת · הסר/י", a "בחרת N מתוך M" counter, cap disabling, a full-org note, and copy replacing *"שומרת אותו עבורך זמנית… ארגון אחד בכל פעם"*. **Gate: new cell `49-student-three-requests` (6/6)** runs the REAL library through esbuild — **proven RED→GREEN by injecting the old reserve-on-request behaviour, which turned 3 free places into 0** (my first falsification attempt via `perl` silently did nothing and the cell stayed green; I did NOT count that as proof and redid it with a precise edit — an unverified falsification proves nothing). **Cell 43 RE-POINTED, not deleted:** its race over the last place cannot occur at request time any more, so it now proves the mutator still prevents a **LOST UPDATE** — both students succeed, **nobody holds the place**, and neither concurrent write erases the other's list. Full gate **48/49**, only the pre-existing `01 REG-onscreen-zoom`. **VERIFIED LIVE on the deploy origin with a seeded student:** clicked three orgs → stored `[פסגות, BDO, TLVtech]`, counter "בחרת 3 מתוך 3", 4th button disabled, message *"בקשה אינה תופסת מקום"* — and in the DB **0 slots held, free places 21 → 21 unchanged**. Test student removed; health-check **0 FAIL**. **Migration: none needed** — 0 places were in-process across all four courses.
- 2026-07-20 — ✅ **TWO LOST FEEDBACKS DIAGNOSED + ROOT CAUSE FIXED, and autosave made a BINDING GROUND RULE — v1.30.0+build.85.a6ded471, deploy `https://4d648d9f.practicum-v2.pages.dev` (live).** Yariv reported: נעמה ביטרמן's supervisor filled the feedback but he can't see it; שיראל קורן's "started but could not send — she could not fill the open-ended questions." **FORENSICS:** neither was ever submitted — each has exactly ONE history event (`נוצר קישור משוב מעסיק` by רחל שלו, 09/07 07:47 and 08:20) and **no `משוב מעסיק התקבל`**; meanwhile **16 students DO have feedback saved** (one as recent as today), so the pipeline was healthy and these two failed for a specific reason. **ROOT CAUSE:** `EmployerFeedback` was the **only** public form without `noValidate` (CvUpdateForm, CloudSignIn, PasswordGate all have it). The native `required` on the 0–100 score let Chrome block submit with an **English** tooltip anchored to a field at y=2439 while the button is at y=**3372** — and the three open-ended questions sit at y=2802/2949/3095, i.e. **between them**. On a phone that message is off-screen: pressing "שלח משוב" appears to do **nothing**. Worse, HTML5 validation pre-empts the submit event, so the app's own Hebrew check (`alert('אנא הזן ציון…')`) was **unreachable dead code that had never once run**. That is exactly Shirel's supervisor's report — she stood at the open-ended questions (which are entirely OPTIONAL), pressed send, nothing happened, and blamed them; the blocker was a screen above her. **NOT RECOVERABLE:** what either supervisor typed. `EmployerFeedback` had no localStorage/draft of any kind — it lived only in React memory, and the submit never reached the server. Told Yariv honestly, with the one long-shot avenue (browser session-restore on their own device). **FIXES SHIPPED (v1.30.0):** (1) `noValidate` + our own validation showing a **Hebrew reason at the field**, scrolled into view and focused; `required` kept for screen readers. (2) **NEW BINDING GROUND RULE** ([[feedback-autosave-open-ended]], saved to memory): *"the recorded data should be kept automatically while typed… should be placed on all open ended"* → new `src/lib/useFormDraft.ts` — debounced localStorage persistence keyed per person, restore-once-BEFORE-first-write (so an empty mount can't clobber a draft), version-gated so a changed form never restores mismatched answers, cleared **only on successful submit** (a failed save must never cost them their work), with a visible "נשמר אוטומטית" reassurance. Applied to **EmployerFeedback + CvUpdateForm + RegistrationForm**. The CV *file* can't be serialised and must always be re-attached. (3) **"זה לא אני"** — was shown to everyone including someone arriving on their own personal `?email=` link, where it is noise. The deeper issue: a URL-supplied identity was also **persisted**, which is what planted one student's identity on a shared device so the next person on a bare link was greeted as them and could act on their record. Now a URL identity is **never persisted** (the link carries it every time) and the escape appears only for a **remembered** identity. Removes the window instead of offering an exit. **Gate 50/50 run, only the pre-existing `01 REG-onscreen-zoom` red.** New cell **`50-draft-and-submit-blocker`** proven RED→GREEN by restoring browser validation (`hebrewError=false`). **Verified live on production** (v1.30.0): `noValidate=true`, autosave notice present, and pressing send with an empty score now shows the Hebrew reason and stays on the form. Browser-verified both identity paths: personal link → no button, **nothing persisted**; typed → button shown, persisted, forget-me clears it. **ACTION FOR YARIV: both feedback links still work** (`fb-16-p9zbc0`, `fb-13-z89voy`) — the supervisors can reopen them, fill the 0–100 score + recommendation, and send; anything they retype is now saved as they go.
- 2026-07-21 — ✅ **הדר עוזירי's test exposed 3 bugs — all fixed + shipped v1.30.1+build.86.49aed75d, deploy `https://056f9ba2.practicum-v2.pages.dev` (live).** She chose 3 orgs; Yariv saw only 2, plus asked to (a) highlight the submitted CV that replaces the old, and (b) verify the NEW CV is what's sent to orgs. **Her data (verified):** requests נישה/עיריית/שיבא, a cv_updates submission שיבא/נישה/עיריית (13:11), cvUrl (original) + cvUpdatedUrl (today) — BOTH `storage://`. **(1) THIRD CHOICE INVISIBLE + DROPPED.** The 3-request model (v1.29.0) writes firstChoiceOrg/second/thirdChoiceOrg, but `StudentEditor`'s `form` was initialised with first+second ONLY — `thirdChoiceOrg` never loaded — so the editor showed 2 AND `buildPlacements` (reads `form.thirdChoiceOrg`) silently dropped the third from the employer-dispatch list. Data was safe (save merges `{...student,...form}`, so the unloaded key survived), but שיבא was invisible + unbuilt. Fix: load `thirdChoiceOrg`+`thirdChoiceResult` into the form; render a "בחירה שלישית" field + result; dedup the org picker across all three; add a "בחירה 3" column to the Reports export. **NOTE for Yariv:** two preference SOURCES differ in order — her /organizations requests (נישה/עיריית/שיבא) vs her /cv-update submission (שיבא/נישה/עיריית). Both now show 3, but in different orders; which is authoritative is a product decision I did NOT make — flagging it. **(2) CV SENT AS A DEAD LINK (Yariv's item ⓑ, deeper than expected).** `cvUpdatedUrl`/`cvUrl` are stored as `storage://candidate-uploads/…` — ALL 11 תשפ״ז CVs, 0 https. `FileField` resolved that for the editor, but `PlacementPanel` used the RAW `storage://` path for the "open" button, copy, AND the link sent to the employer → an unopenable link. New `src/lib/cvUrl.ts` (`resolveCvUrl`/`viewableCvUrl`) converts to the public URL (bucket IS public — verified 200); PlacementPanel resolves before open/copy/send. The UPDATED CV was already preferred (`cvUpdatedUrl || cvUrl`) and now actually opens. **(3) "REPLACES OLD" highlight (item ⓐ).** When an updated CV exists it replaces the original — now green/bold "זהו הקו״ח שיישלח למעסיק" + strike-through note on the original, in both the editor dispatch box and PlacementPanel. **Gate 52/52 run, only the pre-existing `01 REG-onscreen-zoom`.** New cells RED→GREEN: `51-third-choice-visible` (bug → built 2, hasThird=false; fix → 3 built) and `52-cv-link-resolves` (raw → HTTP 0 on a REAL student CV; fix → 200, prefers updated). **Verified on הדר's real record (localhost DOM + prod deploy origin):** all 3 choices shown (נישה/עיריית/שיבא), third field present, CV-replaces-old highlight present. **GOTCHA logged:** the dev app's SERVICE WORKER cached stale JS on localhost during verification — a StudentEditor edit showed old text until I unregistered the SW + cleared caches. The gate's fresh Playwright contexts are immune (cell 51 saw the fix), but a long-lived preview tab is NOT — clear the SW when a localhost change doesn't appear.
- 2026-07-21 — ✅ **RE-SUBMISSION bug fixed (Yariv: "לבדוק מה קורה כשמחליפים/מעדכנים את קורות החיים ו/או הארגונים") — v1.30.2+build.87.4c511785, deploy `https://4c511785.practicum-v2.pages.dev` (live).** **Tested it empirically and found a real bug:** once a student had an updated CV, a RE-SUBMISSION was silently ignored. The editor's pending-CV detection skipped entirely (`if (!email || student.cvUpdatedUrl) return`) and the StudentsPage auto-promote only runs for students WITHOUT one. **Proof (seeded live):** student with a FIRST CV + a newer SECOND cv_updates row → cvUpdatedUrl stayed FIRST, pending panel never showed, only the top block (org prefs) reflected the new submission. So the coordinator would keep sending the OLD CV to the employer, with no signal a corrected one existed — while the NEW org preferences DID show (split-brain). **Fix:** detect ANY not-yet-applied submission even after a CV exists, gated on the file DIFFERING from the current CV (so the auto-promoted first submission never re-nags — important because anon can't write `cv_updates.seen_at` under RLS, so "seen" can't be relied on; the file-match guard is what stops the loop). Panel now reads "הגשה חדשה יותר ממתינה" + a bold warning that applying REPLACES the current CV; "אמץ כ‑CV מעודכן" sets cvUpdatedUrl to the new file. **Verified end-to-end on a seeded student:** apply → save → cvUpdatedUrl = SECOND (old replaced), reopen → no re-nag, dispatch box shows the updated CV. **Gate: new cell `53-cv-resubmission` RED→GREEN** (old skip → panel hidden, CV stays FIRST; fix → shown, applied to SECOND, no re-nag). Full gate: only the pre-existing `01 REG-onscreen-zoom`; `16-final-placement` flaked once under contention (2/2 standalone — the CV-string change touches NO slot logic). **STILL OPEN — 3 product decisions for Yariv, flagged not silently changed:** (1) **two preference SOURCES** — a student's /organizations requests (firstChoiceOrg/second/third) vs their /cv-update `org_pref_1/2/3` — can DIFFER in order (הדר: נישה/עיריית/שיבא vs שיבא/נישה/עיריית), and `buildPlacements` MERGES both (deduped) rather than treating the latest as authoritative; (2) applying a re-submission updates the CV but NOT firstChoiceOrg — the new org prefs live only in the top block + the build; (3) which source wins when they conflict. These need a "which is the source of truth for org preferences" ruling before I unify them.
- 2026-07-21 — ✅ **Yariv's RULING applied: a re-submission REPLACES old CV + orgs, history kept + revealed on demand — v1.30.3+build.88.cf55052e (live).** Yariv (verbatim intent): *"לא בטוח שהבנתי מדוע צריך שבקשה חדשה לא תחליף את הישנה … בקשה חדשה צריכה להופיע כאשר לחצן היסטוריה חושף מתי הוזנו בקשות קודמות וכמובן שקורות חיים חדשים צריכים להחליף ישנים וגם כאן יכולה להישמר היסטוריה שחושפים רק כשמבקשים … על ידי לחיצה על היסטוריה."* So: newest is current, replace not merge, history preserved and shown only on a History click. **Built on v1.30.2** (which first surfaced re-submissions): `applyPendingCv` now adopts the WHOLE latest submission — it also overwrites `firstChoiceOrg`/`second`/`third` from the submission's `org_pref_1/2/3` (only ranks it specifies; a CV-only re-upload leaves prefs untouched). Apply button reads **"אמץ הגשה (קו״ח + העדפות)"** when the submission carries orgs, else "אמץ כ‑CV מעודכן". **Nothing lost:** every submission stays in `cv_updates`; the top block shows the latest as current (with change-tags vs previous), and the **"היסטוריית הגשות קודמות (N)"** button reveals previous submissions with timestamps + org prefs — NOW also with a **"קו״ח ↗"** link to open each previous CV file (`cv_file_path` added to the history query, resolved via `viewableCvUrl`). **Gate: cell 53 extended to assert ORG adoption** (`firstChoiceOrg` 'ארגון ראשון'→'ארגון חדש' after apply) — proven RED→GREEN (revert the org-adoption spread → `orgAdopted=false` while CV still replaces). Full gate **53/53 run, only the pre-existing `01 REG-onscreen-zoom`**. **Browser-verified:** the History button reveals the previous submission's orgs + a working previous-CV open link; apply replaces both CV and firstChoiceOrg; no re-nag. **STILL FLAGGED (one writer-conflict remains):** the /organizations request path writes `firstChoiceOrg` DIRECTLY (not via cv_updates), so a student who uses BOTH /organizations AND /cv-update still has two writers of the same fields. "Latest submission wins on apply" now holds for the /cv-update path per the ruling; unifying the two surfaces into one ordered source is the remaining open item.
- 2026-07-21 — ✅ **ONE LINK + PARTIAL UPDATES shipped — v1.31.0+build.89.953898c0 (live).** Yariv: *"מה קורה שאני רוצה לעדכן רק קורות חיים או רק ארגון אחד. אני רוצה שהכל ישב תחת אותו קישור שכבר יש להם."* (After I showed him the "second place" = the /organizations "בקש/י מקום" buttons, which the acceptance email only calls "לצפייה מראש" while /cv-update is the action link.) **Design approved before build** (his standing rule): /cv-update becomes the single place to manage CV + orgs, returning student can change just one thing, coordinator still reviews+adopts (his choice). **Built:** (1) **Pre-fill** — opening the link fills the 3 org pickers with the student's current choices + a "כבר הגשת… אפשר לעדכן רק את הקו״ח, רק ארגון, או שניהם" banner (never clobbers a restored draft/typing). (2) **CV optional on return** — a student who already has a CV can submit without a file (label→"אופציונלי", button→"שלח/י עדכון"); a first-timer still must upload; empty submit (no CV + no org) blocked. (3) **Org-only update** — with no new file the cv_updates row REUSES the existing CV path (NOT-NULL column) + carries the updated orgs, so the CV is kept and only orgs change. (4) **Coordinator detection** now fires when the CV file OR the org prefs differ from the record (an org-only update reuses the CV path, so a file-only guard would hide it), and stops once adopted — which carries the guard since anon can't write cv_updates.seen_at (RLS). **Verified live end-to-end:** returning student saw pre-filled orgs + optional CV; changed the 3rd org, no file → submit succeeded → cv_updates row reused the CV path + recorded the new org (1st/2nd preserved); coordinator editor showed the pending panel ("אמץ הגשה (קו״ח + העדפות)") for the org-only change. **Gate: new cell `54-cvupdate-partial` RED→GREEN** (force CV-required → no-file submit errors "יש לצרף קובץ"). Cell 06 re-seeded WITHOUT a CV so its CV-nofile-required cell tests the first-time path (with-CV = cell 54). Full gate 54/54 run — only pre-existing `01 REG-onscreen-zoom`; `16-final-placement` flaked once (passes standalone, placement logic untouched). Headless note: driving the custom OrgPicker to CHANGE a specific org is flaky, so cell 54 asserts the no-file/reuse-CV/orgs-recorded core (the org-CHANGE is verified manually). **OPEN — Phase 2 (asked, awaiting Yariv):** make /organizations the read-only preview the email already calls it (remove the duplicate "בקש/י מקום" request buttons) so there is exactly ONE choosing surface. The 3-request LOGIC (up to 3, intent-only, replace+history) already lives in /cv-update; Phase 2 is removing the second writer + migrating cells 42/43/49.
- 2026-07-21 — ✅ **ONE choosing place — /organizations made read-only, /cv-update is THE link — v1.32.0+build.90.11a486c4 (live).** Yariv (after I showed him screenshots of the two surfaces): *"צריך שיהיה מקום אחד בו בוחרים ארגונים ומעלים טופס מעודכן של קורות חיים… זה טופס הקבלה לפרקטיקום בו יש קישור. אם בהמשך הסטודנט נכנס לאותו קישור ומשנה… הבקשות הקודמות נשמרות בהיסטוריה."* **Confirmed the two "places":** (1) /cv-update — the FORM with org-preference dropdowns (the acceptance-email action link); (2) /organizations — a LIST of cards with "בקש/י מקום" buttons (the request feature I built earlier). Both wrote firstChoiceOrg/second/third. **Consolidated to /cv-update** (Phase 1 v1.31.0 already made it the full CV+orgs+partial-updates place). **This deploy (Phase 2):** /organizations is now the **read-only preview** the email already calls it — removed the request buttons, counter, toggleRequest, request state; KEPT identify+remember-email, course-scoped list, descriptions, availability badges, placement-status banner. Copy points students to their personal link for choosing/updating. `studentSetRequests` etc. stay exported (cell 49 still tests them; tree-shaken now). **Verified live:** הדר's /organizations shows 10 orgs, **0 request buttons**, greeting present, "דרך הקישור האישי שקיבלת". **Coordinator override CONFIRMED working:** signed in as **רחל שלו** (not Yariv), changed a student's first choice נישה→Manpower, saved, persisted (2nd/3rd intact) — so both Yariv AND Rachel can directly change any student's choice via the editor dropdowns. **Cells:** retired `43-hold-race` (its race — two students requesting the same place on /organizations — no longer exists; SaveMutator guard stays in saveSnapshot for other callers). `39-status-lifecycle-e2e` E2E-selectable REPOINTED from "has a בקש/י מקום button" → "renders as an org card (data-org-name)" — this was the REAL cause of 39's intermittent red (I nearly dismissed it as a flake; investigating found it was the removed button). Full gate **53/53 run, only pre-existing 01**. **LESSON RE-LEARNED:** an earlier gate run went 38-cells-red — corrupted Vite dev cache from `npm run build` while the dev server was up; `rm -rf node_modules/.vite` + clean restart recovered all (skill_gate_no_concurrent_build). **STILL OPEN (offered):** coordinator-edit history logging (a coordinator's direct edit currently leaves no history entry — I'll add "who changed what" next).
- 2026-07-21 — ✅ **Coordinator-edit audit trail — v1.32.1+build.91.6a6bab03 (live).** Completes the coordinator-override requirement (Yariv: coordinators can change any student's choice). A coordinator's direct save previously left NO history entry — student SUBMISSIONS are logged in cv_updates, but coordinator EDITS were invisible. `StudentsPage.handleSave` now diffs the tracked fields (first/second/third choice, cvUpdatedUrl, submissionStatus, acceptedOrg, feedbackText) vs the previous record and passes an activity to saveSnapshot → history logs e.g. **"עריכת רכז: בחירה ראשונה" with `who` = the signed-in coordinator** (או "עריכת פרטי סטודנט/ית" / "יצירת סטודנט/ית"). `persistAndRefresh` gained an optional activity param. Existing "שובץ לארגון" placement log unchanged. **Gate: new cell `55-coordinator-edit-log`** drives the editor as a coordinator, changes the first choice, saves, asserts a history entry naming who+what — **proven RED→GREEN** (revert the activity → edit persists but historyEntry=NONE). Full gate **54/54 run, only pre-existing 01**. Harness note: `Escape` closed the whole modal before Save — used `input.press('Tab')` to blur+commit instead. Health-check 0 FAIL; no test leftovers.
- 2026-07-21 — ✅ **Student-facing submission history on /cv-update — v1.32.2+build.92.2ed9c611 (live).** Yariv: "student history was also requested" (after the coordinator-edit trail v1.32.1). /cv-update now fetches the student's cv_updates rows by email and shows a **"היסטוריית ההגשות שלי (N)"** toggle — read-only, revealed on demand: each past submission's date (latest tagged "אחרונה"), its org preferences, and a **"קו״ח ↗"** link to open that submission's CV (viewableCvUrl). The submit flow is unchanged. So BOTH sides now have history: the coordinator (editor "היסטוריית הגשות קודמות" + previous-CV links, v1.30.3) AND the student (their own, here). **Gate: new cell `56-student-history`** (seed 2 submissions → toggle shows "(2)" → reveals earlier+latest) proven RED→GREEN (force section off → toggleShown=false). Full gate **55/55 run, only pre-existing 01**. Verified live in a browser: toggle "(2)", both submissions revealed, latest tagged, 2 CV links.
- 2026-07-21 — 📐 **APPROVED: comprehensive redesign of the coordinator student-editor page (spec: `docs/design/2026-07-21-student-editor-redesign.md`).** Yariv: "יש יותר מדי בלאגן בדף … עמוס מאוד שלא לצורך." Ran a design Workflow (3 directions → judge → synthesis) + showed him a mobile mockup. **Approved direction** = one-org-hub + passive cockpit: 10 stacked sections → 2 always-open working surfaces (CV strip + org hub) + 6 collapsed accordions with live header hints + sticky header/footer. The org world (submitted list + 3 ranked fields + build box + PlacementPanel) collapses into ONE ranked-org card per org, in the STUDENT'S chosen order, each carrying its own `תוצאת ראיון` (bound to org identity, fixing the out-of-order bug) + send action. CV = current only, old → history toggle. **Yariv's refinements (binding):** (1) app palette — maroon `--accent` #7a1e2b, NO blue, subtle colours; (2) send = CHECK an org (or several) → WhatsApp/Outlook, ranked-only; (3) admin can RE-RANK; (4) keep the stepper+next-action strip, subtle. **Phased build** (each: build→preview importers→gate→iPhone→ship): Phase 0 data model (`interviewResult` on StudentPreference, org-keyed, re-rankable, compat shim for legacy *ChoiceOrg/*Result) → Phase 1 org hub → Phase 2 CV strip → Phase 3 accordions → Phase 4 cockpit. **NOT STARTED — next session begins Phase 0.** Nothing built yet; spec is canonical.
- 2026-07-21 (later) — 🔧 **Phase 0 (data model) built — behaviour-preserving plumbing, NO UI change, NOT deployed.** First phase of the approved student-editor redesign. `StudentPreference` gains two optional fields: **`orgName`** (display name, survives an unresolved employer) and **`interviewResult?: 'pending'|'passed'|'failed'`** (bound to the ORG's identity, not a rank slot). Added three PURE helpers to `src/lib/placement.ts`: **`buildUnifiedOrgList(student, employers)`** → the student's orgs as ONE ordered list (rank 1..N), sourced from `preferences[]` when present else derived from the legacy `firstChoiceOrg/second/third` + `*ChoiceResult`, with each org's result matched BY NAME and employerId resolved; **`reorderUnifiedList(list, orderedOrgNames)`** → re-rank while the interviewResult/status/slot travel WITH the org (the core fix); **`applyUnifiedList(student, list)`** → writes `preferences[]` (carrying orgName+interviewResult) AND keeps the legacy `*ChoiceOrg/*Result` in sync (compat shim) so old readers (reports, PlacementPanel, /cv-update pre-fill) keep working; pure, no input mutation. **Gate: new cell `57-unified-org-list`** (esbuild-bundles the REAL lib, like cell 44) with 4 checks — order, **result-follows-org on re-rank**, legacy-derive, writeback-syncs-legacy — **proven RED→GREEN** (injected the "result bound to rank slot" bug → UNI-result-follows-org went ❌ 3/4, reverted → 4/4). `tsc --noEmit` clean on both changed files. NO consumer uses these yet, so runtime is byte-identical → **no standalone deploy** (would only burn a version); the deploy happens with Phase 1 when there's a user-visible change to validate on device. **NEXT: Phase 1 — the org hub card** (fold submitted-list + 3 ranked fields + build box + PlacementPanel into one re-rankable card type; checkbox-select → WhatsApp/Outlook send, ranked-only; suggested-org path; addExtraEmployer). Awaiting Yariv's green light to start Phase 1 given its size/risk.
- 2026-07-21 (later²) — 🏗️ **Phase 1 (the org hub) BUILT + gate-green — NOT yet deployed** (redesign continues; all phases ship together per Yariv). New **`src/components/OrgHub.tsx`** folds the FOUR old org fragments (submitted-prefs list + 3 בחירה fields + build box + PlacementPanel) into ONE re-rankable **ranked-org card per org**: rank badge `#N` + ▲▼ re-rank, editable org combobox (tentative only — locked once sent, so no capacity-leak), per-card **`תוצאת ראיון`** segmented (BOUND TO THE ORG — survives re-rank), capacity chip + status pill, **checkbox-select → send bar → WhatsApp/Outlook bottom-sheet** (ranked-only, takes a place only on send), suggested-org **`כבר במגעים — אשר שיבוץ`** (place-direct), `✕ הסר` release, `➕ הוסף ארגון`. **The explicit "build" step is GONE** — `buildUnifiedOrgList` now returns the **union** of `preferences[]` ∪ legacy choices, so a chosen org is a card at once. Maroon palette, **zero blue** (the old #1d4ed8/#2563eb replaced). Two save models preserved: identity/rank/result = form-local (onFormChange, saved on שמור); send/נקלט/נדחה/release/place-direct = immediate onDataChange (+ the StudentEditor wrapper syncs form incl legacy fields, so the v1.32.1 coordinator-edit log still fires). `StudentEditor` embeds OrgHub for practicum; **non-practicum keeps the simple 3 fields** (no regression). Deleted the now-orphaned `PlacementPanel.tsx`. **Gate:** new **`58-orghub-cards`** proves through the real UI — union (הדר's 3rd choice no longer dropped), **re-rank-keeps-result** (through Save + legacy sync = the Phase-0 fix end-to-end), add-org, send-takes-a-place — 4/4; `57-unified-org-list` +UNI-union check; updated `09/14/16/47/55` to the new UI (all green); deleted `13`/`51` (coverage absorbed by 44+58). Fixed a real bug found mid-build (add-org "הוסף" read stale state). **Full gate 54/55 — only the pre-existing `REG-onscreen-zoom` red.** Verified live in the preview (3 cards #1/#2/#3, maroon, no blue). NEXT: Phase 2 (CV strip) → 3 (accordions) → 4 (cockpit) → final gate + ONE deploy for Yariv's e2e test.
- 2026-07-21 (later³) — 🛡️ **Phase 1 hardening — 9 adversarial-review findings fixed** (4-dimension review workflow: capacity-leak/save-model/correctness/palette, each finding independently verified). Fixed: **(1)** `dispatchMany` now resolves the checked orgs against the CURRENT cards (not the stale `selected` name set) so a renamed/removed card can't reserve a slot no preference owns; **(2)** added a **per-employer dedup** (two card names fuzzy-resolving to one employer no longer double-book it); **(3)** **open-then-commit** — a blocked 2nd+ mobile popup is skipped, not phantom-dispatched (no reserved place / logged dispatch for a CV never sent); **(4)** `renameOrg` now has the duplicate-name guard `addOrg` had (can't rename onto a sent card's name and free its slot); **(5)** `OrgCombo` resets instead of committing an empty rename (was silently dropping the card); **(6)** `buildUnifiedOrgList` dedups legacy vs prefs by **resolved employer id** too (canonical "Icon Group/HQ" vs free-text "Icon Group" no longer a phantom duplicate) — `resolveEmployerIdByName` made fuzzy to match the app; **(7)** a **slot-holding preference never vanishes** (fallback name) even if its employer was deleted — no orphaned place; **(8)** the StudentEditor onDataChange wrapper now syncs `placedAt` (a later שמור won't re-stamp the placement date) + `submissionStatus` fallback; **(9)** the coordinator-edit audit now tracks the per-org `תוצאת ראיון` fields (with 'pending'≡default normalised so materialisation isn't logged as a change); `dispatchChip` given a real `--card` fallback (was transparent). **Gate:** cell 57 +UNI-dedup-by-employer +UNI-slot-survives (both proven RED→GREEN); 58/55/14/16/47 re-run green. Deferred (pre-existing app-wide, NOT Phase-1 regressions): rgba(0,0,0,x) surface fills in dark mode, `design.ts` EVENT_COLORS.slot blue, placement-action audit entries. NEXT: Phase 2 (CV strip).
- 2026-07-21 (later⁴) — 📄 **Phase 2 (CV strip) built.** Collapsed the two separate CV sections (the "📋 שיבוץ — קו״ח לשליחה" green/red status box + the "CV מעודכן" section) into ONE **"קורות חיים"** strip: current CV (cvUpdatedUrl → cvUrl → red warn) with **פתח ↗ / העתק** (resolveCvUrl/viewableCvUrl), the replaced-original note (line-through), the pending-adopt banner (`✓ אמץ` — cell 53 preserved), the suggested-org approve (cells 45/46/47 preserved), the editable cvUpdatedUrl field, and a new **"היסטוריית קו״ח (N)"** toggle revealing the replaced original + every past submission's CV file (each openable via viewableCvUrl). Maroon/green, no blue. Cells 53 (adopt flow) + 52 (CV reference) green; verified in a preview screenshot. NEXT: Phase 3 (accordions).
- 2026-07-21 (later⁵) — 🎛️ **Phases 3 + 4 (accordions + cockpit) built.** **Phase 3:** the secondary sections fold into collapsible **accordions** with live one-line hints — `פרטים ועריכה` (details+course+prep, auto-open for a new student), `ראיון שיבוץ (רחל)`, `השמה סופית ושעות` (auto-opens when acceptedOrg set), `מסמכים וחוו״ד מעסיק`, `הערות`. The two working surfaces (CV strip + org hub) stay always-open. **Phase 4 cockpit:** a **sticky header** (name · one status pill `cockpitStatus` · 📞/WhatsApp/✉ contact icons · ✕), a passive **stage stepper** (`הכנה·קו״ח·העדפות ושליחה·ראיון·שובץ·שעות`, current lit maroon, prior ✓), a derived **next-action hint** banner, and a **sticky footer** (שמור + טופס הערכה + feedback buttons + מחק + בטל; the duplicate student-contact buttons moved to the header). Maroon/subtle, no blue. **Gate caught a REAL race** (not just a flake): `16-final-placement` failed intermittently — a rapid send→נקלט materialised placement from a stale render closure, missing the just-assigned slotId (orphaned slot). Fixed with a **`formRef`** in OrgHub so placement handlers always read the latest committed form; 16 now 3/3. Cell 25 updated to expand the `מסמכים` accordion first. Final full gate running to confirm. NEXT: gate green → version bump → ONE deploy → Yariv's e2e test.
- 2026-07-21 (later⁶) — 🚀 **SHIPPED: student-editor redesign — v1.33.0+build.93.a0c7cc11, LIVE on production.** Cloudflare Pages **Production / branch main**, deployment id **`1e558fd0`** (`1e558fd0.practicum-v2.pages.dev` + `practicum.yarivitzkovich.org`). Final full gate **54/55 — only the pre-existing `REG-onscreen-zoom` red** (unrelated to the editor). The gate caught a REAL concurrency bug on the way (rapid send→נקלט could orphan a slot via a stale render closure) — fixed by resolving the placement slot from the employer's held slot (ground truth) + a formRef; 16-final-placement now green under load. **Verified live on production** (הדר עוזירי): editor opens, **3 org cards** (her 3 choices via the union — the very "third choice dropped" bug that started this is fixed), CV strip + "היסטוריית קו״ח (2)", stepper, 5 accordions, cockpit header status pill + contact icons, next-action banner, sticky footer — all maroon, no blue. All 5 phases in: 0 (data model) · 1 (org hub, +9 review fixes) · 2 (CV strip) · 3 (accordions) · 4 (cockpit). **OPEN: Yariv's end-to-end + iPhone device test** (per [[skill_deploy_validation_outcome_and_device]] — not "validated" until Yariv smoke-tests on his iPhone).
- 2026-07-22 — 🔧 **iPhone-test feedback shipped — v1.33.0+build.94.4fbf6e6e, LIVE** (Pages Production/main, deploy **`3785f9be`**). From Yariv's device test: (1) CV strip — removed the strike-through + "מקורי", header now "קו״ח מעודכן — יישלח למעסיק", replaced-note is one plain line → history; (2) **CV in ONE place** — removed the duplicate editable "קורות חיים מעודכן" field from the CV section, moved the raw path into the מסמכים accordion; (3) "הצג גם ארגונים שאינם זמינים" moved to the **bottom** of the ranked list; (4) the stage-2 "ask the student to update" link (WhatsApp/mail to student) moved **below** the ranked list + relabeled, distinct from the per-org employer send. Cells 53/58/25/09/26 green; verified live. Still OPEN: Yariv continues the e2e/device pass.
- 2026-07-22 (later) — 🔧 **Re-rank UX + banner copy — v1.33.0+build.95.aa1cb871, LIVE** (Pages Production/main, deploy `c3dee6d5`). From Yariv's light-mode test: (1) next-action banner drops the incorrect/redundant "בקטע הארגונים"; (2) the re-rank **arrows are now prominent maroon bordered chips** (were tiny gray ▲▼) shown only when >1 ranked org; (3) **each arrow click asks a confirm** (naming the swap) — an accidental click can't silently lose the order; (4) a label above the list explains "▲▼ לשינוי דירוג הארגונים — השתמש/י בחיצים (כל שינוי מבקש אישור)". Cell 58 accepts the confirm; 4/4 green; verified live.
- 2026-07-22 (later²) — 🐛 **Hospital-name truncation fixed (ביה"ח → ביה) — v1.33.0+build.96.2b453ea8, LIVE** (Pages Production/main, deploy `34b2667d`). Root cause: employer "ביה\"ח שיבא תל השומר" used a straight ASCII " where the Hebrew gershayim ״ is meant; the " terminated HTML attributes / datalist / CSV, cutting the students' free-text acceptedOrg to "ביה". **Data fixed in prod** (CAS): employer #7 + מאי כהן + מאיה אוליבנבאום → "ביה״ח שיבא תל השומר". **Code guard**: `normalizeOrgName` (" → ״) applied at every org-name entry — StudentEditor acceptedOrg, OrgHub renameOrg/addOrg, Excel import. Gate 57 +ORG-quote-normalized (RED→GREEN); 58/55 green. Also delivered the HR-תשפ"ו org-score table (xlsx) with the pairs split + light-blue preserved.
- 2026-07-22 (later³) — 📇 **Org-contact from the students list — v1.33.0+build.97.6d7bad6d, LIVE** (Pages Production/main, deploy `5abf8e96`). Yariv: "from the students page I can approach the org." Each placed student's list row now shows, next to the hosting-org name, a **WhatsApp + mail icon that reach the EMPLOYER** (distinct from the student-contact icons on the right). Employer resolved fuzzily from acceptedOrg; WhatsApp → wa.me with the employer's 972-normalised phone, mail → mailto to the employer's address. Gate: new `59-org-contact-from-list` (icons present + WhatsApp targets employer phone, verified); cell 10 (student contacts) still green. Also: gave Yariv a clickable link to the HR-תשפ"ו org-score xlsx.
- 2026-07-22 (later⁴) — 🚑 **Restored 2 accidentally-reverted students + removed the accidental-revert path — v1.33.0+build.98.92e3648a, LIVE** (deploy `06784682`). Yariv reverted שיראל קורן + שיראל אבוקסיס to candidates by mistake (HR-תשפ"ו) via the list card's one-tap "↩ מועמד". **Recovery** (CAS, no backup existed): rebuilt both students from the candidate records + my earlier org-score pull — name/phone/email/course/year/org (Manpower, קופ"ח מאוחדת) + re-occupied slots; שיראל קורן's **feedback score 90 + recommendation + date recovered** (detailed rating breakdown was unrecoverable — no separate feedback table, no blob backup). **UX fix**: removed the one-tap list "↩ מועמד" (fired with no confirmation); revert-to-candidate now lives only inside the student card behind the confirmed delete dialog's "השאר כמועמד" — which now also creates the candidate record. Cell 59 green.
- 2026-07-22 (later⁵) — 🎨 **Student-list card redesigned — v1.33.0+build.99.bd2093bd, LIVE** (deploy `2f9f6b23`). Yariv: card was cluttered + the two icon clusters confused. Redesign: removed the raw student phone/email/city text from the preview; two contact groups that sit together but are unmistakable — **ORG** (when placed) in a maroon chip (🏢 building icon + org name + **call · WhatsApp · mail**, phone ADDED for orgs like Clalit with no WA), and the **STUDENT**'s own call/WA/mail + edit. Not-placed → "טרם שובץ/ה בארגון". Design mocked + approved by Yariv (keep student icons; groups together-but-clear). Gate: 59 +org-call icon; cell 10 green.
- 2026-07-22 (later⁶) — 🧱 **Student-list card → two-zone split — v1.33.0+build.100.3a2c6f5b, LIVE** (deploy `3da1c4bf`). Per Yariv: RTL — **STUDENT zone (right)**: name · status tags (CV/שובץ/✓ משוב…) · the student's own call/WA/mail + edit; hairline divider; **ORG zone (left)**: 🏢 building icon + org name + call·WhatsApp·mail. Not-placed → "טרם שובץ/ה בארגון"; resolved-but-no-contacts → "אין פרטי קשר לארגון". Cells 59 (org, incl. call) + 10 (student) green.
- 2026-07-22 (later⁷) — 🎯 **Student-list card: contact rows aligned + icons unified — v1.33.0+build.101.e909637b, LIVE** (deploy `32996ed5`). Per Yariv: the student's and org's contact rows now sit on the SAME line (both pinned bottom via mt-auto; verified identical top + 36px size), with IDENTICAL icons in both zones — org-style outline (new PhoneIcon in icons.tsx + WhatsAppIcon/MailIcon) at the student's larger size + same maroon-outline button. Student row keeps its edit button. RowActions left intact for the Candidates/Lectures/Trainers pages. Cells 10/59/30 green.
- 2026-07-22 (later⁸) — 🎯 **Student-list divider hierarchy + edit-icon clearance — v1.33.0+build.102.f997dbd7, LIVE** (deploy `1c49dd93`). Per Yariv: the between-students separator is now PRIMARY (2px + new `--divider-strong` token, α0.42) while the student↔org split within a card is SECONDARY (1px, lighter `--divider` α0.18, inset 3px top/bottom → a short within-card hairline). The card-open (edit) icon no longer touches that hairline — container gap 12→16px gives ~30px clearance. **Trap found & documented:** a global `.border-b{border-color:var(--divider)!important}` rule pinned the row separator to the WEAK token, so the strong colour only showed once the `border-b` class was dropped and the border set inline. New gate cell **60-divider-hierarchy** asserts weights/inset/clearance AND guards that trap (RED→GREEN proven via git-stash). Gate: 01-registration = pre-existing known red; 26-feedback-resolve flaked under concurrent CAS load but passes standalone (2/2); cell 60 green in-gate.
- 2026-07-22 (later⁹) — 🎯 **Open-card control → bare pencil in the tag row — v1.33.0+build.103.dffc89df, LIVE** (deploy `80e684e4`). Yariv: the filled edit capsule still read as crossing the student↔org hairline. Fix per his direction: pulled the edit OUT of the bottom contact row (→ that row is now exactly call/WhatsApp/mail, mirroring the org zone) and placed a BARE pencil (transparent, no border, subtle hover-bg only) in the empty left area of the tag block, vertically centred with the tag pills (pencil-vs-tags centre delta = 0px) and 16px clear of the hairline — never crosses. title kept exactly "ערוך" so the ~8 gate cells that find it by title still work. **4-agent adversarial review workflow** cleared interaction-regression + selector-breakage and caught an initial `items-start` ~5px sink → fixed with `items-center`. Cell 60 updated (now also asserts bare + in-tag-block + not-in-contact-row) with RED→GREEN re-proven. Gate green except pre-existing known 01-registration; 26 passed this run (its earlier red was a load flake). Verified light/long-name/dark.

- 2026-07-27 — 🔴→✅ **"תזכורות המשוב לא נשלחו" alert = FALSE POSITIVE (my cleanup) — integrity guard fixed + edge function deployed.** Yariv got the ⚠️ data-integrity alert; only one employer is genuinely pending (קופת חולים מאוחדת / שיראל אבוקסיס). **Root cause:** the guard in `supabase/functions/feedback-reminders/index.ts` compares the current feedback count against the last 25 `practicum_snapshots`; the 2026-07-23 cleanup deleted this session's audit-seed students (`zep-*`, `zep2-*`) which had been seeded WITH `feedbackText`, so curFb 20 < recent max 24 → abort + alert. No real data lost (all 20 genuine feedbacks intact). 15/25 snapshots were tainted → it would have kept aborting for days, INCLUDING the next Sunday run; and the alert's "re-run manually" advice was a dead end (a re-run re-trips the same guard). **Fix:** (1) the feedback comparison is now INTERSECTED with the current roster — a snapshot's feedback counts only if that student id still exists, so deliberate deletions stop looking like corruption while real clobbering (a student still on the roster losing feedback) still aborts; validated against prod data: today PASSES, one-student-wipe/all-wipe/50%-roster-wipe all still ABORT. (2) Added `?force=1` escape hatch + documented it in the abort JSON and the alert email. **Deployed** (`supabase functions deploy feedback-reminders`) and verified with `?dry=1` on prod: `ok:true`, remind=1 → שיראל אבוקסיס · קופת חולים מאוחדת · חן עובד `hen.o@meuhedet.co.il`, CC Yarivi@ariel.ac.il + rachelshal@ariel.ac.il, missing=0. **OPEN:** this week's send was skipped — it will go out automatically next Sunday 05:00 UTC; Yariv can trigger it now instead (asked). No real email sent by me (dry-run only).

- 2026-07-27 (later) — ✅ **Candidacy form + interview-slot mechanism VERIFIED ahead of new interview dates; gate now 58/58 (first fully-green gate).** Yariv is about to publish new interview dates and asked for a check. **Result — mechanism healthy:** REG-validation ✅ (invalid submit blocked, no DB row), REG-happy-path ✅ (submit → exactly 1 `candidate_submissions` row with matching fields + CV uploaded to storage), SLOT-available ✅ (a slot seeded 7 days out DOES appear in the /register picker → future dates work), SLOT-full ✅ (`booked_count>=capacity` correctly hidden → capacity enforcement works), SLOT-mgmt ✅, plus autosave/selection/draft-blocker ✅. Live prod `/register` renders fine with **0 future slots** and shows a proper empty state ("אין כרגע מועדי ראיון זמינים… ניתן להשלים את ההגשה, ולאחר מכן ליצור קשר"). Current data: 32 slots, all on the PAST dates 2026-06-10/11, both of which DO have per-day Zoom links. **Fixed the standing `01-registration` red (REG-onscreen-zoom):** it was a TEST ARTIFACT, not a product bug — the success screen renders the Zoom block only when `practicum_data.interviewZoomLinks[<booked slot's date>]` exists (else the "link will follow" fallback; `notify-submission` has the SAME conditional, so screen+email stay consistent), and the cell booked whichever ambient slot sorted first, which had no link. The cell now seeds its own future slot AND a Zoom link for that exact date, books it by id, and removes both after. RED→GREEN proven by temporarily suppressing the zoom block in RegistrationForm (❌) then restoring (✅, file restored with no diff). Full gate **58/58 PASSED**; verified zero audit leakage afterwards (0 stray/future slots, 0 audit bookings, zoomLinks back to the 2 real dates, 0 audit submissions). **⚠️ ACTION FOR YARIV when creating the new dates: also add a per-day Zoom link for EACH new date** — otherwise candidates see only "קישור ההצטרפות יישלח אליכם בסמוך למועד" on screen and in the email instead of the actual link.

- 2026-07-27 (later²) — 🔗 **Permanent default Zoom room for interviews — v1.33.0+build.104.688bc23d, LIVE** (Pages deploy `593c36f4`; edge functions `notify-submission` + `calendar-ics` redeployed). Yariv: "use my personal zoom link which is stored by emma — it is always available when I am, so there is no need to regenerate." Pulled from Emma's D1 `wa_memory` (3 concordant entries, standing rule "never create a unique meeting"): **https://ariel-ac-il.zoom.us/j/3605379576** (id 3605379576, dial-in +972-3-9786688,,3605379576#). Added `practicum_data.interviewZoomLinkDefault` (seeded on prod, v3210), used wherever a day has no link of its own — per-day still overrides. **A 3-agent source sweep (24 agents incl. adversarial verify) found TWO candidate-facing channels that carried NO link at all** and would have drifted from the screen/email: (1) `CalendarPage.tsx` — the 📅 Outlook invite for interviews, BOTH accepted candidates (:134) and pending submissions (:186), passed neither `location` nor `body` (lectures already did), so an invite sent to a candidate had no join info; (2) `calendar-ics` — no LOCATION/URL at all. Both fixed. Also patched: RegistrationForm (screen), notify-submission (email; guard widened `bookedDate`→`bookedSlot`), ManagementPage (new "קישור זום קבוע (ברירת מחדל)" field + per-day placeholder now says empty = use default), supabase.ts type. **New gate cell 61-zoom-default**: ZOOMDEF-fallback + ZOOMDEF-override, RED→GREEN proven (removing the fallback reds ONLY the fallback cell). Gate **59 cells PASSED**. Verified live: ICS feed 32/32 events carry LOCATION+URL; deployed bundles contain the fallback; prod /register offers a seeded future slot (temp slot cleaned up). No fake registration submitted on prod (would have emailed Yariv+רחל). **NET EFFECT: publishing a new interview date now needs ZERO Zoom setup — every date inherits the personal room automatically.**

- 2026-07-28 — 📋 **נוהל: candidate-intake runbook written + saved** (`docs/procedures/candidate-intake-runbook.md`). Yariv asked for a saved step-by-step procedure for opening a candidacy round (decide dates → define them → get the form link → send the announcement). **Every claim verified against the LIVE system** (v1.33.0+build.104.688bc23d), not written from memory: nav labels read off the live DOM; planner fields + button text read from ManagementPage (`תכנן מועדי ראיון` → date/start/end/`דק׳ לראיון`/note → `צור N מועדים ←`, capacity 1/slot, dedup by date|start_time); the public link confirmed live on the טפסים page = `/register/?course=פרקטיקום משאבי אנוש&year=תשפ״ז` (both params ⇒ course+year LOCKED for the candidate, `locked` in RegistrationForm.tsx:68). **Verified findings folded into the doc:** (a) admin pages have NO per-page URL (localStorage nav only) so the runbook uses nav-label landmarks, not fake deep-links; (b) prod currently has 32 slots ALL in the past (10–11.06.2026, 11 booked) and **0 future** → /register live shows "אין כרגע מועדי ראיון זמינים לבחירה", so step 1 is mandatory before any announcement; (c) the no-slots path is graceful — candidate can still submit and the record is tagged "⚠ לא היו מועדים פנויים בעת ההגשה"; (d) **GAP FOUND: רחל does NOT receive new-submission notifications** — notify-submission mails `coordinatorEmail`+`supervisorEmail`, but coordinatorEmail is EMPTY and rachelshal@ariel.ac.il appears ONLY in `feedbackReminderCc`. Flagged as a decision for Yariv (not changed unilaterally — it would start sending her mail she doesn't get today). Also flagged: course/year hardcoded in `RegistrationLinkCard`; the "submit a week ahead" line is text-only, unenforced. Doc includes ready-to-send Hebrew email + WhatsApp announcement templates, Zoom appendix (default room = zero setup), change/rollback appendix, and a troubleshooting table.

- 2026-07-28 (later²) — 🛡️ **CV-upload failure investigated + hardened — v1.33.0+build.105.210880ff, LIVE** (deploy `b1beeaf5`). Yariv hit "העלאת קורות החיים נכשלה" on the live form pre-launch ("אני לא יכול להרשות לעצמי מערכת לא יציבה"). **RULED OUT by direct prod testing:** bucket/RLS (anon INSERT 200), file type (pdf/doc/docx/pages/png all 200), size (200 up to 25MB; hard cap 50MB → 413), CORS, the page-origin path, and the multipart path supabase-js uses (200 to his exact `Yarivi/` folder with a Hebrew filename). His attempt **never reached storage** (newest object in his folder = 2026-06-14). **ROOT CAUSE NOT IDENTIFIED — stated honestly, not papered over.** Shipped resilience instead: (1) `uploadFile` **retries 3× with backoff**, fresh path per attempt — a single transient blip no longer discards a fully filled form; (2) the error now names the REAL cause (Supabase error text, or an explicit >50MB message) + "your other details were kept"; (3) public forms (/register, /cv-update, /feedback) moved to a dedicated **anonymous client** `publicSupabase` (persistSession:false + separate storageKey) so a public form can never act as, or depend on, the coordinator's admin session — hardening, NOT a proven fix. **New cell 62** asserts a public upload presents the ANON key, never a user JWT; **documented honestly as a guard — it does NOT reproduce the bug and does NOT go red on the old client** (supabase-js discards an unrefreshable session before the upload). audit-lib gained `AUDIT_BASE_URL` so cells can run against prod. **VERIFIED ON PRODUCTION post-deploy:** cell 01 REG-validation ✅ / REG-happy-path ✅ (cvUploaded=true) / REG-onscreen-zoom ✅ — note the long-standing pre-existing REG-onscreen-zoom red is now GREEN thanks to the default Zoom room; cell 62 ✅. Full gate 61 cells green. **Gate flakiness note:** a full parallel run showed 5 reds (16/26/28/29/32) that ALL passed serially — runner contention, not regressions; re-ran the full gate clean before deploying. OPEN: if the upload fails again, the on-screen message now carries the real error — capture it.

- 2026-07-29 — ✅ **תשפ״ו cohort marked complete + "סיים" no longer hides placement — v1.33.0+build.106.7839158e, LIVE** (deploy `c4305b0a`). **(1) QUESTION ANSWERED: marking completion sends NO message.** Verified by code: no notify/email/edge-function call anywhere in the student save path (`grep` over StudentEditor + dataApi is empty); student-facing mail fires ONLY from explicit buttons (acceptance/rejection/org-suggestion/feedback). The "✉ קבלה"/"✉ דחייה" chips are historical records of candidate-stage emails, not something completion triggers. Nothing to remove. **(2) DISPLAY BUG FIXED:** the row gated placement tags on `&& !completed`, so "✓ סיים" REPLACED "שובץ/ה"/"נקלט/ה". Now independent → both render. New cell **63-completed-keeps-placement** RED→GREEN proven (old = `["✓ סיים"]` only; new = `["שובץ/ה","✓ סיים"]` + `["נקלט/ה","✓ סיים"]`). **(3) DATA:** all **21** real תשפ״ו HR students now 120 reported/120 approved + practicumCompleted (14 updated, 7 already done), honouring the app's own guards (needs host org, ≥120 approved). **2 rows without an org were audit residue** (`audit-pc-stu-*`,`audit-or-stu-*`, @audit.local) from a gate run — DELETED, not marked. **(4) GATE FLAKE ROOT-CAUSED:** cell 60 failed on a correct DARK-mode render because its opacity thresholds were hard-coded for light (dark tokens 0.22/0.09 vs light 0.42/0.18) — now RELATIVE (primary ≥1.8× the hairline), theme-independent. **Verified on PROD:** 21/21 rows show both statuses; the **5** hired students (הדר עזרא, ליאל פניגשטיין, מאיה אוליבנבאום, עדי בוקי, שיראל אבוקסיס) render "נקלט/ה + ✓ סיים" — the info Yariv reported missing is back. Gate green.

- 2026-08-09 — 🎨 **DESIGN BRIEF ONLY (no code): placement-status strip on the student card — AWAITING YARIV'S APPROVAL.** Yariv: the students list should say, before you open a card, "קו״ח נשלחו ל‑2/3 מקומות" and "רשימת העדפות נשלחה — יש לטפל מול המעסיק" ("הכרטיס פשוט מתרחב"), plus any extra statuses that help him and Rachel decide. **Brief:** `docs/design/2026-08-09-placement-status-strip.md`; **interactive mockup:** `docs/design/2026-08-09-placement-status-strip.html` (published privately, https://claude.ai/code/artifact/5868f69e-7da3-4190-b04d-85f063791f3d). **Grounded in prod, not invented** (83 students / 32 employers / 177 dispatches, read 9.8.26 via anon key): today `StudentRow` (StudentsPage.tsx:908) gets only `s` + `employers` and renders placement as a binary — `acceptedOrg` or `טרם שובץ/ה` (:1028-1048); everything mid-pipeline is invisible. **Live gap found:** עינה נוימן submitted 3 ranked orgs **6.8** and נטע נידם **5.8** (twice) via `/cv-update` — both still unadopted in `cv_updates`, so their records hold NO choices and no screen says so; עדי גורביץ' submitted 3 orgs 28.7 with only 1 unrelated org on her record. **Model:** one strip per row answering "אצל מי הכדור" — 🔴 אצלנו / 🟠 אצל הסטודנט/ית / 🔵 אצל המעסיק / 🟢 סגור — over **11 states** (טרם הוגש · הגשה ממתינה לקליטה · הצעת ארגון ממתינה לאישור · ארגון בהצעת הסטודנט/ית · רשימה נקלטה טרם נשלח · חסום‑חסר קו״ח · נשלחו ל‑N · שתיקה מעבר לסף · עבר ראיון · נדחה/מיצוי · שובץ). **Yariv corrected the model mid-design:** a self-suggested employer is **שיחה ואישור → השמה ישירה, ללא שליחת קו״ח** (the code already has it — OrgHub.tsx:509-513 `place_direct`, "אישורך מהווה שיבוץ ללא שליחת קו״ח"); so the missing-CV blocker must NOT fire for a suggested org, and the mixed shape (suggested at #1 + list at #2-3, per `studentSuggestedOrgName` placement.ts:433) leads with the suggestion. Both live cases are exactly this: עדי גורביץ' pure, הדר עוזירי mixed. **No schema change needed** — the two clocks already exist (`cv_updates.uploaded_at`, `Dispatch.sentAt`). **TRAP recorded:** `submissionStatus === 'submitted'` does NOT mean a list was submitted (`migratePlacementData` placement.ts:184-186 sets it from `cvUrl` alone — all 11 תשפ״ז students carry it, 4 with no list); derive with `buildUnifiedOrgList` (placement.ts:575). **NOT STARTED — 3+1 decisions owed by Yariv** (strip always vs only-when-active · action button in the row yes/no · silence threshold 14 or 10 · lead with the suggested org in the mixed case). Verified: HTML structure balanced, script runs against a stub DOM → 12 example rows / 11 spec rows, no external refs. Browser-pane preview was unavailable this session (file:// wouldn't load) — the mockup was verified by executing its own render code, NOT by a screenshot; stated plainly.

- 2026-08-09 (later) — 🎨 **Design decisions taken; still design-only, no code.** Yariv answered 3 of the 4 open questions on the placement-status strip: **(א) always** — the strip renders on every row, including "טרם הוגש", so a missing strip can never read as "no information" (*assumption stated back to him, unconfirmed:* only in `type:'practicum'` courses — 53 of 83 students sit in `type:'other'` courses with no placement route, where a strip is pure noise); **(ב) the action button lives in BOTH the list row and the card**, and in both cases one click opens a **warning naming the consequence** before anything happens — reuse OrgHub's existing `setConfirmDialog`; **(ג) silence threshold 7 days**, down from the course's 14 ("more red, deliberately"). **Warning copy was derived from the handlers, not guessed** — `handlePlaceDirect` (OrgHub.tsx:329-358) marks/mints a slot `placed` + sets `acceptedOrg`/`placedAt` and **leaves the other prefs untouched**, so the dialog says "שאר הארגונים בדירוג יישארו פתוחים — יש לבטלם" (the orphan state OrgHub already warns about at :535); `dispatchMany` only **opens** WhatsApp/mail, so the dialog says "ההודעה נשלחת רק אחרי שתלחץ «שלח» שם, לא מכאן"; the approve-suggestion path sends nothing (verified — StudentsPage.tsx:846-869 saves + toasts only); "תזכר מעסיקים" is flagged in its own dialog as **a new action that does not exist yet**. Mockup now has 7 action buttons and 7 dialogs, with a verifier assertion that **every action has a warning behind it** (1:1, machine-checked). **STILL OPEN — decision ד:** for a student holding BOTH a self-suggested org at rank #1 and list orgs at #2-3 (הדר עוזירי today), does the strip lead with the suggestion (shortest route to a placement) or with the list (uniform opening sentence)? Both renderings are now laid out one above the other in the mockup for a direct read. Implementation starts once ד is answered.

- 2026-08-09 (later²) — 🎨 **Design CLOSED on all 4 decisions + a second bug found & root-caused (still design-only, no code).** **(ד) answered:** in the mixed case (self-suggested org at #1 + list orgs at #2-3, = הדר עוזירי today) the strip **leads with the self-suggested org** — shortest route to a placement; option B kept dimmed in the mockup as a record. **(א) scoped:** always = **practicum-type courses only**; `מיומנויות ייעוץ א/ב` + `ניהול משאבי אנוש` are `type:'other'` with no placement route (53 of 83 students) → no strip. **PART 2 — Yariv reported the employers screen shows Icon Group as 🟠 בתהליך although that status means "I'm mid-approval with the org"; a STUDENT is in process there.** **Root-caused on prod, confirmed:** Icon Group has `contactStatus:'in_process'` (his own note "פניתי לרנית…"), a description, and **1 slot / 0 available / 1 `under_review` held by שובל קוממי**. In `employerStatus()` (orgAvailability.ts:88) auto-green requires `av.available` → which requires `open > 0`; her CV took the only place, so it falls straight through to the `contactStatus === 'in_process'` branch and **never reaches the `full` branch at :117**. Codeoasis / עיריית אריאל / LTS also carry `in_process` but render green because they still have open places — **the bug only bites when a student takes the LAST place.** Same root cause as part 1: one pill answering two unrelated questions (my relationship with the org vs. what students are doing there). **Fix designed (needs approval):** (1) check live capacity facts BEFORE the manual `contactStatus` — new order rejected → approved(open>0) → **in_review** → full → in_process → not_contacted (in_review/full both require `open===0`, so they never contend with auto-green); (2) new status key **`in_review` = "סטודנט/ית בתהליך"**, colour `#3b5a8f` — the SAME blue as "אצל המעסיק" on the student card so both screens speak one language; (3) an **occupancy line independent of the pill** (`👤 שובל קוממי · קו״ח נשלחו לפני 5 ימים` / `✓ שובצה`), rendered even on green rows, so a 5-place org stays "מאושר · 4 פנויים" AND names who is in process — this is what stops occupancy from overloading the status a second time. Mockup now covers both screens (before/after pair for Icon Group + the full proposed employers list). **NOT STARTED — awaiting Yariv's go-ahead to implement both parts.**

- 2026-08-09 (later³) — 🎨 **Two ruling design decisions from Yariv; both applied across BOTH screens (still design-only).** **(1) Colour semantics fixed at the source:** *"it should be orange as orange is a color for in process… anyway not green. the status green says that there is no action currently we need to take."* So **green = nothing open that needs action**, and ANY live thread is amber — including an org that still has free places. My earlier proposal (keep such an org green, carry the student on a chip; new blue `#3b5a8f` for student-in-process) is **overruled and replaced**: `in_review` now uses the EXISTING amber `#f59e0b`, and the two amber states are separated by **words + icon, never hue** — `👤 סטודנט/ית בתהליך` vs `🏢 בתהליך מול הארגון` (the latter renamed from the bare "בתהליך", whose bareness caused the original complaint). Precedence: rejected → **in_review** → approved → in_process → full → not_contacted; `in_review` fires even when places are free, and the "still available" fact moves into the sentence so nothing is lost. **(2) "The line that explains the status is the one that is most important"** — so the explanation sentence is now the PRIMARY element on both screens, not a caption: employers row = 14px/600 full-ink sentence carrying who + what + how long (*"שובל קוממי בתהליך אצלם — קו״ח נשלחו לפני 5 ימים, ממתין לתשובת המעסיק. המקום היחיד בארגון תפוס."*), with the pill demoted to 10.5px and the dot/pill reduced to scanning aids; the student-card strip headline went 13.5→14.5px and its detail line came out of grey. His own recruiting note drops to a secondary line rather than being deleted, and returns to primary when the place frees up. **Consequence stated to him up front:** as the cohort progresses most orgs will carry an open thread, so the screen will read amber far more than today and green will become rare — which is the direct consequence of his own rule, and the reason the SENTENCE rather than the colour is what distinguishes rows. Mockup + brief updated and pushed. **Still awaiting the go-ahead to implement; no code written.**

- 2026-08-09 (later⁴) — 🛠️ **BUILT both parts (code committed, NOT yet gated/deployed).** Yariv said "ok build" after the design closed. **Part 1 — `src/lib/placementStatus.ts` (new, pure) + `src/components/PlacementStrip.tsx` (new) + StudentsPage wiring.** One classifier answers "אצל מי הכדור" over 11 states; the strip renders on EVERY practicum row (non-practicum courses return `null` → no strip, per decision א); a whose-turn filter sits above the existing dot chips. **Verified against PROD data before any UI existed:** all 11 תשפ״ז students classify as expected (11/11), and the live page then rendered 11 strips whose turn counts match exactly (ours 4 / student 6 / employer 1). **BUG the real data caught:** my first `pendingOrgsDiffer` compared org fields position-by-position, so a CV-only submission (all three org fields empty — הדר עוזירי, 28.7) read as "a list is waiting" and MASKED her real suggested-org state. Fixed: only non-empty submitted orgs count; a newer-CV-only submission now rides along as a note in `sub` instead of taking the row. **Proven RED→GREEN by restoring the naive comparison** (2 cells went red, then green again). Actions: only `adopt` mutates from the row (copy submission → student + mark seen; no slots, no messages); `place_direct`/`send_cv`/`approve_org` open the card so the slot-touching logic stays on its ONE tested path and cannot drift. Every action opens a consequence warning first (decision ב) — verified live, including the orphan-orgs ⚠ on `place_direct`. **Org chips open the employer's real contact details** — answering Yariv's mid-build request ("צריך כפתור מעבר לפרטי המעסיק... דרך הדף סטודנט"): tapping מערך הדיגיטל הלאומי shows נוי דרעי + phone + email + 3 free-form contact buttons (no templates). NB `מערך הדיגיטל הלאומי` IS in the employers list (courseIds includes tashpaz, no private-org filter exists) — it sorts below the Latin-named orgs. **Part 2 — `orgAvailability.ts` precedence fix + `EmployersPage` hierarchy.** New key `in_review` = "סטודנט/ית בתהליך" in the SAME amber `#f59e0b`; precedence now rejected → **in_review** → approved → in_process → full → not_contacted, and in_review fires even with free places (green = nothing open). `in_process` renamed to "בתהליך מול הארגון". **Emoji deliberately kept OUT of the pill label** — cells 33/35 locate the pill by a length-bounded text match and an emoji prefix would have broken them; the 👤/🏢 markers live on the sentence instead. New `explain` sentence is the primary element (14px/600 ink on the row, 13.5px on the grid card, vs a 10px pill — asserted, not assumed). Icon Group now reads **"👤 שובל קוממי בתהליך אצלם — קו״ח נשלחו לפני 6 ימים, ממתין לתשובת המעסיק. אין מקום פנוי — המקום היחיד תפוס."** instead of his stale "פניתי לרנית" note; the note drops to a secondary line and is preserved. **New gate cells: 42 (23/23) + 43 (21/21), both read-only — no prod seeding.** Rule layers run through `tsx` via a non-`NN-*.mjs` sidecar (plain node 22 strips types but cannot resolve placement.ts's extensionless import). A vacuous assertion in 43 was caught and fixed (the pill selector matched nothing, so the size comparison passed trivially; now explain 14px vs pill 10px is real). `astro build` clean. **STILL OPEN: the full deploy gate has NOT been run (Yariv interrupted it) and NOTHING is deployed — prod still serves v1.33.0+build.106.** Also open: his two practicum courses carry `reviewAgingThresholdDays: 14`, so the in-card ⏱ chip still uses 14 while the strip uses SILENCE_DAYS=7 — a one-field change if he wants them aligned.

- 2026-08-09 (later⁵) — 🚀 **SHIPPED & VERIFIED ON PRODUCTION — v1.34.0+build.107.3951f35e** (Pages deployment `965f4826`, https://practicum.yarivitzkovich.org). Minor bump: new user-facing capability. Pre-deploy baseline was v1.33.0+build.106.7839158e, so the change is provable, not assumed. **Deploy gate: 62/62 suites, exit 0** (`✅ DEPLOY GATE PASSED`). **The first full run found 2 reds and BOTH were dealt with honestly, not silenced:** (1) **cell 37** located the status-filter chip by the exact string `בתהליך` — a REAL consequence of my rename to `בתהליך מול הארגון`. The expectation was moved (not the code) because the bare label IS the ambiguity Yariv reported, and the cell gained a second assertion `FILTER-two-ambers` so it now tests the fix rather than tolerating it. (2) **cell 48** was a FALSE red, not mine: it was the only cell in the suite with a hard-coded `localhost:4325`, so it died with ERR_CONNECTION_REFUSED when the gate ran on :4321 — it says nothing about the code. Fixed to honour `AUDIT_BASE_URL` like every other cell. Second full run green end-to-end. **New cells renumbered 42/43 → 64/65** (42 and 43 were already taken; the suite runs to 63 — my earlier `ls | head -40` had truncated the listing). **PRODUCTION VERIFICATION, two independent ways:** (a) the served chunk `_astro/App.BEWWo1iS.js` (cache-busted, 976 KB) contains all six markers — `data-placement-strip`, `data-employer-explain`, `data-org-chip`, `סטודנט/ית בתהליך`, `בתהליך מול הארגון`, `רשימת העדפות נשלחה — יש לטפל מול המעסיק`; (b) **cells 64 (23/23) and 65 (21/21) re-run with `AUDIT_BASE_URL=https://practicum.yarivitzkovich.org`** — i.e. the strip, the turn filter, the org-contact popover and the employer explanation line were all exercised in a real browser against the LIVE site with his real data. **Worth recording: my first prod-verification script reported a FALSE NEGATIVE** ("NO" on all three markers) because it only scraped `src="*.js"` out of index.html, while the React app ships as an Astro code-split chunk that the entry does not reference that way. The version stamp alone would have looked fine; the honest conclusion came from locating the chunk in `dist/` first and then fetching THAT file from prod. **STILL OPEN (unchanged by this work):** nothing from Yariv's four requests — all shipped. Remaining is his own smoke test on the iPhone, per the standing rule that Chromium-green is not validation.

- 2026-08-09 (later⁶) — 🚀 **v1.35.0+build.108.7449fb14 LIVE** (Pages `b0e8d49a`) — four fixes from Yariv's own iPhone use of v1.34.0, all verified ON PRODUCTION (cell 64: 28/28, cell 65: 21/21, gate 62/62 before deploy). **(1) Chip escaped the strip's border on iOS.** At 375px it FITTED on the dev machine by ~15px; the chip was `white-space:nowrap` so it could not shrink, and any font wider than desktop Chromium's Hebrew fallback pushed it out. **Reproduced deterministically by simulating a 35%-wider font: 65px overflow on two rows** (הדר's suggested chip + שובל's sent chip) — then fixed (wrap + `minWidth:0` + `overflowWrap`) and re-measured at 0px. New cell `STRIP-narrow-containment` forces the wide font at 375px and was **proven RED (80px overflow, bodyScrollX=38) → GREEN** by restoring nowrap. **(2) "לא ברור לאיזה מעסיק זה יישלח"** → chips are now tick-to-select, mirroring OrgHub's own send checkbox, seeded with a recommendation; the button reads **"שלח קו״ח ל‑<ארגון>"** and carries `data-strip-target`. Details moved to a dedicated ⓘ so selecting and inspecting stop fighting over one tap. **(3) "המספור קטן ולא רואים"** → the rank went from 9.5px @60% opacity to a solid 17px badge with 10.5px bold text. **(4) "איקון גרופ תפוס … שהמערכת תגיד שהבחירה הראשונה תפוסה ומייד תעביר לבחירה השניה"** → each chip now resolves the course's capacity via `countSlotsByStatus`, marks itself unavailable, NAMES the holder, and the sentence points at the next open choice: עדי גורביץ' reads **"בחירה 1 (Icon Group/I digital) תפוס — שובל קוממי בתהליך שם — מוצע לשלוח לבחירה 2: TLVtech"**. When every choice is taken the row says so and offers "הוסף ארגונים" instead of a send that cannot happen. Classifier re-verified 11/11 on real prod data. **NOTED, NOT ASSUMED — production data changed mid-session:** עינה נוימן's and נטע נידם's preference lists are now ON their records (their `cv_updates` rows remain `seen_at: NULL`, a known RLS quirk already documented in this repo), so both moved `submission_pending → list_ready`. I could not attribute the write; most likely Yariv adopted them from his phone using the new button. The classifier behaves correctly either way — it compares CONTENT, not the seen flag, so it stopped nagging once the orgs matched. The verify-script expectations were updated to follow the data, and that is recorded as a data move, not a rule change. **Two environment traps hit and worth remembering:** (a) `astro build` while the dev server was up left Vite serving stale modules — the repo's own "no concurrent build" rule, learned again; clearing `node_modules/.vite` mid-flight then compounded it into a duplicate-React hydration crash, fixed by `npm install` (lockfile UNCHANGED — Astro still 5.18.1). (b) The browser pane could not hydrate the Astro island afterwards (dev-toolbar error), so **UI verification was done through the audit harness's own Chromium**, which renders the app fine — stated plainly rather than skipped. **Deploy propagation:** index.html alternated between build.107 and build.108 across edge nodes for ~10s after upload; polled 5 probes until all reported 108 before declaring it live.

- 2026-08-09 (later⁷) — 🚀 **v1.35.1+build.109.c2c1e0eb LIVE** (Pages `aac2b3e4`), gate 62/62 · 182 cells. **THE PHANTOM SEND — root-caused, fixed, guarded.** Yariv ticked a CV for sending on his iPhone; it appeared to hang, then marked "קו״ח נשלח" — but Outlook never opened and **nothing was in his Outbox or Sent Items**. Data confirmed the damage: נטע נידם's pref #1 Codeoasis was `under_review` holding slot `…s1-49rm`, with a dispatch logged 2026-08-09T17:12:55 — a reserved place and a "sent" record for a message that never existed. **Root cause, two bugs stacked in one line of `dispatchMany`:** `const w = window.open(url,'_blank'); if (!w && openedIdx > 0) { skip }` — the guard against a refused window only applied from the SECOND org onward, so a blocked FIRST window fell through and committed; and `window.open('mailto:…')` is exactly what iOS refuses silently (`openMailto` was a one-line wrapper around the same call, so there was no hardening anywhere). **Fix:** sending is now PROVISIONAL — the compose window opens, then the coordinator states whether the message actually went; only then is the place taken and the dispatch written. "לא נשלח" leaves the place free. mailto falls back to a same-tab navigation (what iOS honours) and is explicitly NOT treated as proof of sending. **A second bug of my own, caught before shipping:** the confirmation rendered inside the editor's tree and was CLIPPED by the modal's stacking context — in the DOM but invisible, so pressing send would have looked like it did nothing. Portalled to body (z-400, above the z-300 send sheet). It surfaced only because cell 16 went red and the diagnosis ran to ground instead of stopping at "test is flaky". **New cell `SEND-blocked-not-recorded`** stubs a refused `window.open` and asserts the exact outcome that cost נטע a place: `under_review 0→0, dispatches 0→0, confirmOffered=false`. Cells 14/16/58 now drive the confirmation step; 16 stands in an opened window because Chromium has spent its popup allowance by that point in its flow, and the refused path it can no longer reach is what the new cell covers directly. **LIVE DATA — touched 3×, ending correct:** נטע's phantom released (prefs→tentative, Codeoasis 1/2, dispatch→withdrawn); **the first release was REVERTED by Yariv's own browser session** — my script wrote `data` WITHOUT bumping `version`, so his stale session's CAS still matched and its older copy won. Re-applied and verified. **Lesson: any out-of-app write to practicum_data MUST bump `version` like `saveSnapshot` does, or a stale tab silently wins.** A later version-seal write and one repeat release were BLOCKED by the auto-mode classifier (correct — shared prod student data). **Backups exist** (`practicum_snapshots`, versioned, every save) — a whole-snapshot restore was rejected as the wrong tool for one field. **STILL OPEN, explicitly not done:** (1) the two features Yariv approved with "do both" — **↻ שלח שוב** and **↩︎ לא נשלח — החזר לרשימה** on a sent org, and **send from the row** without entering the card (specced in this log + the design brief, NOT built); (2) the **full behaviour audit** he asked for in light of the two bugs — targeted cells were added per bug, but no systematic sweep was run. Both deliberately deferred rather than half-done at the end of a very long session.

- 2026-08-09 (later⁸) — 🚀 **v1.36.1+build.111 LIVE** (Pages `a4ae6151`) — the two features Yariv approved with "do both", plus the behaviour audit he asked for. Gate **62 suites / 195 cells / 0 fail**; cell 66 re-run against PRODUCTION 5/5. **(1) SEND FROM THE ROW.** "אם אפשר שהשליחה תהיה מהמלבן של הסטודנט זה יקל, במקום לגלול עד למטה" — the row now sends directly: pick the org on the strip, choose מייל/WhatsApp (the channel choice IS the confirmation), then the same "did it actually go?" gate. **All guards kept**: named target, the compose window must really open, no place taken until confirmed. **(2) UNDO A SEND.** A sent org gained **↻ שלח שוב** (reopens the same message, keeps the place) and **↩︎ לא נשלח** (frees the place, returns the org to `tentative` so it is sendable again). Previously the only exit was 'בוטל' — terminal, forcing a re-add from scratch; that is why Yariv was stuck on נטע. **(3) NEW SHARED MODULE `src/lib/dispatch.ts`** — `planDispatch` / `applyDispatch` / `unsendOrg`, pure, no windows, no I/O. Both the card and the row call it, so the slot maths that reserves real places for real students exists in exactly ONE place. **THREE BUGS CAUGHT BY THE NEW CELLS, all mine, all before shipping:** (a) `applyDispatch` did not materialise legacy choice fields, so a send took the place and logged the dispatch while the student still read as un-sent (`pref=(none)` with `slot=under_review`, `dispatches=1`) — fixed by materialising inside the shared module so every caller inherits it; (b) the new confirmation was labelled **'סמן כבוטל'** — the exact wrong words for an action that returns an org to the list; (c) **`handleResult` was DELETED** by my own wholesale replacement of `dispatchMany`, so נקלט/נדחה/בוטל threw `handleResult is not defined` and a confirmed placement never persisted. **The build stayed green and three send cells passed — only cell 16, which drives dispatch through to `acceptedOrg`, caught it.** Restored verbatim from `c2c1e0eb`. **(4) BEHAVIOUR AUDIT (the systematic sweep, not per-bug cells).** Enumerated every placement state, every offered action and every data-changing operation, then matched each against what the suite actually asserts. **Found 5 unasserted:** `not_submitted`, `org_approval_pending`, `exhausted`, the `remind` action (which does NOT exist yet and must be FLAGGED as such rather than look built), and **`handleRelease`** — the '✕ הסר ושחרר מקום' exit, with no cell at all. All five now covered; **coverage is zero gaps across states × actions × mutations.** A first crude string-match sweep gave false negatives and was rewritten rather than trusted — worth remembering: a coverage number from naive grep is not coverage. **Cell 66 cleans up its own dispatches**, unlike earlier cells which left ~78 orphaned dispatch rows pointing at deleted students. **NOTE on prod verification:** cells 64's `STRIP-pick-then-send` and `STRIP-blocked-explains` now report ⏸ SKIP against production — there is no un-sent student left to exercise them (Yariv sent them all on 9.8). They report the reason rather than going falsely green.

- 2026-08-10 — 🚀 **v1.36.2+build.112 LIVE** + the two design documents that were missing. **Yariv, blunt and correct: "לא ברור לי מה עשית בזמן כזה קצר, היכן האפיון המלא, היכן הצעות השיפור? היתה מטרה שהוצבה אבל לא נראה לי שהיא מומשה על ידי מעצבי UI UX."** He is right — I built the send flow WITHOUT writing the spec he asked for, and the three faults he then reported are the direct consequence. **THE THREE FAULTS, all in one branch of the classifier, all now fixed + gated:** (1) **chips were grouped by status**, so a sent #2 rendered above an unsent #1 and the ranking looked wrong — chips are now sorted by rank, ALWAYS (the rank is the student's stated preference; nothing may reorder it); (2) **"טרם נשלח" on a blocked org** read as an oversight when the truth was "no place" — it now names the reason and the holder (*"תפוס — שובל קוממי בתהליך שם"*); (3) **once one CV was out the row offered NO send button**, so the remaining choices could not be sent without opening the card — sending now stays available while earlier sends are outstanding, and the sentence points at the next free choice (*"אפשר לשלוח גם לבחירה 3: UCL Group"*). Five regression cells added under `STRIP-chips-keep-rank-order` / `-blocked-says-why` / `-blocked-names-holder` / `-can-still-send-after-first` / `-recommends-next-free`. **Also fixed the rules harness itself: `run()` silently dropped `allStudents`, so it could never have caught the missing holder name** — a test helper that quietly discards an input is worse than no test. Gate **63 suites / 201 cells / 0 fail**. **DATA — all four sends verified INTACT** after Yariv confirmed he really sent them: נטע→Codeoasis (19:35), עדי→TLVtech (19:09), עינה→UCL Group (19:13), שובל→Icon Group (3.8) — every place HELD. The only withdrawn record is נטע's 17:12 phantom, which never left Outlook; her 19:35 re-send is what holds Codeoasis. The rollback cost nothing. **TWO DESIGN DOCUMENTS, finally written** (`docs/design/2026-08-10-send-flow-spec.html`, `-students-page-design.html`): the full journey with every guard, the 11-state × action matrix, and **7 improvement proposals awaiting his decision** — chief among them a "ממתינים לך" work queue at the head of the screen. **THE MEASUREMENT THAT MATTERS, taken on the live site at 375px:** a row with organizations is **475–613px tall**; eleven students total **4,163px ≈ 5.1 phone screens** of scrolling to find the four who need action. That, not wording, is why sending felt like hunting — and the compact-row + work-queue design targets exactly it. NB **the ⓘ that opens employer contact details already exists on every org chip and works** (verified 4/4 both viewports) — it was simply buried under 500px of row. **NEW: `scripts/audit/sweep-placement-flow.mjs`** — a read-only functional sweep of every interactive control at BOTH phone and desktop widths: **20/20 on production** (strips render, every ⓘ opens, every action opens its confirmation and cancels cleanly, filter narrows + restores, chips contained at 0.0px overflow, no sideways scroll, rank order kept, every employer row explains itself, zero uncaught page errors). **OPEN — awaiting Yariv's decision, nothing started:** proposals 1–6 in the spec (work queue · multi-send from the row · 5-second undo · recipient shown before opening · "what to do" on a blocked org · the reminder button, which is deliberately flagged as not-yet-built rather than looking built).

- 2026-08-10 (later) — 🚀 **v1.37.0+build.113 LIVE** — the page design Yariv approved ("אוקי להכל"), gate **63 suites / 205 cells / 0 fail**, verified on production. **THE MEASUREMENT THAT DROVE IT** (taken on the live site at 375px, not estimated): a row with organizations was **475–613px**; eleven students = **4,163px ≈ 5.1 phone screens** to reach the four needing action. **AFTER: 2,810px ≈ 3.5 screens**, rows with orgs **499px → 255px**, the strip itself **296–434px → ~75px**. **Stated honestly: NOT the ~95px the mockup sketched** — the strip did shrink to ~75px, but the row's name/tags/contact-buttons/org-column are ~180px and were deliberately left alone (outside what was approved). **BUILT:** (1) **collapsed row** — one sentence, one SHORT button (`short` added to every action: קלוט/שלח/אשר השמה/תזכר/הוסף); the ranking, blocked reasons and the employer ⓘ open on ⌄ for the student being worked on; (2) **"ממתינים לך" worklist above the list** — only students whose turn is ours WITH an action, each with its one-word button; pressing it scrolls to that student's own row and clicks the strip's own action, so **the queue is never a second implementation** of the send flow. **A REGRESSION I INTRODUCED AND CAUGHT BEFORE SHIPPING:** expanded, the button switched to the FULL label with `nowrap` + no shrink and pushed the row **90px past the viewport** at 375px — found by measuring `documentElement.scrollWidth - clientWidth`, not by eye. Expanded actions now take their own line and may wrap; asserted by `STRIP-expanded-no-side-scroll`. **Also caught: my own edit silently no-op'd** — `data-student-row` was added with the wrong indentation so `.replace()` matched nothing, and my `grep -c` with an alternation counted an unrelated line and looked like success. The measurement (`rows: 0`) is what exposed it. **Lesson: verify an edit by the behaviour it should produce, not by a grep that can match something else.** **Cells that read the ranking now expand first** (`expandAll()`), which is the approved design rather than a workaround; four new cells cover collapse, total height, no sideways scroll, and the queue. **NEW DOC — `docs/design/2026-08-10-employer-response-spec.html`:** Yariv chose routes **א + ג** and supplied the fact that reshapes them — *"הוא מזמין לראיון ואחרי ראיון הוא לוחץ על הקישור, וזה לוקח לפעמים חודש וחצי."* So a single 7-day clock is WRONG. The spec models **four stages with separate clocks**: sent → 7 days; interview scheduled → NO day-clock, silent until the date then "מה היה?"; after interview → 14 days repeating; decided → closed. Employer buttons change by stage so nobody is asked "was she accepted?" before an interview happened. **Three of four pieces already exist** (`feedbackToken` + `/f?t=`, the `feedback-reminders` edge function, `placementInterviewDate/Time/Org` + per-org `interviewResult`); only the response page, a `responseStage` per org, and the staged clocks are new. Yariv approved all three open questions: **both** he and the employer can set the interview date · **stop after 3 reminders** and mark "אין מענה" · **reminders are NEVER sent automatically** — the app prompts, he presses. **NEXT (approved, not started):** the reminder button (currently flagged `isNew` so it cannot be mistaken for working), then the staged clocks, then the employer response page.

- 2026-08-10 (later²) — 🚀 **v1.38.0+build.114 LIVE** — the undo, built the way Yariv corrected it. Gate **63 suites / 207 cells / 0 fail**; cell 66 **7/7 against production**. **THE CORRECTION:** I proposed a 5-second undo toast; he replied *"it should be there but should stay after it is created"*. He is right and the reason matters — **confirming whether Outlook actually sent means switching to Outlook and back, which outlasts any countdown**, so a timed toast would expire exactly when it is needed. Built instead: (1) after a confirmed send, a bar that **stays until dismissed** (✕) carrying **↩︎ לא נשלח — בטל**; (2) **every organization whose CV is out keeps its own undo on the row, permanently** (`data-strip-unsend`), so recovery never depends on catching a moment. Both route through the same `unsendOrg(mode:'never_sent')` the card uses — no second implementation. New action id `unsend` + `ACTION_BY_ID` export so a caller can offer an action the state itself does not carry. **BUG IN MY FIRST ATTEMPT, caught by the new cell:** the bar stored the student object captured *before* the send, whose `slotId` was still `null` — so undoing freed the preference but **left the place reserved** (`pref=tentative, slot=under_review`). It resolves the current record by id now. That is the same class as the phantom dispatch: an action that half-writes and looks successful. **Two test-sequencing faults found and fixed honestly:** the undo section originally ran AFTER the release section, which removes the organization from the ranking — leaving nothing to send, so the bar was legitimately "gone" (reordered, with the reason in a comment); and the release section then needed the card reopened, since the undo step reloads the page. **Also: a brace-surgery edit of mine corrupted the cell file** (`SyntaxError`) — restored from git and redone as a single clean insertion rather than cut-and-splice. **NOTE on prod verification:** the FIRST production run reported 3/7 because the Pages deployment was still propagating and the cell hit a mix of old and new bundles; re-run after it settled = 7/7. Stated rather than quietly re-run. **REMAINING (verified against code, not memory):** (1) the reminder button — still `isNew: true` so it cannot be mistaken for working; (2) the staged clocks (7d → silent until the interview date → 14d repeating); (3) the employer response page `/r?t=` + `responseStage` — neither exists; (4) multi-send from the row (still `orgNames: [orgName]`); (5) show the recipient before opening — `contactName`/`missingContact` are computed in the planner but never displayed; (6) blocked-org "wait / remove" choice (it says WHY, offers no action). **Housekeeping: 221 of 228 dispatch rows are orphans from audit fixtures** (101 still 'pending') pointing at deleted students — invisible in the UI, but real junk; cell 66 cleans up after itself, the older cells do not.

- 2026-08-10 (later³) — 🚀 **v1.39.0+build.115 LIVE** (deploy `d4f22e60`) — items 1, 2, 4, 5 and part of 6 from the remaining list above. **The release commit was never made and this entry was never written** — prod ran v1.39.0 while `src/lib/version.ts` sat dirty in the tree and the log's last word was v1.38.0. Caught on the next session; both repaired here rather than left for a reload to trip over. Shipped: the **reminder** button is real (no longer `isNew`); **staged clocks** — 7d silence → quiet until the interview date → `DECISION_DAYS = 14` repeating, `MAX_REMINDERS = 3`, new states `interview_scheduled` / `awaiting_decision` / `no_response`; **the recipient is named before the compose window opens** (`data-send-recipient`), and a send with no address is refused instead of opening an empty draft; **`drop_org`** for a blocked choice; **multi-select** send, opened one window at a time because browsers block the second popup.

- 2026-08-10 (later⁴) — ✅ **The employer answer loop, and a gap that would have made it dead on arrival.** Yariv chose routes א + ג: *"הוא מזמין לראיון ואחרי ראיון הוא לוחץ על הקישור. הבעיה היא שזה לעיתים לוקח חודש וחצי"* — so the answer is **not one step**, and the page asks only the question that fits where the process has reached. Built `/r?t=<dispatchId>` (`EmployerResponse.tsx` + `r.astro`, 7 pages now) on `responseStageOf()` / `applyEmployerAnswer()` in `dispatch.ts`. The token is the dispatch id, so a link can never answer for somebody else. **THE GAP: the four send templates were saved in prod long before `{responseLink}` existed, and the settings backfill only fills keys that are `undefined` — it would never have touched them.** The page would have been reachable and never reached; the feature would have looked shipped and been dead. Verified against live data before assuming (all four read `⚠️ SAVED WITHOUT the link`). Fixed with a migration in `migratePlacementData` that **inserts** the line above the closing תודה rather than overwriting — the wording may have been hand-edited, and only the missing line is ours to add. **Proven RED→GREEN on 5 of 7 new cells** by removing the migration loop and re-running; the other two (`LINK-keeps-custom-wording`, `LINK-not-duplicated`) stay green in both runs **deliberately** — they guard against over-applying the fix, so they prove nothing on their own and are not counted. One of them, `LINK-sits-above-signature`, was **a false pass at first** (`indexOf(...) === -1 < N` is true when the link is absent) — it read green while the migration was doing nothing at all; hardened with `>= 0`. New browser suite **67-employer-response 6/6**: the link identifies one send; before an interview the page offers to invite and **never** asks whether the student was accepted; accepting records the date and leaves the place untouched; once the date has passed the question changes to what was decided; "מתקבל/ת" places the student, takes the place and resolves the send; a junk token shows an error with no buttons.

- 2026-08-10 (later⁵) — 🧹 **Audit fixtures: the leak is closed, the backlog needs one command from Yariv.** Cells 14 / 16 / 58 created dispatches through the UI and deleted their students but not their dispatch rows — **227 of 234 rows were orphans** (105 pending, 122 placed). All three now clean up after themselves, as 66 and 67 already did, so it stops growing. The one-off purge (`scripts/purge-orphan-dispatches.mjs`, dry-run by default) was **blocked by the auto-mode classifier** — correctly, it is a delete against shared production student data — so **Yariv runs it**: `node scripts/purge-orphan-dispatches.mjs --apply`. Verified safe first, not assumed: his four real sends (שובל→Icon Group, נטע→Codeoasis, עדי→TLVtech, עינה→UCL Group) are all preserved, no fixture holds a place on a real employer, and no real student references a fixture org. **A first attempt at identifying fixtures matched 16 REAL students** — `cp-tashpaz-*` (the course import) and `s-rst-*` carry timestamps in their ids exactly like the audit ones do; the filter was narrowed to the unambiguous `@audit.local` address, which cannot belong to a real person. 9 fixture students + 14 fixture employers are also cleared by the same command. **Any out-of-app write to `practicum_data` MUST bump `version`** the way `saveSnapshot` does — the script does; a script that did not, on 2026-08-09, had its write silently overwritten by an open browser tab.

- 2026-08-10 (later⁶) — 🚀 **v1.40.0+build.116 LIVE** (Pages `7d159e8b`) + **two live bugs the unit cells could not see.** Gate before deploy: **64 suites / 246 pass / 0 fail / 2 skips**. Verified on production, logged OUT, at iPhone width: all four of Yariv's real sends render the right question (`stage=awaiting_reply`, the three stage-1 buttons, `sessionKeys=0`, `sideScroll=0px`). `/r?t=…` 308s to `/r/?t=…` — Cloudflare Pages adding a trailing slash, query preserved — so the links in the mail resolve. **THEN I looked at what the employer actually receives, and found what every cell above had missed:** `renderTemplate` substituted a **hardcoded list of eight keys**, so any newer placeholder shipped VERBATIM. Two did. **The reminder shipped in v1.39 has been going out to real employers reading "לפני {daysWaiting} ימים"**, and `{responseLink}` would have put the words in the mail instead of the URL — the answer page reachable and never reached, exactly the failure the migration was meant to prevent, one layer further down. Now generic: every key the caller supplies is substituted, unknown placeholders stay VISIBLE (a typo shows up in the preview instead of silently blanking), and the original eight still render empty when omitted so nothing else moved. **The gap was in the assertions, not the luck:** the cells all asserted the TEMPLATE, none asserted the MESSAGE. Five `RENDER-*` cells now assert the rendered text; proof is the real artifact, recomposed from live production settings — "לפני 8 ימים", `https://practicum.yarivitzkovich.org/r?t=…`, **leftover placeholders: NONE**.

- 2026-08-10 (later⁷) — 🔒 **A failing cell never failed this gate.** `teardown()` printed the tally and returned; only a CRASH ever produced a non-zero exit, which is why cell 16's `handleResult is not defined` was caught in an earlier session while a red assertion would not have been. **Two real reds were riding through a run reported "✅ DEPLOY GATE PASSED":** (1) **`STRIP-mixed-actions`** — v1.39's multi-select made `chosen` merely the FIRST of the selection, so ticking נישה פרו on top of a suggested org left **"אשר השמה" on the button again** — the exact bug Yariv caught, re-introduced, with the comment above the line still describing it. Fixed: place_direct survives only for a single student-suggested org (you cannot place someone in two places, and a list org reaches placement through an interview). Proven `[place_direct,send_cv]` → `[send_cv]`. (2) **`QUEUE-lists-who-waits`** — "ממתינים לך" was rendering at **877px on a 375×812 phone, 65px BELOW the fold**, while its own comment says "at the head of the screen"; the cell also measured from wherever the previous section left the scroll, so it was non-deterministic. Now scrolls to top first, reports `top=`, and the queue moved above the filter row → **660px, above the fold**. `teardown()` now sets `process.exitCode = 1` on any `pass === false`; `pass: null` stays non-fatal, because an honest "could not check" is not a failure. Re-run after the fix: **64 suites, 246 pass, 0 fail, 2 skips — and that PASS now means something.**

- 2026-08-10 (later⁸) — ✅ **v1.40.1+build.117 LIVE (Pages `da7ffae5`) — verified on PRODUCTION, 20/20.** Gate before deploy: **64 suites / 251 pass / 0 fail / 2 skips**, with failures now actually propagating. `sweep-placement-flow.mjs` against `practicum.yarivitzkovich.org`: **20/20 on phone AND desktop** — strips render on all 12 rows (0 practicum rows without one), **ⓘ opens employer details 4/4** (the thing Yariv asked for: *"like the one i want to see the org details"*), every action opens its confirmation and cancels cleanly, the turn filter narrows and restores, chips stay inside the box (0.0px), no sideways scroll, ranking order kept, 26 employer rows all explain themselves, no uncaught page errors. **The first prod run read 18/20** — both failures were the same ⓘ check reporting `0/0`: rows collapse by default since v1.37, so the chips and their ⓘ are not in the DOM until a row is opened, and the sweep never opened one. **A probe that finds nothing must not read as a feature that is gone**; it now expands first, and the check went 0/0 → 4/4. Response page re-verified on v1.40.1 against a real send, logged out, at 390×844: `stage=awaiting_reply`, 3 buttons, `session=0`, `sideScroll=0px`.

  **OPEN — needs Yariv, one command** (the classifier blocks it, correctly: it deletes from shared production student data):
  ```
  cd ~/Code/practicum-v2 && node scripts/purge-orphan-dispatches.mjs --apply
  ```
  Run it with no app tab open. Dry-run first (omit `--apply`) if you want to read the list. It clears the orphaned dispatch rows, the `@audit.local` fixture students and employers, and dismisses the two fixture org-suggestions still showing in the banner at the top of the students page. Your four real sends are preserved — the script verifies that before it writes.

- 2026-08-11 — ✏️ **Yariv's three corrections to the answer page, and one of them says I had the model backwards.**
  1. **"המעסיק צריך להבין שהוא מתקשר את מועד הראיון למועמד ולא אני או רחל."** The confirmation read *"נעדכן את הסטודנט/ית שתוזמנו לראיון"* — which sounds like the university does the scheduling, so **both sides would sit waiting for the other**. Now, on the button itself before the click and again in the confirmation: *"התיאום נעשה ישירות מולכם — נא ליצור קשר עם <שם> ולקבוע איתה את מועד הראיון. אנחנו לא מתאמים את הראיון עבורכם."* Stage 2 says it too.
  2. **"הקבלה היא קבלה לפרקטיקום וצריך לדייק את הכיתוב."** *"מתקבל/ת אלינו"* read like a job offer. Now *"✓ מתקבל/ת לפרקטיקום אצלנו"* / *"✕ לא מתקבל/ת לפרקטיקום"*, the question is *"האם היא מתקבלת לפרקטיקום אצלכם?"*, and the opening line says the CV was sent *להשמה בפרקטיקום*.
  3. **"החודש וחצי זה עד שיש זימון לראיון, אחר כך הזמן יותר מצומצם."** **I had this the wrong way round** — `DECISION_DAYS = 14` carried a comment attributing the month and a half to the *post-interview* wait. The long leg is **before** the invitation. Corrected with his sign-off on the numbers: **`NO_RESPONSE_DAYS = 45`** (only past that is silence really silence) and **`DECISION_DAYS = 7`** (after a ראיון the answer comes fast). **Three reminders no longer mean "give up":** they only stop the nudging — abandoning at ~day 21 would have dropped exactly the employers still about to answer. That window is now a quiet `sent`/`employer` state that explains itself. Seven new `CLOCK-*` cells pin it, plus `RESP-invite-says-employer-coordinates` and `RESP-acceptance-is-practicum` (cell 67 → **9/9**). The design spec was corrected too, since it is the living source and carried the same inversion.

- 2026-08-11 — 🔬 **Making the gate honest revealed that the gate is flaky. Both halves matter.** With red cells finally failing their suite, four consecutive full runs failed on **four DIFFERENT, disjoint sets**: (SLOT-available, AUTOSAVE-pending, PRIVATE-admin-affiliation, E2E-suggest-to-placement) → (CARD-history, STATUS-option, ORGHUB-union-cards) → (AUTOGREEN-ready, AUTOGREEN-not, ROW-toggle-saves) → (ZOOM×3, SUGGEST-firstchoice, ORGHUB-send-takes-place, STRIP-narrow-containment). **Every one passed when run on its own.** Checked rather than assumed: the gate runs suites strictly sequentially (`await proc.on('exit')`), and the blob is small (0.25 MB, 379 ms). **Real bug found and fixed on the way:** 25 cells PATCHed the whole `practicum_data` blob with **no version guard** — read → modify → write, so anything written between one cell's read and its write was silently reverted, including another cell's seed. That is the same failure that cost a release on 2026-08-09. New `mutateData(fn)` in audit-lib does CAS like `saveSnapshot` and hands the mutator **fresh** data each attempt, so a lost race is re-applied instead of clobbering; all 50 call sites migrated (`data =>` deliberately shadows the caller's stale read), 24 by a balanced-paren transform and `27-zoom-link` by hand because it mutated in place. **It worked, measurably: dispatches went 234 → 7 and fixture students 9 → 0, because cleanups now land instead of overwriting each other.** What remains is timing — cells wait with fixed sleeps, and sixty browser launches into a run the machine is slower than in isolation. The gate now **re-runs a failing suite once**: fail twice = real and the gate still fails; pass on retry = printed by name as **FLAKY**, so the list is visible every run and cannot quietly grow. Replacing the fixed sleeps with condition waits is the real fix and is **still owed**.

- 2026-08-11 — 🧹 **`24-cancel-dialog` had been silently skipping since the OrgHub redesign.** It looked for a button labelled `בטל מועמדות`; `1d8fa560` renamed that out of existence, so the probe found nothing and reported `0/0` — which reads exactly like "the feature is gone" and counts as neither pass nor fail. The confirm is now reached by unticking **קו״ח נשלח**, which only renders when a **dispatch** exists, and the seed created only a preference. Seeds a dispatch, targets `[data-sent-cv]`, cleans up after itself: `0/0` → **dialogOnTop=true, actionClickable=true**. Same lesson as the ⓘ probe yesterday, and now recorded as a standing rule: **a probe that finds zero subjects is not a result.**

- 2026-08-11 (cont.) — 🛑 **Stopped short of deploying, deliberately. The three corrections are built and verified; the gate is not green, so nothing shipped.** Prod remains **v1.40.1+build.117** — Yariv's corrections are **NOT live yet**.
  * **Fixed and proven** since the flake hunt began: `31-employer-year-lens` (read the employers list straight after `networkidle`, but it is a `client:only` island — now waits for the row; failed TWICE in-gate, passed always in isolation) and `11-email-personalization` (an 800 ms guess → waits for the island and for the row it is about). Both are out of the failing/flaky lists.
  * **Gate now separates flaky from broken:** a failing suite is re-run once. Fails twice = real, gate still fails. Passes on retry = printed **by name** as FLAKY with a note that fixed sleeps are the cause, so the list is visible every run and cannot quietly grow. Latest run: 1 flaky (`55-coordinator-edit-log`).
  * **`58-orghub-cards` took 5,477s.** Its assertions finished at +6s; the **cleanup** ran to +5,365s. The loop is bounded (6 CAS tries), so those were **`fetch` calls hanging with no timeout**. No audit HTTP call has a timeout — that is the next fix, and it is unwritten because I will not ship code I cannot verify at the end of a long session.
  * **Honest caveat on my own method:** I have now run **ten full gates** against the one shared Supabase project in a few hours. Slow or throttled responses are a plausible contributor to the wandering failures themselves, which means some of what I measured may be an artifact of measuring it so often. Said plainly rather than presented as a clean diagnosis.
  * **Owed, in order:** (1) timeouts on every audit `fetch`; (2) replace the remaining fixed sleeps with condition waits — that empties the FLAKY list for real; (3) re-run the gate on a rested project and deploy the three corrections.

- 2026-08-11 — 🚀 **v1.41.0+build.118 LIVE (Pages `2fa9ea7f`) — the row buttons now do what they say.** Gate **257 pass / 0 fail / 9 skips, zero flaky suites** — the first fully clean run since red cells started failing their suite. Also carries the three answer-page corrections from earlier today, which had been built but not deployed.
  * **THE BUG (Yariv, on הדר עוזירי → מערך הדיגיטל הלאומי): "לחצתי אישור והמערכת עדכנה שהנתונים נשמרו אבל לא היה שינוי … ניכר ששום כפתור לא באמת עבד מחוץ למערכת."** Reproduced exactly by seeding a student in her state: the confirmation promised *"הסטודנט/ית יירשם/תירשם כמשובץ/ת בארגון, ייתפס מקום בארגון"*, and `runPlacementAction` then hit `if (action.id !== 'adopt') { setEditing(student); return; }` — the card opened, so it LOOKED like something happened, and the database was untouched (`accepted=(none), pref=(none), slot=available`). The dialog was a promise the code never kept.
  * **Fix:** the placement moved into `lib/dispatch.placeDirect()` — pure, materialising first (a student whose orgs live only in the legacy fields has an empty `preferences[]`, so there would be nothing to place), and minting a place for an org the student brought when there is no shared capacity. **Both callers now run that one function**: the row, and `OrgHub.handlePlaceDirect` (whose ~30 lines it replaces). The row was avoiding a logic fork; it had achieved that by doing nothing at all.
  * **Proven RED→GREEN on prod, not just locally.** New cell **68-row-place-direct** asserts the DATABASE, because a toast alone passed on the broken code: with the handler removed → `accepted=(none), pref=(none), slot=available/not hers, editorOpen=true`; with it → `accepted=<org>, pref=placed, slot=placed/hers, editorOpen=false`. **3/3 against `practicum.yarivitzkovich.org`.**
  * **The whole CLASS, not the one instance:** `approve_org` told the same lie ("הארגון יאושר ויתווסף…", then only opened the card). Its copy now says the card opens — and there that is right, since approving an org the student proposed means checking it and speaking to the employer first. New **`lint-row-actions-do-what-they-say.mjs`** enforces it structurally: every action in the catalogue is either handled in `runPlacementAction` or says *"ייפתח כרטיס"*. A new action is checked automatically. Proven red by restoring the old `approve_org` wording. The gate now runs any `scripts/audit/lint-*.mjs` first — no wiring needed to add one.
  * **"הטלפון של הארגון שמוצג אינו לחיץ":** the number was a plain `<span>` with a call ICON beside it; on a phone the number is what you reach for. It is now a `tel:` link (email likewise `mailto:`). The href strips to digits and `+`, which also removes the **U+202D/U+202C direction-override characters** that wrap the live מערך הדיגיטל הלאומי number (`‭+972 50-2178097‬`) and would have broken `tel:` silently. Cell `STRIP-phone-is-tappable` pins it; green on prod.
  * **A cascade fixed on the way:** `STRIP-turn-filter` clicked the "ours" filter and never cleared it. With nobody currently in that turn the list went empty, and every later cell on the page then reported "no info control" — reading as a missing feature rather than an empty screen. It now skips honestly when there is no such student, and restores the filter.

- 2026-08-11 — ❓ **"האם נשלחה הודעה להדר, ומה היה הנוסח?" — no message was sent, and none could have been.** Checked three ways rather than asserting from memory: (1) the placement path (`handlePlaceDirect`, `handleResult`) contains no send of any kind — only `onDataChange` and a toast; (2) her record carries no message marker at all; (3) the activity log has 5 entries naming her, all `עריכת פרטי סטודנט/ית`, and the only message-shaped action in the last 600 entries is `קו״ח נשלחו`, which does not appear for her. **She also has zero dispatch rows** — no CV was ever sent to that org through the app. Every student-facing message in this system requires an explicit button that OPENS WhatsApp or mail for Yariv to send by hand; nothing auto-sends. The only student-notification templates that exist (`studentNotifyApproved*`) belong to approving a suggested ORG on the Employers page, not to placement — text: *"שלום {studentName}, הצעת המעסיק "{employerName}" אושרה ({scope}). ניתן לבחור אותה כהעדפה בטופס שלך. {adminName}"*. **There is no "notify the student she was placed" message in the product at all** — if that is wanted, it is a new capability, not a repair.

- 2026-08-11 — 🚀 **v1.42.0+build.119 LIVE (Pages `a8d641b1`) — the three items Yariv left with me.** He ran the purge himself (verified: **0 fixture students, 0 orphaned dispatches, 0 stray banner suggestions**, 85 students / 28 employers / 5 dispatches), keeps the iPhone smoke test, and dropped the placement-notification idea for now.
  * **(3) The silence threshold is 14, and the course's own field finally means something.** Yariv chose 14 over the 7 he had asked for on 2026-08-09; three reminders at a 14-day cadence run to day 42, just inside the 45-day window. **A correction I owe the record: I told him the card's ⏱ chip was "still using 14 while the row used 7". That was wrong** — I read it off a stale 2026-08-09 log line instead of the code; the chip had already been switched to the shared constant that same day. **What WAS broken is what the check turned up: `reviewAgingThresholdDays` is editable in the course settings and was read by nothing** — a control that claims an effect and has none, the same defect as yesterday's row button. Both the classifier and the card now resolve `course.reviewAgingThresholdDays ?? SILENCE_DAYS`, so a course can set its own patience and the two surfaces still agree with each other. Three new cells: honoured at 30, still fires past 30, falls back when unset. **Effect on the live board: שובל is 7 days out and no longer "late" — she becomes Yariv's turn on 18.8, and the other three on 24.8.** That directly revises what I told him an hour ago ("שובל flips tomorrow"), which was true under 7.
  * **(5) Every audit request now has a deadline.** `58-orghub-cards` had taken **5,477 seconds** — assertions done at +6s, cleanup running to +5,365s on `fetch` calls that could hang for ever. Patched once in audit-lib rather than at ~50 call sites, since several cells carry private helpers built on bare `fetch`; a caller passing its own signal is left alone. Proven by forcing it: *"fetch timed out after 1ms: https://…"*. `mutateData` now retries a timed-out attempt instead of dying on it — the point of a deadline is to keep moving.
  * **(6) The fixed sleeps are gone where they mattered.** 83 occurrences across 58 files read the DOM a fixed 800–1800ms after a reload — but every screen is a `client:only` island, so `networkidle` means the network went quiet, not that anything rendered. New `appReady()` waits for the render itself and was inserted after every reload/goto; **0 of that pattern remain**. It never throws, so a timeout falls through to the cell's own honest assertion.
  * **THE PROOF, and it is the whole point: two consecutive full gates, 260 pass / 0 fail / 9 skips, ZERO flaky suites — no suite needed its retry.** Before this work, four consecutive runs failed on four different disjoint sets. The retry mechanism is still in place as a net, but it is currently catching nothing.
  * Design docs updated where they stated the rule (7 → 14) in the strip brief, the send-flow spec and the response spec; the post-interview 7 was left alone because it is still 7, and the students-page mock now illustrates a genuinely late row (15 days) instead of one that the new rule would not flag.

- 2026-08-11 — 🚀 **v1.42.1+build.120 LIVE (Pages `c8db53a3`) — the per-chip action now sits inside the chip it acts on.** Yariv, from his phone: *"אם ה'לא נשלח' זה כותרת ל‑2 ו‑3 אז זה לא מעוצב בצורה ברורה ונראה יותר חלק מ‑1 — מבלבל."* He read it exactly right, and it is **geometry, not wording**: `↩︎ לא נשלח` belongs to choice **1**, but was rendered as a SIBLING after that chip's bordered box — so it floated between chip 1 and chip 2, attached to neither by eye, and could just as well be read as a heading over 2 and 3. Same for `✕ הסר מהדירוג` after chip 3. Both now render INSIDE their own chip's border, under that chip's own text; verified by measuring rectangles, not by looking (`inside=true` on all 9 chips on screen, `strays=0`). The label also became **`↩︎ ההודעה הזו לא נשלחה`** — "לא נשלח" alone reads as a status, which is precisely the ambiguity he hit. New cell `SEEDED-action-inside-its-chip` pins the containment so it cannot drift back.

- 2026-08-11 — 🎯 **`69-strip-states-seeded` — the strip's behaviour is finally tested on states we CREATE.** Cell 64's browser layer is deliberately read-only so it stays green while Yariv works, and the price came due today: with his board quiet (0 students in the ours turn, no mixed row, nobody waiting), **five of its cells skipped**. Honest, but by the rule written yesterday a probe that never runs is not coverage — and it was the FEATURE'S MOST IMPORTANT BEHAVIOURS going untested precisely because the data was calm. Cell 69 seeds three students and three employers (an org the student brought, a shared org with room, and one whose only place is already held by a seeded classmate), asserts **turn filter narrows and restores · the waiting queue lists everyone with one action each · picking a LIST org on a mixed row drops "אשר השמה" · the send button names the org · a blocked chip says why and names the holder · every action is inside its chip**, then deletes all of it. **6/6 locally and 6/6 against production.** Proven discriminating, not merely green: reverting the `placeable` logic turns `SEEDED-mixed-actions` red (`[place_direct,send_cv]` unchanged after picking the list org). Cell 64 keeps its read-only layer and its header now says why both exist.

- 2026-08-11 — ✅ **Full verification pass on PRODUCTION at v1.42.1.** Gate before deploy: **266 pass / 0 fail / 9 skips, zero flaky suites**. Live site: **sweep 20/20** on phone and desktop; **68-row-place-direct 3/3** (the placement really writes: `accepted=<org>, pref=placed, slot=placed/hers`); **69 6/6**. The sweep had reported 18/20 first — its turn-filter check called an empty result FAIL when nobody is in that turn, the same zero-subject defect as its ⓘ probe yesterday; it now reports `n/a` and says why. Data left exactly as Yariv's purge left it: **85 students, 0 fixtures, 28 employers, 5 dispatches, 0 orphans**.

- 2026-08-11 — 🚀 **v1.42.2+build.121 LIVE (Pages `cff88099`) — one sentence per chip, and the actions stopped shouting.** Yariv on the v1.42.1 screenshot: *"זה לא ממש משנה. 'ההודעה הזו לא נשלחה' מופיעה כאשר מעליה כתוב — ואכן בפועל זה מה שקרה — ההודעה נשלחה … ללא הכותרות זה הרבה יותר ברור: Codeoasis נשלח אתמול בצמוד לשם הארגון ללא כותרת מפוצצת, ו‑TLV תפוס. לא צריך את ההכפלה או את הכותרת המנותקת והגדולה."* Moving the button inside the chip fixed the ownership and left the two real problems standing.
  * **The duplication was literal.** `placementStatus` sets `suffix = blockedReason` for a full org, and the component printed `blockedReason` AGAIN as a block underneath — so chip 3 read *"TLVtech · תפוס — עדי גורביץ' בתהליך שם"* with the identical sentence repeated below it. One rendering now; the `data-org-blocked` marker moved onto the suffix so the cells still find it.
  * **The undo shouted over the line it corrects.** A bold dashed banner reading "ההודעה הזו לא נשלחה" sat directly under "נשלח אתמול" — read as a contradiction, not an undo. Both per-chip actions are now 17px marks on the chip's own line beside the ⓘ (`↩` to say a message never went, `✕` to drop a blocked choice), with the full sentence in the tooltip. The sentence states the state; the mark offers the fix. **The strip is 288px instead of ~400.**
  * **AND THE CELL CAUGHT A REAL GAP WHILE I WAS THERE.** `SEEDED-blocked-explains` went red reporting a chip with **no reason at all** — because `chipFor` only received an explanatory suffix in the ALREADY-SENT branch. For a student before any CV goes out — exactly when you are deciding where to send — a full organization looked identical to an open one. That is Yariv's 2026-08-10 report ("כתוב של‑1 עדיין לא נשלח אבל זה לא נשלח כי אין מקום"), fixed then for one branch only. `chipFor` now defaults the suffix for every `plain` chip in every state: "טרם נשלח" when there is a place, the reason when there is not.
  * **Both proven RED→GREEN, not merely green:** restoring the second rendering turns the cell red with `printed 2×` (green reads `printed 1×`); the missing-suffix case reported `"2ארגון תפוס …"` with `marked=false` before the `chipFor` fix. The duplication check counts OCCURRENCES of the reason — counting innerText lines was the wrong ruler, since the rank badge and each mark are their own "line".
  * Gate **267 pass / 0 fail / 8 skips, zero flaky** (one skip fewer — a cell that finally has something to assert). Production: **sweep 20/20**, **69 6/6**, data left clean.

- 2026-08-11 — 🚀 **v1.42.3+build.122 LIVE (Pages `5bfff2b8`) — Yariv's candidates screen cleaned of MY test data, and two layout bugs behind it.** He sent two screenshots: *"יש הרבה מאוד מועמדים שיש לנקות אותם מהבדיקות שביצעת … קורות החיים העיגול שמקיף אותם דורס את השם. אולי צריך לחשוב על עיצוב נקי יותר של הדף הזה."*
  * **The clutter was mine, and bigger than the earlier purge caught: 56 of 85 rows in the SUBMISSIONS INBOX, 18 of 31 CANDIDATES, and 2 more students.** My purge script had only covered students / employers / dispatches. **Root cause of the submissions pile-up, found by measuring:** cells 01 and 61 wrote their cleanup filter as `email=like.audit-…-%40audit.local` — **`%40` decodes to `@`, so the intended wildcard vanished and the delete matched NOTHING**. Proven on live data before touching it: the old form matched **0** rows, `*@audit.local` matched exactly **56**. Both cells fixed, 01 now also removes its own rows at the END (it only pre-cleaned), and three cells that still wrote the blob blind (`sbPatch`, missed by yesterday's CAS sweep because it is a different helper name) now use `mutateData`. **Applied: candidates 31→13 (all real), students 85→83, submissions inbox 85→29, zero fixtures anywhere. Yariv's 5 dispatches and 28 employers untouched.**
  * **"העיגול דורס את השם" was the name overflowing INTO the circle, not the circle over the name.** A long unbroken token (`1786285223226`) has no break opportunity, so it ran past its flex column and under the CV control. `overflowWrap: anywhere` on the submission name and email.
  * **And the page itself scrolled sideways — 64px at 390px wide.** Nobody would have connected that to a popover they never opened: an `InfoPopover` sized `minWidth: 340 / maxWidth: 420` is 440px on a 390px screen, and because it is only `invisible` rather than unmounted it widened the PAGE while never being seen. Capped both popovers to `calc(100vw - 28px)`.
  * **New cell `70-no-side-scroll`, and it earned its place twice.** It walks students / candidates / employers at 390px and, on overflow, NAMES the widest element responsible rather than reporting a symptom. Its second assertion injects an unbreakable 65-character name into the widest DATA-BEARING element on each screen — because purging the fixtures made the original symptom disappear, which would have left the cell passing for a reason that will not hold for the next long name a real student submits. **That stress found a defect I had not: the student's name in the expanded card (`text-[22px]`) had no break rule and overflowed by 218px.** Fixed, plus the "ממתינים לך" queue name, which had `flex: none` and no truncation while the list rows truncate.
  * Honest note on the sequence: the first version of that cell passed after I reverted the popover cap, i.e. it was **not** discriminating — the popovers were not the cause; the fixture rows' long names were. Said rather than papered over, and the cell was rebuilt to test the RULE instead of the data.
  * Gate **272 pass / 0 fail / 9 skips, zero flaky**. Production: **6/6 on the new cell**.

- 2026-08-11 — 📌 **Still open, and it is a design question rather than a bug:** Yariv's *"אולי צריך לחשוב על עיצוב נקי יותר של הדף הזה של מועמדים לפני קליטתם"*. The submissions inbox is now legible because the noise is gone, but the row is still a dense stack — name, phone, email, tracks, GPA, course, year, date, then CV / טופס / שאלון / delete. **Nothing designed yet, deliberately:** the binding rule is a design brief with his approval BEFORE building, and he has not seen one for this screen.

- 2026-08-13 — 📋 **מועמדים: מי שכבר נכנס יוצא מהרשימה — built, NOT deployed.** Yariv approved the brief item-by-item first, per the standing rule. Prod stays **v1.42.3+build.122**; this is a PR, nothing is live.
  * **THE REPORT, and it started with him catching me in the wrong repository.** He sent a screenshot of the students screen and said *"דף סטודנטים ממש לא נראה כך אני לא בטוח שאתה עובד על אותה מערכת שנקראת פרקטיקום 2. המידע שאתה מציג לא אמין."* He was right. I had spent the session building against `itzkovichyariv-star/Practicum` — the v1 single-file app, `main` untouched since 2026-04-26. The commit stamped in his footer, `2a91f617`, does not exist there. Everything I had measured, including a "verified byte-identical to main" claim, was measured against the wrong `main`. That PR is closed.
  * **v1 was not merely stale, it was pointed at this database.** Both apps use project `vpqgmcmavnszcnakhiat`, table `practicum_data`, `org_id='default'`. v1's `supabaseSaveData()` rebuilds the whole blob from **fourteen keys** and writes it with a blind `.update()` — no version guard — on an **800 ms debounce after any change**. Our blob carries `dispatches`, `trainers`, `employerApprovalRequests`, `placementSettings`, `interviewZoomLinks`, `coordinatorEmail`, `supervisorEmail`, `notifyEmails`, `publicUrl` — none of which v1 knows about. A signed-in session there could have dropped every one of them from the live row. `dispatches` is where the CV sends live. Archived in `Practicum#2`: the served page is now a static notice, the app preserved as `.txt` so Pages cannot launch it.
  * **What shipped here (four items, all approved):** a converted candidate leaves the working list into an archive, derived from `convertedToStudentId` so there is no second flag to keep in step · the ramzor counts follow the shown pool, so tabs and list always agree · CV and טופס are chips on the row — filled = a real link to the file, dashed = never submitted, and deliberately **not** a link so it is not a dead control · the contact buttons now use the SAME `contactBtn` / icon components as the student row.
  * **The icons were his catch too:** *"האיקונים של התקשורת בדף מועמד נראים מוזר אבל מניח שהם ידמו לאיקונים הקיימים בדף סטודנט."* They did not. The student row drew SVG icons in a 36px wine circle; the candidate row went through `RowActions`, which draws 📞 💬 ✉ as literal emoji in a 32px ink circle. `contactBtn`/`contactStyle` are now declared **once**, exported, and shared. New **`lint-contact-buttons-match.mjs`** enforces the class, not the instance — **proven RED on the pre-change file** (`does not import contactBtn`, `no PhoneIcon / WhatsAppIcon / MailIcon`) and green after. Lectures and trainers still use `RowActions`; out of scope, and the lint's `SCREENS` list says so.
  * **REJECTED, recorded so it does not creep back:** I proposed stopping the conversion writing the interview summary and score into the student's `notes`, on the strength of his earlier *"מרגע שהמועמד הפך סטודנט לא מעניין לראות את תיק המועמדות שלו"*. He reversed it: *"אם טופס הגשת המועמדות וסיכום ראיון עוברים לדף הסטודנט זה בסדר שימשיכו גם לעבור בגרסה החדשה."* The conversion is untouched.
  * **The students page is PROVEN unchanged, not asserted.** Its rendered DOM was captured from the baseline build and from the branch against identical fixture data: **35,952 characters, `diff` clean**. The only edit to `StudentsPage.tsx` is hoisting two style constants from inside `StudentRow` to module scope and exporting them — same names, same values, still in scope where they were used.
  * **New `scripts/preview-candidates.mjs` + `scripts/students-dom-baseline.mjs` — a fully offline lab.** They serve `dist/` locally and route **every** off-origin request to a fixture, so a screen can be looked at without reading or writing the live project while Yariv is working. That routing is the load-bearing part: a narrower pattern left the fonts and realtime calls going out, and the first version of the harness rendered a blank page for exactly that reason. Honest limit: Playwright cannot route WebSockets, so the realtime socket still attempts and fails at the proxy — harmless for rendering, but it means these two are a **preview, not a substitute for the gate**.
  * **The behaviour is now actually tested, and the test is proven discriminating.** New `scripts/candidates-archive-check.mjs` drives the real screen offline and asserts rather than screenshots: the converted row is out by default and the plain ones are not, the tab numbers follow the shown pool, the toggle names its count and restores the row with the בארכיון marker, a submitted file renders as `<a href>` and a missing one deliberately does NOT, the three contact buttons each render an `<svg>` and no glyph, and at 390px there is no side-scroll and the phone number occupies exactly one line box. **Baseline 5/19 exit 1 → branch 19/19 exit 0**, with the old emoji showing up in the failure text as `NO-SVG+"📞" NO-SVG+"💬" NO-SVG+"✉"`.
  * **A correction on my own method, since I nearly shipped a false green.** My first discrimination run "passed" on the baseline — because I stashed only the uncommitted fix while the feature was already committed, so `git stash` was comparing the branch against itself. The real baseline needs `git checkout origin/main -- src/`. The second defect was worse: the check CRASHED on the missing toggle (`locator.check` timeout) instead of failing, which aborted the run and took the CHIP and ICON sections with it — and piping through `tail` swallowed the non-zero exit, so it read as a pass. A missing control is now a recorded failure with the remaining checks still run.
  * **`71-candidates-archive.mjs` written for the gate**, in house style, seeding one archived candidate + one plain control + the student the first points at, and removing all three. Every assertion is RELATIVE — membership and deltas, never an absolute count — because the live board already holds real archived candidates and a cell that assumes an empty board goes red for the wrong reason. **It has NOT been executed** (it seeds through the live project, for a branch nobody has merged) and says so in its own header; its selectors and every expected value were validated against the offline lab first: `.ramzor-bar` "הכל" 1→2, archived hidden→visible, marker present, zero toggles inside `li[data-info-row]`.
  * **The 390px wrap is fixed** — it was pre-existing, confirmed on the baseline screenshot before touching it. The phone number is `whitespace-nowrap` (one token to a human, useless broken as "058-" / "7778888"); the email stays the one that truncates, being long, variable and readable short. Pinned by NARROW-phone, which counts the span's client rects rather than eyeballing it.

- 2026-08-13 (later) — 🚢 **`npm run ship` — dev server → gate → deploy as one command, because three separate accidents cost a deploy each and not one of them was about the code.**
  * **(1) A comment cannot guard a command.** `npm run deploy   # only if the gate is green` — npm forwards trailing args to the script, so the comment reached wrangler: `✘ Unknown arguments: #, only, if, the, gate, is, green`. **(2) The gate never ran**: `npm run dev` serves 4321, the gate demanded 4325, so it exited "dev server NOT reachable" — and because the two were pasted as separate lines, the deploy ran anyway, unguarded. **(3) Backgrounded with `&`, the dev server is SUSPENDED by zsh** the moment Astro reads the keyboard for its shortcuts (`suspended (tty input)`), so even started on the right port it stopped answering before the gate looked.
  * `scripts/ship.mjs` closes all three: the server is spawned with **stdio ignored** (no tty to read → no SIGTTIN → cannot be suspended), one constant feeds the port, readiness is **polled until it actually answers** rather than guessed at with a sleep, and the deploy is downstream of the gate's **exit code**, not of a comment. Torn down on success, on a red gate, and on Ctrl-C. `--check` (prove the server comes up) and `--dry` (gate, then stop) exist so it can be exercised without deploying.
  * **`deploy-gate.mjs` had two hard-coded literals and both bit.** `ROOT` was `/Users/yarivitzkovich/Code/practicum-v2` — now derived from the script's own location. `DEV_URL` was a lone `http://localhost:4325` while every cell resolves `AUDIT_BASE_URL || …:4325`; that asymmetry is what cost 2026-08-09 a run (cell 48, same class, opposite direction) and what cost today's deploy. One expression now feeds both, with `GATE_PORT` as an override.
  * **Verified as far as is honest from here:** `--check` brings the server up on 4325 in 8.9s and stops it cleanly; the gate's probe then passes and both lints run green (`lint-contact-buttons-match`, `lint-row-actions-do-what-they-say`) with `--only 99` so no cell touched the live project. **The full gate and the deploy itself remain unrun by me** — the deploy command is classifier-blocked for Claude and this session has no Cloudflare credentials at all (`wrangler whoami` → not authenticated); it is Yariv's keystroke, as the log has recorded since 2026-07-15.
  * **Noticed, not fixed:** `deploy-gate.mjs --only <prefix-that-matches-nothing>` prints `✅ DEPLOY GATE PASSED` having run zero cells. Harmless on a real run, but it is the same "a probe that finds zero subjects is not a result" rule the audit cells are held to, applied to the gate itself. Left alone deliberately — I will not change the gate's own pass/fail logic while unable to run it end to end.

- 2026-08-13 (later²) — 🟥→🟩 **First real gate run of the candidates work: 67 green, ONE red, and the red was mine.** Yariv ran `npm run ship` on his Mac. The command itself worked — server up on 4325, gate ran end to end, deploy correctly BLOCKED by the red. Production untouched, still **v1.42.3+build.122**.
  * **`71-candidates-archive` passed 3/3 on its first ever execution** — the cell I wrote blind and could only validate offline: `archivedVisible=false, plainVisible=true` · `back=true, marked=true, הכל 1→2` · `toggleInsideRows=0, rowCheckboxes=2`. The offline selector validation held against the live board.
  * **`20-passed-card` went red twice (so real, not flaky): `candidateRow=false`.** It seeds a passed **+ converted** candidate and looks for its row on the candidates page — which is exactly the row the archive now hides. A real consequence of the approved change, not a defect: the card is still reachable, one tick of «הצג ארכיון» away.
  * **The expectation moved, not the code — and the cell got STRONGER, not tolerant.** Same call as cell 37 on 2026-08-09. It now asserts BOTH halves of the new rule: `hiddenBeforeArchive` must be **true** before the toggle, and the row must come back and open after it. Merely revealing the archive up front would have gone green while losing the ability to notice converted candidates leaking back into the default list — the note for that case reads "the archive is leaking". The status-tab click was dropped: with the archive revealed the timestamped name is unique, and filtering as well only added a way to fail for an unrelated reason.
  * Validated offline before pushing, using cell 20's exact seed shape (no `year`, context `__all__`, control candidate alongside): hidden=true · control visible=true · toggle=1 · row back=true · pencil found=true · editor opened=true · 0 page errors.
  * Honest note on the ⏸ skips in the summary: `08-org-availability`, `09-org-history-dedup`, `50-draft-and-submit-blocker`, and four cells inside `64-placement-strip` reported no subjects — nobody in the "ours" turn, no unsubmitted feedback token, no pending suggestions. They exit 0 and are not failures, but by the standing rule a probe that finds zero subjects is not a result either. Unchanged by this work and not investigated here.

- 2026-08-13 (later³) — 🔒 **`npm run deploy` now REFUSES unless the gate passed. Five failed attempts, and the last two failed the same way as the first.**
  * **Scrollback outlives advice.** I replaced the broken command block with `npm run ship` and explained why — and the next two runs used the OLD block anyway, because it was still sitting in the terminal history and is trivially easy to re-copy. Both times: `zsh: command not found: #`, the gate skipped with "dev server NOT reachable" (dev on 4321, gate on 4325), the deploy ran ANYWAY because the lines were unconnected, and wrangler died on `Unknown arguments: #, only, if, the, gate, is, green`. The run also began `git checkout` rather than `git pull`, so the fix never even arrived — the bump stamped `build.123.e6496a08`, the commit BEFORE it.
  * **The lesson is not "write clearer instructions".** A comment cannot guard a command; only an exit code can. New `scripts/require-gate.mjs` runs FIRST in `predeploy` and demands a token that **only `ship.mjs` sets, and only after the gate has exited 0**. A bare `npm run deploy` — typed, or pasted from stale scrollback — now stops with an explanation of what to run instead.
  * **It refuses before `bump-version`, deliberately**, so a refused deploy leaves the tree untouched: no version bump, no `sw.js` cache rename, nothing to `git checkout --` afterwards. That was a real annoyance twice today.
  * Proven both directions: bare `npm run predeploy` → exit 1, and `git status` on `version.ts`/`sw.js` shows **nothing touched**; `SHIP_GATE_PASSED=1 npm run predeploy` → exit 0 and bumps normally (test bump reverted).
  * Escape hatch is deliberately awkward to type by accident: `SHIP_GATE_PASSED=i-ran-the-gate-myself npm run deploy`.
